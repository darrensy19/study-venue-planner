# BACKLOG.md

Deferred items surfaced by `/close`, `/loose-ends` and design rounds — parked, not forgotten.
Not for active or in-progress work (see PLAN.md / DECISIONS.md). Flip Status
to `closed` in place rather than deleting a row; detail stays below for the
record.

| ID    | Date       | Source | Item | Status |
| ----- | ---------- | ------ | ---- | ------ |
| BL-001 | 2026-09-03 | /close | `scraper/hours.py`'s `_decompose_current_period` emits a spurious zero-length `{open:0, close:0}` entry when a `currentOpeningHours` period closes exactly at 00:00 on the next date (e.g. `08-29 07:30 → 08-30 00:00`), instead of emitting no entry for that date at all. Reproduced directly against the shipped `IMP-006` parser. Fix: skip emitting an entry when the decomposed `open == close` for that offset (the half-open `[open, close)` interval doesn't touch a date the close merely reaches). Fixed in `IMP-007` (closed 2026-09-03). | closed |
| BL-002 | 2026-09-04 | /close | `web/`'s UI has never had a design/UX pass — plain vanilla `style.css` only, deliberately minimal per `CLAUDE.md`'s no-framework rule. User flagged the current form/results layout as "ugly" and "not user friendly" (2026-09-04), and explicitly chose to sequence it as its own assignment after `IMP-014` (live refresh + manual acceptance) closed, rather than pivot mid-assignment. The `web-design-guidelines` skill installed at `.claude/skills/web-design-guidelines` is intended for exactly this review. **Design confirmed 2026-09-05**: the Warm Dusk direction (Manrope, iPhone-only, dark warm palette) covering the asking screen, the result screen and a "What's checked" reference page, with six demonstration states. Imported and committed as `b5420e2` — `docs/superpowers/specs/2026-09-05-study-plan-frontend-design.md` (spec, §5 data contract, §9's 21-item acceptance checklist) and `...-study-plan-frontend-handoff.md` (per-component measurements, copy, animations, state model). Presentation only; `web/ranking.js` untouched. Sequenced as **slice 4** of `ARCH-004` §13 ("UI hierarchy, disclosure, always-visible warnings, focus"), gated on slice 2's vocabulary and behind slices 0/0b/1a/1b. Enters the protocol as `ARCH-005` — **not yet opened**, follows the pending `ARCH-004` revision-5 transcription. The two P1 policy questions it raised are settled in `DECISIONS.md` 2026-09-05 (refuse-don't-cap; 90-minute Plan A floor). Blocked on `BL-003` before build. | open |
| BL-003 | 2026-09-05 | design v1.0 | **Reconcile the frontend design's data contract against the migrated pipeline shape — do this before slice 4 is built.** `2026-09-05-study-plan-frontend-design.md` §5 is written against the pre-migration `rankVenues()` shape; `2026-09-05-review-response-design.md` §10 changes it across slices 1a/1b. Known deltas: `refusals` is replaced by `resultState` (D6, 1a); `planB.travelMinutesMid`, `latestLeaveAtState`, candidate-level `hoursStatus`/`histogramStatus`, `bestAlternative`, `snapshotEmpty`/`hardFilteredCount` are added; and §10 **forbids** the design's assumed "midpoint of `fallbacks[].travel_band`" for the fallback walk figure, since re-parsing the band in the renderer is a business rule — `planB.travelMinutesMid` exists for exactly that. §5's six "Assumed — needs confirming" entries should be resolved in the same pass. Next pending task after the `ARCH-004` transcription. | open |
| BL-004 | 2026-09-05 | design v1.0 | **Cycle-at-night cutoff (P2).** `RETURN_CYCLE_LATEST_MINUTES` is `null` ([`web/ranking.js:24`](web/ranking.js#L24), whose own comment calls it an open policy question), so cycling home at 3am passes as schedule-free and the screen says `Verified`. Needs a figure or an explicit "no cutoff" ruling. | open |
| BL-005 | 2026-09-05 | design v1.0 | **Pre-dawn gap presentation (P2).** A session ending 4am-7am is unverified with basis `pre_dawn_gap`, but currently reads identically to "no timetable data recorded". Worth its own wording, or its own outcome. | open |
| BL-006 | 2026-09-05 | design v1.0 | **Leaving the flow (P2).** Once the decision is made the next actions are outside the app: the venue address in Maps, and a pack-up alarm for `latest_leave_at`. Two taps, no new ranking data. Needs the venue's `formattedAddress` — already fetched by `scraper/places.py` — surfaced through `venues.json` to the frontend. | open |
| BL-007 | 2026-09-05 | design v1.0 | **Seat-log aggregation (P2).** The log has no return path: nothing reads the rows the Apple Shortcut writes, so logging has no payoff and stops happening. The design specifies one line under the seat verdict ("You have logged a seat here 4 of the last 5 visits") — a count, not a probability. Needs per-venue visit outcomes aggregated at generation time, shown only at 3+ visits, windowed to the last five visits and ~6 months, and kept strictly beside `seat_confidence` rather than folded into it (folding it in would be a ranking change and belongs upstream). The data exists; only the plumbing is missing. | open |
| BL-008 | 2026-09-05 | design v1.0 | **Staleness threshold (P3).** The warning copy states an age ("28 days old") without a verdict. No threshold exists above which hours should stop being trusted at all. Related: `build/refresh.py` must stamp a per-source age at build time, since there is deliberately no global `generated_at`. | open |
| BL-009 | 2026-09-05 | design v1.0 | **Light theme (P3).** The Warm Dusk palette inverses cleanly to a warm paper ground; not built. Needed if the tool is used in daylight on a phone set to light mode. | open |
| BL-010 | 2026-09-05 | design v1.0 | **Self-hosted font (P3).** Manrope is loaded from Google Fonts in the prototype. The generated page loads no external assets beyond the optional manifest, so this is either a base64 subset inlined into `style.css` or a system fallback with the type scale re-checked (Manrope is narrower than most system UI faces). | open |

---
