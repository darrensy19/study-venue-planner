# Handoff

Current project state, one active assignment, and one next action — nothing else. No rationale;
see `DECISIONS.md` for that. The block below is capped at 25 lines by `WORKFLOW.md`'s boundary
rules; when a field would exceed it, point to `PLAN.md` or the review record instead of expanding.

## Current assignment

- **ID**: `ARCH-002`
- **Work type**: architecture/high-level
- **State**: `completed`
- **Primary route**: `claude_opus` — Opus, effort high
- **Verification route**: `codex_sol_high` — Sol, effort high
- **Route triggers**: architecture spanning components; new file format (`data/venue_sources.json`) and shared cross-component invariants (registry↔meta ID agreement, coarsening prefix authority); external side effects (subprocess bridge, API calls); frozen Phase 0 artifacts; primary route is `claude_opus`
- **Baseline commit**: `e8f5cc7b71013a62f0fce39b999fbb957cbff982`
- **Artifact under review**: uncommitted working-tree diff against that baseline — `PLAN.md` and `CLAUDE.md` (the design), `reviews/LEDGER.md` (allocation), `HANDOFF.md` (this block), plus untracked `reviews/ARCH-002-gate.md`
- **Objective**: Design and formalize Phase 1's orchestration/UI-shell architecture — fetch layer, venue-source registry, coarsening, refresh orchestration, return-validator bridge, top-level ranking pipeline, HTML generation, and frontend shell
- **Scope exclusions**: all production implementation (`build/refresh.py`, `scraper/` fetchers, `web/app.js`, `web/index.template.html`, `web/style.css`, `data/venue_sources.json` itself, any test file); allocating any `IMP-###`; live API calls; `README.md`'s separate Phase 1 staleness; the flagged-but-unopened outbound-mirror `ARCH`
- **Acceptance criteria**: `PLAN.md` and `CLAUDE.md` state, without internal contradiction, all of: the generation-time Python venues/meta merge, with `ranking.js` doing no client-side merge and an ID mismatch in either direction failing generation; `data/venue_sources.json` as the canonical Phase 1 fetch registry — four required nonempty fields, unique `venue_id` and `place_id`, exact ID-set agreement with `venues_meta.json` before any API call and again at generation, seeded once from frozen `data/phase0/place_ids.csv`; `fetch_hours(source)` / `fetch_busyness(source)` taking source records, busyness searching `resolved_name` + `resolved_address` with the validated `data`-parameter retry; per-source `ok`/`stale`/`failed` with first-success known-good, no fabricated data on `failed`, independent source failure, and closure only from explicit `businessStatus`; the full coarsening contract — raw schema with offset-bearing `occurred_at`, two-location non-recursive candidate selection with 0/1/many behaviour, cursor-free processed-prefix re-projection and comparison, pre-fetch histogram stamping from that histogram's `last_success_at`, in-memory build with same-directory temp file and atomic replace, and every listed negative-path fixture obligation; the Node return-validator bridge with argv-only `subprocess.run`, JSON-only stdout, per-venue `invalid` continuing generation, and bridge failure stopping the refresh before replacement; `refresh.py` sole ownership of `data/venues.json` and `web/index.html`, the preserved eight-step order, and `make refresh` never committing and not exposed until the contract is wired; hand-maintained `holidays.json` never written by `refresh.py`, failing generation visibly when absent or malformed, with no new maintenance schema or UI state; one pure whole-dataset `ranking.js` entry point owning control/arrival/`STEP 0`/evaluation/Plan-B-before-ranking, the exhaustive ranked-and-unranked taxonomy (including explicit-`null` versus missing `access`), both refusal states, and the seven-key fallback order with `surplus_mid` compared only through `surplusSortKey()` and a validated `preference` never falling through to `venue_id`; the frontend and generation constraints with their test obligations; and the eight-step Phase 1 implementation order carrying **no** `IMP-###`, plus the acceptance set and the explicit statement that live success does not prove partial-failure, last-known-good, malformed-data or atomicity behaviour. Every stale reference named in scope is corrected, and no production code is written.
- **Required verification**: documentation and schema self-consistency only — there is no Phase 1 production code to test. `git status` and `git diff` confined to `PLAN.md`, `CLAUDE.md`, `HANDOFF.md`, `reviews/LEDGER.md`; no surviving `fetch_busyness(place_id)`, no "venues/meta merge" in a `ranking.js` or `tests/js` context, no "the list lives in `data/venue_seeds.csv`"; the `venue_sources.json` schema checked against the real `data/phase0/place_ids.csv` and `data/venues_meta.json` (28 `confident` rows, all four fields nonempty, `venue_id` and `place_id` unique, ID sets exactly equal); `node --test tests/js/*.test.js` still passing as a no-regression check; `.cross-agent-workflow/audit_due.py reviews/AUDIT-LOG.md` returning `NOT_DUE`; `reviews/LEDGER.md` read back showing exactly one `ARCH-002` row
- **Claude gate result**: `GATE_PASS`
- **Gate evidence**: `reviews/ARCH-002-gate.md`
- **Review record**: `reviews/ARCH-002.md`
- **Independent review**: 9 rounds, `codex_sol_high`. Round 1 issued `F01`–`F04`, the only findings ever raised; `F03`/`F04` `resolved` round 2, `F02` round 3, `F01` round 9 after successive corrections. Round 9 `APPROVE` — all four `resolved`, `Could not verify` **None**, no user decisions required
- **User decision**: 2026-09-03 — approved, and commit authorized; recorded in `DECISIONS.md`
- **Next action**: None — terminal. A new task requires a new assignment ID.
