# Handoff

Current project state, one active assignment, and one next action — nothing else. No rationale;
see `DECISIONS.md` for that. The block below is capped at 25 lines by `WORKFLOW.md`'s boundary
rules; when a field would exceed it, point to `PLAN.md` or the review record instead of expanding.

## Current assignment

- **ID**: `IMP-020`
- **Work type**: implementation
- **State**: `completed`
- **Primary route**: `claude_sonnet` — Sonnet, effort high — single-module implementation against an already-approved contract (`PLAN.md`'s "The returned presentation shape"), no new architecture decision
- **Verification route**: `codex_terra`, effort medium
- **Route triggers**: two hard triggers fire — (1) public interface / compatibility-contract change, same class as `IMP-019`: `ranking.js`'s returned shape gains `achievableSessionEndMid`, `bindingLimitMid`, `latestLeaveAtState`, `hoursStatus`/`histogramStatus`, `displayName`/`disambiguatedLabel`, `bestAlternative`, `refusalInstant`, `planB.travelMinutesMid`; (2) a decision-bearing adequacy claim (the stale-data structural invariant) requires non-vacuity evidence beyond a behavioural round-trip — a projector returning `{}` would otherwise pass
- **Baseline commit**: `b81c55f`
- **Artifact under review**: `web/ranking.js` (achievable-end/binding-limit plumbing through `resolveBound`/`boundBindingMetrics`/`resolveOverallFeasibilityAtArrivals`; `deriveLatestLeaveAtState`; `stripFreshnessFields`; `resolveVenueNaming`; candidate/`removed`/`travelUnknown`/`planB` naming and freshness fields; `bestAlternative`; `refusalInstant`), `tests/js/ranking.test.js` (27 new tests + 2 existing removed/travelUnknown-shape tests updated for the `name`→`displayName` rename), `web/index.html` (regenerated, no manual edits) — the diff to these three files. `app.js` untouched: every new field is additive, no existing field's meaning changed
- **Objective**: Slice 1b (`PLAN.md`'s "Phase 1 review-response slice order") — achievable end, binding limit, latest-leave state, candidate/removal/Plan B freshness and naming, Plan B transfer time, and `bestAlternative` in `web/ranking.js`. Public-shape migration step 2 of 2
- **Scope exclusions**: no `app.js` wording/vocabulary/hierarchy work (slice 2 consumes these fields; none are wired into rendering here); no `resolved_name` registry fallback (`BL-013`, still deferred — unrelated Python-side change); `refusalInstant`'s definition (`departureAbs + durationMinutes`, travel excluded, since `no_verified_return` has no single venue to hang a per-candidate instant off) is a judgement call flagged for reviewer confirmation, `PLAN.md` giving only an illustrative example, not a formula
- **Acceptance criteria**: every candidate/`planA`/`bestAlternative` carries `displayName`/`disambiguatedLabel` (area-suffixed only when the name is not unique snapshot-wide) and `hoursStatus`/`histogramStatus` read from the venue's own records; `achievableSessionEndMid` equals `arrivalMidAbs + usableMinutesMid` and is `undefined` exactly when `usableMinutesMid` is; `bindingLimitMid` is the raw binding instant (distinct from the derived `latestLeaveAt`) on both the combined and hours-only branches, tagged `"UNDETERMINED"` when COVERED with no last departure; `latestLeaveAtState` is exactly one of `future`/`past`/`undetermined`/`closed_at_arrival`; `planB` carries `travelMinutesMid` plus the fallback's own naming/freshness; `bestAlternative` is non-null only under `session_does_not_fit`; `refusalInstant` is non-`undefined` only under `no_verified_return`; `stripFreshnessFields` is proven non-vacuous by a direct test against a synthetic nested object, independent of the structural stale-data round-trip; no regressions in either test suite
- **Required verification**: `.venv/bin/pytest tests/python/ -q` (190 passing, unchanged) and `node --test tests/js/*.test.js` (233 passing, up from 206); `make generate` against the real repo, network blocked, twice, byte-identical between the two runs; seven non-vacuity spot-checks (`achievableSessionEndMid`, `bindingLimitMid`, `deriveLatestLeaveAtState`'s comparison direction, `stripFreshnessFields` reverted to a vacuous `{}` projector, `bestAlternative`/`refusalInstant`, `disambiguatedLabel`'s area-suffix condition, `planB.travelMinutesMid`) independently reverted and confirmed to fail the relevant tests, then restored — diff-clean against the pre-check baseline
- **Claude gate result**: `GATE_PASS` (invocation 1) — `reviews/IMP-020-gate.md` (round 1, `draft`)
- **Independent review**: round 1 `codex_terra` recommends `APPROVE` — no findings, no user decisions required. Verification reproduced independently: `pytest` 190 passed, `node --test` 233 passed, `make generate` ×2 SHA-256-identical; `stripFreshnessFields`'s non-vacuity separately checked
- **Review record**: `reviews/IMP-020.md` — round 1, header + recommendation, no corrections needed
- **User decision**: approved — close and commit (2026-09-06)
- **Next action**: none — assignment closed. Next in `PLAN.md`'s slice order: Slice 2 (the renderer — `web/app.js` wording, day markers, label vocabulary, naming, consuming 1a+1b's fields). Open a new ID when ready to proceed
