# Handoff

Current project state, one active assignment, and one next action — nothing else. No rationale;
see `DECISIONS.md` for that. The block below is capped at 25 lines by `WORKFLOW.md`'s boundary
rules; when a field would exceed it, point to `PLAN.md` or the review record instead of expanding.

## Current assignment

- **ID**: `IMP-021`
- **Work type**: implementation
- **State**: `completed`
- **Primary route**: `claude_sonnet` — Sonnet, effort high — single-module implementation against an already-approved contract (`PLAN.md`'s "The returned presentation shape", "Result states", "Naming and presentation hierarchy"), no new architecture decision
- **Verification route**: `claude_only` — no hard trigger fired: `web/app.js` has no public/shared contract of its own (it consumes `ranking.js`'s, unchanged here), and nothing here touches schema, architecture, security, or a promotion decision
- **Baseline commit**: `5e79252`
- **Artifact under review**: `web/app.js` (naming via `displayName`/`disambiguatedLabel` instead of a locally reconstructed name; all six `resultState` branches rendered, including `invalid_request` violations, `session_does_not_fit`'s `bestAlternative`, `no_verified_return`'s day-marked `refusalInstant`, and `nothing_evaluable`'s four simultaneous diagnostics; day markers on every instant that can cross midnight via `ranking.js`'s exported `dateFromAbs`; a label-vocabulary map for `seat_confidence`/`baseline_seatability`/busyness bands; `achievableSessionEndMid` replacing the mislabelled `sessionEndMidAbs` in the "ends ~" line; a `bindingLimitMid`-driven "leave the venue by" line; `latestLeaveAtState`-driven wording; `hoursStatus`/`histogramStatus` freshness warnings on candidates and Plan B; `planB.travelMinutesMid` transfer time and the CLAUDE.md-specified "not the Xh you asked for" salvage wording), `tests/js/app.test.js` (fixture gains a `name` field so naming comes from the pipeline, not the id; new coverage for the above)
- **Objective**: Slice 2 (`PLAN.md`'s "Phase 1 review-response slice order") — the renderer: wording, day markers, label vocabulary, naming, consuming 1a's/1b's fields, deciding nothing itself
- **Scope exclusions**: no progressive-disclosure restructuring (Plan A / Row / Disclosure surfaces) and no focus-preservation fix — both explicitly Slice 4 per the slice table; no `ranking.js` change; no `style.css` change
- **Acceptance criteria**: every rendered instant that can cross midnight carries a day marker; all six `resultState` values render distinct, correct text with no default case silently swallowing an unhandled one; `nothing_evaluable` shows all four diagnostics simultaneously, never inferring a single cause; `session_does_not_fit` renders `bestAlternative` explicitly labelled "not a recommendation"; `no_verified_return` renders `refusalInstant`, day-marked, never pre-stringified; naming uses `displayName` (with a separate area line) or `disambiguatedLabel` (inline, no area line) per the naming-hierarchy table, never a locally reconstructed name; `seat_confidence`/`baseline_seatability`/busyness bands render through the label-vocabulary map, never a raw identifier like `usually_available`; stale/failed `hoursStatus`/`histogramStatus` are always visible, on any candidate or Plan B view carrying them; the "ends ~" line uses `achievableSessionEndMid`, never the requested end; a `usableMinutesMid === undefined` candidate renders "closed when you'd arrive", never "Usable: unknown"; Plan B's salvage wording states its actual duration against the requested one; no regressions in either test suite
- **Required verification**: `.venv/bin/pytest tests/python/ -q` (unchanged pass count — this assignment touches no Python) and `node --test tests/js/*.test.js` (existing suite plus new `app.test.js` coverage, all passing); `make generate` against the real repo, network blocked, confirming the generated page still parses with the updated `app.js` inlined
- **Gate result**: `GATE_FAIL` invocation 1 (3 test-coverage gaps, no functional defect) → fixed → `GATE_PASS` invocation 2, independently re-verifying all three closures and re-running full verification fresh — `reviews/IMP-021-gate.md`. No hard trigger fires (`claude_only` verification route), so no review round is required
- **User decision**: approved — close and commit (2026-09-06)
- **Next action**: none — assignment closed. Next in `PLAN.md`'s slice order: Slice 3 (outbound feasibility, independent of 1-2), the `outbound_transport` hand-curation, then Slice 4/`BL-002` (needs slice 2's vocabulary — now settled), Slice 5 — before `ARCH-005` can open
