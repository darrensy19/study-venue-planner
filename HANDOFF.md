# Handoff

Current project state, one active assignment, and one next action — nothing else. No rationale;
see `DECISIONS.md` for that. The block below is capped at 25 lines by `WORKFLOW.md`'s boundary
rules; when a field would exceed it, point to `PLAN.md` or the review record instead of expanding.

## Current assignment

- **ID**: `IMP-006`
- **Work type**: implementation
- **State**: `completed`
- **Primary route**: `claude_sonnet` — Sonnet, effort high
- **Verification route**: `codex_terra` — Terra, medium
- **Route triggers**: new file format (`data/venue_sources.json`); frozen artifact read (`data/phase0/place_ids.csv`, bootstrap only, never modified); shared cross-component invariant (registry↔`venues_meta.json` ID-set agreement)
- **Baseline commit**: `f35c7d5453ec77acbf53f4aa3a230744a219d62b`
- **Artifact under review**: uncommitted working-tree diff against that baseline
- **Objective**: Implement Phase 1 implementation-order step 1 — the venue-source registry, its one-time bootstrap from frozen `data/phase0/place_ids.csv`, the hours parser, and `fetch_hours(source)` — per `PLAN.md`'s "Fetch layer and refresh orchestration" and "The venue-source registry"
- **Scope exclusions**: `fetch_busyness`/SerpApi (step 2), the return-validator bridge (step 3), `ranking.js` changes (step 4), the coarsening stage (step 5), frontend/generator (step 6), `build/refresh.py` orchestration and the `Makefile` target (step 7), any live API call (step 8); no edits to `data/phase0/place_ids.csv` or any other frozen Phase 0 artifact
- **Acceptance criteria**: `data/venue_sources.json` produced by `build/bootstrap_venue_sources.py` (run as `.venv/bin/python3 build/bootstrap_venue_sources.py`), one record per venue (`venue_id`, `place_id`, `resolved_name`, `resolved_address`, all four nonempty), seeded from `data/phase0/place_ids.csv`'s `confident`-match rows only (`proposed_venue_id` → `venue_id`); the bootstrap is a true one-time step, not an idempotent regenerate — it **refuses to run and writes nothing** if `data/venue_sources.json` already exists, consistent with "after bootstrap the registry is hand-maintained" (`PLAN.md`:2036); preconditions enforced as a reusable check before any API call — all four fields nonempty, `venue_id` unique, `place_id` unique, registry `venue_id` set exactly equal to `venues_meta.json`'s key set; an hours parser under `scraper/` implementing the full settled contract (`CLAUDE.md`'s hours-ingestion non-negotiables) — `always_open` periods omit `close` entirely (`{"open": 0, "always_open": true}`); multi-day decomposition at `day_gap = (close.day − open.day) mod 7 >= 2` with `day_gap` 0 and 1 untouched, decomposed entries **appended, never overwriting**; `currentOpeningHours` decomposed by **calendar-date difference**, never weekday or `mod 7`; the seven-day current-hours window (`current_hours_valid_from`/`current_hours_valid_through`) **computed from the request's local date** (start through start+6 days) with every period endpoint date validated against that window, failing loudly on disagreement; truncation covering **open-only** (window's first date, 00:00), **close-only** (final date's boundary), **both ends**, and **invalid interior/inconsistent** truncation failing validation, with `continues_beyond_window: true` propagated through **every** entry of a decomposed truncated chain; the seven-date materialised `current_hours_by_date` map (every window date `known` or `closed`, span-in coverage marked `known` not `closed`, a missing in-window entry treated as malformed); the special-closure (`specialDays`) fixture built **synthetically and labelled as such**, since no saved Phase 0 payload contains one; `fetch_hours(source) -> Hours` accepting a **full source record**, never a bare Place ID, calling Place Details keyed by `source.place_id`, passing the raw response through the hours parser above (no duplicated parsing logic), and **propagating** any transport or parse failure to the caller **without writing any file** — fetchers return data, they never write files
- **Required verification**: `.venv/bin/pytest tests/python/` covering registry preconditions (missing/empty field, duplicate `venue_id`, duplicate `place_id`, both directions of ID-set disagreement, each failing before any API call); bootstrap re-run refusal (invoked with `data/venue_sources.json` already present fails loudly and leaves that file byte-unchanged); hours parsing (cross-midnight, 24-hour with no `close`, split periods, missing fields, multi-day at `day_gap` 2 and 6, `day_gap == 0`/`close == open` rejected, `always_open` serialising with no `close` key, decomposed entries proven appended not overwritten, `currentOpeningHours` calendar-date decomposition, the computed-and-validated window, open-only/close-only/both-end truncation plus interior-truncation failure, `continues_beyond_window` propagation through a decomposed chain, the materialised seven-date map, the synthetic labelled special-closure fixture); `fetch_hours` against fixture Place Details responses only, **no live network call** — the success path (source record → parser output) and a failure path (transport error or malformed response propagates, no file written); `node --test tests/js/*.test.js` as a no-regression check (142 passing); `git status`/`git diff` confined to `data/venue_sources.json`, `scraper/`, `build/bootstrap_venue_sources.py`, `tests/python/`, `HANDOFF.md`, `reviews/LEDGER.md`
- **Claude gate result**: `GATE_PASS` (invocation 2 of 2; see `reviews/IMP-006-gate.md`)
- **Gate evidence**: `reviews/IMP-006-gate.md` (2 invocations)
- **Review record**: `reviews/IMP-006.md` — round 1 `CHANGES_REQUESTED` (`IMP-006-R1-F01`, accepted and fixed), round 2 `APPROVE` (finding `resolved`, no new findings)
- **Independent review**: `codex_terra`, medium — 2 rounds, complete
- **User decision**: 2026-09-03 — approved, and commit authorized; recorded in `DECISIONS.md`
- **Next action**: None — terminal. A new task requires a new assignment ID.
