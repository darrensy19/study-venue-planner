# Handoff

Current project state, one active assignment, and one next action — nothing else. No rationale;
see `DECISIONS.md` for that. The block below is capped at 25 lines by `WORKFLOW.md`'s boundary
rules; when a field would exceed it, point to `PLAN.md` or the review record instead of expanding.

## Current assignment

- **ID**: `IMP-019`
- **Work type**: implementation
- **State**: `completed`
- **Primary route**: `claude_sonnet` — Sonnet, effort high — single-module implementation of an already-approved contract (`ARCH-004`), no new architecture decision
- **Verification route**: `codex_terra`, effort medium
- **Route triggers**: two hard triggers fire — (1) public interface / compatibility contract change (`ranking.js`'s returned shape: `resultState` replaces `refusals`, plus a new exported `CONTROL_CONTRACT`); (2) correctness depends on fail-closed/negative paths (`resolveHours` totality over two malformed shapes, Plan A eligibility floor, control validation short-circuit)
- **Baseline commit**: `ff00831`
- **Artifact under review**: `web/ranking.js` (control contract + validation, tolerance ownership, Plan A eligibility floor, `resultState`, failed-source diagnosis, naming on `removed`/`travelUnknown`), `web/app.js` (removed local `FEASIBILITY_TOLERANCE_MINUTES`; `render()`'s refusal branch reads `resultState` instead of the removed `refusals` field — a minimal crash-avoidance stopgap only, not slice 2's wording/hierarchy work), `tests/js/ranking.test.js` (~20 new tests, 3 existing fixtures rewritten to use in-range durations), `tests/js/app.test.js` (1 new source-text test) — the diff to these four files
- **Objective**: Slice 1a (`PLAN.md`'s "Phase 1 review-response slice order") — the result-state machine, Plan A eligibility, control-contract export + validation, tolerance ownership, and failed-source diagnosis in `web/ranking.js`. Public-shape migration step 1 of 2 (`PLAN.md`'s "candidate freshness"/`hoursStatus`/stale-data invariant is step 2, slice 1b — explicitly out of scope here)
- **Scope exclusions**: no presentation-shape fields (achievable end, binding limit, latest-leave state, candidate freshness, `bestAlternative` — slice 1b); no `app.js` wording/vocabulary/hierarchy redesign (slice 2) beyond the minimal `resultState` stopgap named above; no `resolved_name` venue-source-registry fallback wiring (a Python-side `build/generate.py` merge change, not in `web/ranking.js`'s scope — flagged as a follow-up, not implemented)
- **Acceptance criteria**: `rankVenues()` validates the whole control contract before computing population state, returning `invalid_request` with per-control violations on failure; `CONTROL_CONTRACT` is deeply frozen and is the sole source read by validation; Plan A requires `overall_tier ∈ {robust,tight}` AND `seat_confidence ≥ mixed`, filtered not re-sorted; exactly one `resultState` of six is always returned, `nothing_evaluable` carrying all four diagnostics simultaneously; `toleranceMinutes` defaults to 15 with no declaration left in `app.js`; `resolveHours` is total over both malformed hours shapes; every `removed`/`travelUnknown` entry carries a `name`; no regressions in either test suite
- **Required verification**: `.venv/bin/pytest tests/python/ -q` and `node --test tests/js/*.test.js` (no regressions — 190 + 203 passing); `make generate` against the real repo, network blocked, twice, byte-identical; non-vacuity spot-checks (Plan A floor, control-validation short-circuit, `resolveHours` totality) independently reverted and confirmed to fail without the fix, then restored
- **Claude gate result**: `GATE_PASS` (invocation 1) — `reviews/IMP-019-gate.md` (round 1, `draft`)
- **Independent review**: round 1 `codex_terra` returned `CHANGES_REQUESTED` — 2 findings, both `accepted`/`confirmed` and corrected (F01: `app.js` now builds origin/mode options and duration bounds from `CONTROL_CONTRACT`; F02: `resolveHours()`'s missing-`regular_hours` check now runs before the current-hours window check). Round 2 `codex_terra` independently re-verified both corrections (fresh probes, both suites, `make generate` ×2) and recommends `APPROVE`, both findings `reviewer_resolved`
- **Review record**: `reviews/IMP-019.md` — rounds 1-2 + primary response, all present and in the correct order. Round 2 was initially appended before the primary response due to a benign read/write race across sessions; corrected via a verified, content-preserving reorder (confirmed byte-for-byte via a character-multiset diff) — no content was altered, only repositioned. `finding_state.py` now resolves both findings `reviewer_resolved`
- **User decision**: approved — close and commit (2026-09-06)
- **Next action**: none — assignment closed. Next in `PLAN.md`'s slice order: Slice 1b (presentation shape — achievable end, binding limit, latest-leave state, candidate freshness/`hoursStatus`/`histogramStatus`, candidate/Plan B naming, `metricsBasis`, Plan B transfer, `bestAlternative` — in `web/ranking.js`). Open a new ID when ready to proceed
