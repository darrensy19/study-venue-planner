# Gate record: IMP-002

## Gate invocation 1 — 2026-08-31

- **Gate route**: `claude_sonnet`, effort high
- **Brief source**: generated from `HANDOFF.md` via `.cross-agent-workflow/gate_brief.py HANDOFF.md` (rerun myself; output matches the brief given verbatim)

### Checks run

- **Diff since baseline, scope and additivity.** `git log --oneline 0dfd855 -1` confirms `0dfd855` = "Sync cross-agent-workflow protocol to updated skill templates" and is `HEAD`. `git status` shows the changes as uncommitted working-tree modifications (expected, per the brief) to `web/ranking.js`, `tests/js/ranking.test.js`, plus incidental `HANDOFF.md`/`reviews/LEDGER.md` bookkeeping. `git diff 0dfd855 --stat -- web/ranking.js tests/js/` shows exactly two files: `tests/js/ranking.test.js` (+575) and `web/ranking.js` (+435), **1010 insertions, 0 deletions** — confirmed purely additive by reading the full diff, not just the stat line: every hunk is a pure addition (`@@ -X,N +X,M @@` with the old file's line count unchanged), and no line of the pre-existing hours/feasibility code (`resolve_hours`, `findActivePeriod`, `effectiveClose`, `resolveBound`, `resolveFeasibility`, the `AT_LEAST_0`/accessor block) was touched. `ls fetch_hours.py build/refresh.py app.js index.template.html` and a `find . -iname` sweep confirm none of these exist anywhere in the repo — same as before this assignment, per the brief's expectation.
- **`node --check` and `node --test`, rerun independently.** `node --version` → `v18.15.0`. `node --check web/ranking.js` → exits clean, no output. `node --test tests/js/ranking.test.js` from repo root: output ends `# tests 93`, `# pass 93`, `# fail 0`, `# cancelled 0`, `# skipped 0`, `# todo 0`. All 93 subtests printed `ok`, including all 47 return-transport-titled tests (`resolveReturnService`, `admissibleReturnModes`, `resolveReturnBound`, `resolveReturnFeasibility`, `overallTier`, `combineBindingLimit`, `resolveOverallFeasibility`, `validateReturnTransport`, plus the calendar/band-parsing helpers). **Passing, confirmed first-hand.**
- **`resolveReturnService` (L154-191).** Precedence is holiday branch first (L165-171: `holiday_return_policy === "substitute_sun"` → `by_weekday.sun ?? default`; anything else, including the unset default, returns `missing` — never falls through to `by_weekday`/`default`), then `by_weekday[targetWeekday]` before `default` (L172-177), matching the pseudocode in `plan.md:879-916` exactly. Weekday is `weekdayAbbrev(serviceDate)` (L172) — the parameter is literally named `serviceDate`, never the session-end date — confirmed by the test at L567-584 (a 03:30 Saturday's service date is Friday, and the resolved band is Friday's `by_weekday` override, not Saturday's). `normaliseBand` is called only at L179, after the `!entry || !entry.last_departure_band` guard at L178 — an absent entry never reaches band normalisation, confirmed by the test at L553-557 (`{default:{}}` → `missing`, not `malformed`).
- **`validateReturnTransport` (L474-497).** Walks every destination (`Object.keys(returnTransport)`, L480), every mode (L481), and `[default, ...by_weekday entries]` (L484) — matching `plan.md`'s traversal exactly. All three `MISSING` shapes (no block L479 `if (returnTransport)`; block present but this destination/mode absent — the mode loop simply doesn't iterate an absent mode; a selected entry with no `last_departure_band`, L485 `if (!entry || !entry.last_departure_band) continue`) stamp `ok`, confirmed by the tests at L960-969. Classifies per-venue via a `failures` array per venue id (L476-495) and never aborts or throws; the per-venue-isolation test at L989-996 confirms one malformed venue doesn't affect another.
- **`admissibleReturnModes` (L193-211).** A total function of only its parameters — `venue`, `bicycleWithYou`, `raining`, `sessionEndAbs`, `serviceDate`, `cycleLatestMinutes` (defaulted from the module constant, but overridable, L199) — with no closure over mutable outer state. Reads `venue.access?.origin_a` only (L201), never any other origin. Bicycle/rain/cutoff gating on `cycle` specifically: L202 removes `cycle` unless `bicycleWithYou`; L203 removes it if `raining`; L204-207 removes it if `cycleLatestMinutes !== null` and `sessionEndAbs` is past `absMinutes(serviceDate, cycleLatestMinutes)`. The cutoff-comparison test at L657-667 and the mid/upper-disagreement test at L669-678 both confirm the comparison goes through `absMinutes(serviceDate, ...)` rather than a raw offset, and that the two bounds can independently disagree — proving no implicit shared `session_end`.
- **`overallTier` (L324-333).** `RETURN_TIER_RANK = {unverified:0, shorter:1, tight:2, robust:3}` (L324) and `overallTier` returns whichever of the two inputs has the lower rank (L332) — an ordinal composition over exactly `robust > tight > shorter > unverified`, confirmed order-independent by the test at L846-853 (`overallTier("unverified","robust") === "unverified"`, matching `overallTier("robust","unverified")` by symmetry of the `<=` comparison).
- **Binding-limit composition — the six-row table (`combineBindingLimit`, L349-368; read against `plan.md:1350-1370`'s literal table).** Verified row-for-row:
  - Row "finite C + finite last departure": L360-366 computes `closeAdjustedAbs = C - buffer` then takes whichever of `closeAdjustedAbs`/`lastDepartureAbs` is earlier — matches `min(C - closing_buffer, last_departure)`. Test L861-869 confirms both directions (`closeWins`/`departureWins`).
  - Row "finite C + AT_LEAST(0)": L361-363 (`!isLastDeparture` → `venue_close`, `closeAdjustedAbs`). Test L871-875.
  - Row "COVERED + finite last departure": L354-356 (`COVERED && isLastDeparture` → `{bindingConstraint:"last_departure", bindingLimitAbs: returnOutcome.lastDepartureAbs}`) — **the subtle row**, confirmed to yield a genuine finite value, not `UNDETERMINED`, by the test at L877-881 and independently traced by hand for the end-to-end test at L919-937 (`resolveOverallFeasibility`: a COVERED always-open venue with a `23:00-23:05` last-departure band; worked the arithmetic through `serviceDateFromAbs`/`lastDepartureEdge`/`combineBindingLimit` by hand and confirmed `latestLeaveAt` lands on `absMinutes("2026-08-31", 1262)` exactly as asserted, and is not `"UNDETERMINED"`).
  - Row "COVERED + AT_LEAST(0)": L354-357 (`COVERED && !isLastDeparture` → `{bindingConstraint:"none", bindingLimitAbs:undefined}`), and `boundBindingMetrics` (L373-388) maps that to `latestLeaveAt: "UNDETERMINED"` (L384) — the one row producing `UNDETERMINED`. Confirmed by the test at L883-889 and independently hand-traced for the end-to-end test at L940-957 (a 60-minute core-span session: `latestLeaveAt === "UNDETERMINED"`, `surplusMid.kind === "at_least_0"`).
  - Row "hours NONE": handled upstream, not inside `combineBindingLimit` at the real call site — `boundBindingMetrics` (L374-375) checks `active.result !== "open"` and returns `undefined` *before* calling `effectiveClose`/`combineBindingLimit` at all, matching `resolveBound`'s pre-existing NONE convention. `combineBindingLimit` itself still handles a literal `"none"` sentinel (L350) for direct unit-testability (test L856-859), even though the real pipeline never reaches it that way.
  - Row "UNKNOWN": never passed to `combineBindingLimit` — `resolveOverallFeasibility` (L402-472) short-circuits at L412-414 (`hoursResult.tier === "invalid" || hoursResult.tier === "hours-unknown"`) before either bound's binding limit is computed, and `resolveFeasibility` (unmodified, L798-...) sets `tier: "hours-unknown"` exactly when either bound's `effectiveClose` is `UNKNOWN` (read at L822-...) — so by the time `boundBindingMetrics` runs, neither bound can be `UNKNOWN`. No test exercises this row directly inside `combineBindingLimit` (it is structurally unreachable there), which is consistent with the design comment at L347.
- **`unverified` vs `UNKNOWN` kept distinct.** `resolveOverallFeasibility` (L412-414) returns the hours-only result unchanged, before the return side is evaluated at all, whenever `hoursResult.tier` is `"hours-unknown"` (or `"invalid"`). The tripwire test at L891-899 sets `venue.access` to a getter that throws if ever read (the exact object `admissibleReturnModes` would read) and confirms `resolveOverallFeasibility` returns `tier: "hours-unknown"` without throwing — proving the return side, specifically the one field `admissibleReturnModes` consumes, is never touched. Separately, `return_tier: "unverified"` is a ranked tier value (L317-322 `resolveReturnFeasibility`), never a removal — confirmed by the "return unverified composes to overall unverified" test (L901-917), which shows the venue still produces hours-only metrics (`metricsBasis: "hours_only"`) rather than disappearing.
- **Route-prerequisite-before-clock ordering.** `resolveReturnBound` (L236-295) checks `modes.size === 0` (L246-248) *before* the schedule-free check (L250-252), the core-span check (L254-256), and the pre-dawn check (L258-260) — confirmed by reading the function top-to-bottom, and independently by the test at L689-694 (`sessionEnd` at 13:00, squarely inside the core span, with an empty `access.origin_a` → `{kind:"unverified", basis:"no_recorded_route"}`, never a core-span pass).
- **Core-span waives the timetable, not the route.** L254-256: the core-span branch returns `{kind:"pass", basis:"core_span", margin: AT_LEAST_0}` with no reference to `return_transport` anywhere in that branch or the ones before it. Confirmed via the tripwire fixture `tripwireReturnTransport()` (test file L74-83, a getter on `origin_a` that throws if read) attached to `venue.return_transport` (not `venue.access` — this tripwires the timetable file specifically) in the tests at L696-708 (inside-span pass, zero throws) and L710-716 (boundary at 21:30 exactly, inclusive). Grepping `web/ranking.js` for `return_transport` confirms the only reads are at L154 (`resolveReturnService`, step 5) and L479+ (`validateReturnTransport`, a separate whole-file stage) — never inside the schedule-free, core-span, or pre-dawn branches of `resolveReturnBound`.
- **Pre-dawn gap unmodelled.** L258-260: `unverified`, basis `pre_dawn_gap`, terminal (returns immediately, no fallthrough to the timetable resolver at L262+) on a schedule-bound-only mode set — confirmed reachable only because the schedule-free check at L250-252 already returned for any set containing `walk`/`cycle`, so by construction the pre-dawn branch only ever sees a schedule-bound-only set. Test L718-724 uses the `tripwireReturnTransport()` fixture again to confirm zero `return_transport` reads at 05:00 with only `transit` admissible. Test L726-732 confirms an admissible `walk` at the same 05:00 clock time overrides the gap entirely, resolving `{kind:"pass", basis:"schedule_free", margin: AT_LEAST_0}` — matching `plan.md:1276`'s explicit table of the two cases.
- **Tag/accessor discipline.** `grep -n "surplus\."` and `grep -n "\.kind\b"` across `web/ranking.js` show every `.kind`/`.minutes` read of the `AT_LEAST_0`/finite-surplus tagged type occurs only inside the four pre-existing accessor bodies (`passesFeasibility` L696-702, `finiteShortfall` L704-712, `surplusSortKey` L714-716, `surplusDisplay` L730-735) — none of the new return-transport code destructures a surplus value directly. `resolveReturnBound`'s own `margin` field is produced via the pre-existing, unmodified `AT_LEAST_0` constant (imported/reused, not redefined — confirmed the diff adds no new `AT_LEAST_0 =` or accessor-function declaration) and `finiteSurplus(...)` (L292, reused unchanged from the hours side). `resolveReturnFeasibility` (L297-322) computes `robust`/`tight` via `passesFeasibility(upper.margin)` and `passesFeasibility(mid.margin) || finiteShortfall(mid.margin) <= toleranceMinutes` (L312-313) — the identical accessor-mediated pattern as the pre-existing `resolveFeasibility`, never a raw numeric comparison on `.minutes`.
- **Scope discipline.** `grep -n -i "seat_confidence\|backup_strength\|plan_a\|plan_b\|planA\|planB"` across `web/ranking.js` and `tests/js/ranking.test.js` returns zero matches — none of the deferred/excluded machinery appears anywhere in the diff.
- **CLAUDE.md "Getting home" bullet-list spot-check** (read the full section; spot-checked against the code beyond what's cited above): "A recorded, currently-admissible route home is an unconditional prerequisite... An empty admissible set is unverified at every hour of the day" — confirmed structurally: `resolveReturnBound`'s only path to `modes.size === 0` is the L246-248 check, which runs unconditionally before every other branch, so no clock value can bypass it. "`resolve_return_service`... `edge()` takes `lo` for the upper bound and the floored midpoint for the mid bound" — `lastDepartureEdge` (L136-138): `boundKind === "upper" ? band.lo : Math.floor((band.lo + band.hi) / 2)`, matching exactly, and contrasted against a travel band's own upper-edge convention by the dedicated test at L129-135 (`lastDepartureEdge` vs `travelBandUpperEdge` explicitly asserted `notEqual`). "`MAX` (not `MIN`) over admissible modes" — `resolveReturnBound` L290-292 uses `Math.max(...present.map(...))`, confirmed by the two-schedule-bound-mode test at L744-759 (the later, `shuttle`, departure wins over the earlier `transit` one).

### Could not verify

- Whether the `RETURN_CORE_FROM_MINUTES` / `RETURN_CORE_UNTIL_MINUTES` / `RETURN_SERVICE_DAY_START_MINUTES` constants (L20-24) match the values `ARCH-001`'s review record actually approved — I read `plan.md:1115-1116` (07:00/21:30) and cross-checked the constants match that text, but I did not independently re-verify the LTA source claims underlying that design decision; that verification is `ARCH-001`'s, not this gate's, to have performed.
- The plan.md testing-checklist item "the return leg reads `access[origin_a]`, not the outbound origin — a trip from `origin_b`..." (`plan.md`'s Testing section, "the return leg reads access[origin_a]") is not exercised by a test that actually varies an outbound-origin parameter — `admissibleReturnModes`/`resolveReturnBound` take no outbound-origin parameter at all (they hardcode `venue.access?.origin_a`), so the property holds structurally, but no test in the diff constructs an `origin_b`-only fixture to demonstrate it. I traced this by reading the function signatures rather than by finding a matching test; I consider the property true by construction, not a gap in the implementation, but it is a gap against the plan's own testing checklist as literally written.
- Whether `resolveOverallFeasibility`'s short-circuit on hours `tier === "invalid"` (validation_failure — disagreeing periods) is covered by an equivalent tripwire test the way the `hours-unknown` short-circuit is (test L891-899 only exercises `hours-unknown`, not `invalid`). I confirmed by reading the code (L412-414 checks both) that the short-circuit itself covers both cases identically, but did not find a dedicated tripwire test for the `invalid` branch specifically.
- Runtime/model verification of which model actually authored the primary's commit-in-progress — not observable from this environment; not claimed either way.
- Whether `HANDOFF.md`'s and `reviews/LEDGER.md`'s prose (beyond the baseline commit and artifact identification, independently confirmed via `git log`/`git diff`) accurately narrates the implementation — not re-derived beyond what the brief itself covers.

### Not asked to check

- `seat_confidence`, `backup_strength` grading, and Plan A/B recalculation — explicitly deferred to a later assignment per the brief; confirmed absent (see Scope discipline above) but not otherwise evaluated.
- `fetch_hours.py`, `fetch_busyness.py`, `app.js`, `index.template.html`, and `build/refresh.py`'s fetch/coarsen pipeline — explicitly excluded; confirmed none of these files exist in the repo, but their (non-existent) content was not evaluated.
- `validate_return_transport`'s call site — i.e., the `STEP 0` precondition in `ranking.js` that would read a venue's `return_transport_status` stamp before evaluating either bound. The brief is explicit that only `validate_return_transport` itself is in scope, not its call site, and no such STEP 0 check exists anywhere in the diff — consistent with that exclusion, not a defect.
- `tests/python/` and any Python-side validation of `return_transport`/`holiday_return_policy` data shape in `data/venues_meta.json` — out of this assignment's `tests/js/`-scoped required verification.
- The free-text `basis` field privacy rule (no exact times, no line/direction names) — mentioned in `CLAUDE.md` but concerns hand-maintained `venues_meta.json` content, not `ranking.js` or its tests; not evaluated here.

### Status

`GATE_PASS`

---
