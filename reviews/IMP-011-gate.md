# Gate record: IMP-011

## Gate invocation 1 — 2026-09-03

- **Gate route**: `claude_sonnet`, effort high
- **Brief source**: generated from HANDOFF.md acceptance criteria + required verification

### Checks run

- **Read `PLAN.md`'s "The coarsening stage"** (lines 2052–2229) in full, including the ~20-item "Negative-path fixture obligations" list and the Group 1 / Group 2 discussion.
- **Read `build/coarsen.py`** (350 lines) and **`tests/python/test_coarsen.py`** (753 lines, 50 test functions) in full.
- **Reran `.venv/bin/pytest tests/python/ -q`** myself: `128 passed in 1.07s` (and again at the end, after mutation-restore, `128 passed in 1.18s`). Matches the claimed 128 passed / 0 failed exactly. `grep -c "def test_" tests/python/test_coarsen.py` = 50, consistent with the claimed 78 pre-existing + 50 new = 128.
- **Reran `node --test tests/js/*.test.js`** myself: `# tests 178 / # pass 178 / # fail 0`. Matches the claimed 178 passed, and `git status`/`git diff` confirm no `web/` file is touched.
- **`git status --short`**: `M HANDOFF.md`, `?? build/coarsen.py`, `?? tests/python/test_coarsen.py`. **`git diff --stat HEAD`**: `HANDOFF.md | 4 ++--`. `reviews/LEDGER.md` and `reviews/IMP-011-gate.md` are untouched/absent as expected at this stage (LEDGER updates at close; this gate file is this report). Scope is confined exactly to the claimed files.
- **Mutation-tested non-vacuity of the primary's own claim**: disabled the prefix-comparison raise in `coarsen()` (replaced the loop body with `pass`), reran `tests/python/test_coarsen.py`: **exactly 7 failed, 43 passed** — the same 7 named in the primary's claim (`test_unmirrored_raw_side_change_across_hour_boundary_fails`, `..._weekday_boundary_fails`, `..._venue_id_edit_fails`, `..._outcome_edit_fails`, `test_unmirrored_committed_side_edit_fails`, `test_reorder_of_two_prefix_rows_with_differing_projections_fails`, `test_deletion_inside_run_reaching_only_the_last_committed_position_fails`). Restored the file and reran to confirm byte-identical (`sha256` matched before/after: `868cf5023cca8cf3600229f14d0b5a78162c126937758716dac55c4af5b44dc6`) and all-green (128/128).
- **Mutation-tested 2 additional checks not covered by the primary's own stated evidence**, each applied, tested, reverted, and hash-verified byte-identical after each revert:
  - Disabled the "raw-log candidates > 1" raise in `find_raw_log` → exactly 1 test failed (`test_two_or_more_candidates_fails_before_any_write`). Non-vacuous.
  - Weakened `_histogram_busyness_for` to return `(0, fetched_at)` instead of `None` when an hour has no coverage → exactly 1 test failed (`test_venue_with_no_histogram_entry_for_the_exact_hour_fails_before_any_write`). Non-vacuous.
- **A third mutation surfaced a genuine vacuity, not claimed by the primary**: disabled the `histogram.get("status") == "failed"` branch in `_histogram_busyness_for` → **0 tests failed** (`50 passed`). Investigation: `test_venue_histogram_status_failed_is_unusable` sets `last_success_at=None` on its "failed" fixture venue, so the *separate* `if not fetched_at: return None` guard masks the removed status check — the test is vacuous with respect to the specific mechanism it names. (This fixture is not one of PLAN.md's ~20 enumerated cases — the primary's own docstring for the neighbouring test already flags that class as "not literally enumerated" — and the code's behaviour is still correct given `CLAUDE.md`'s status semantics, where `failed` implies no last-known-good by construction. Recorded here for completeness, not as a gating defect on its own.)
- **A separate mutation trace surfaced a genuine defect against a PLAN.md-enumerated fixture**: `test_insertion_or_deletion_shifting_a_differing_projection_fails` covers PLAN.md's explicit bullet "an insertion or deletion that shifts a **differing** projection into another position → fail." Tracing the run when the prefix-comparison check was disabled (the mutation above, "7 failed, 43 passed"): this test was **among the 43 that still passed** — i.e. it still raised `CoarsenError` even with the exact mechanism it is meant to prove (prefix-comparison catching the shifted mismatch) turned off. Root cause: the test's raw suffix references venue `"v2"`, but its `venues_json` fixture is `default_venues_json(tmp_path)`, which only populates `"v1"`. With the prefix check disabled, the run falls through to suffix processing and fails there instead — on `_histogram_busyness_for` returning `None` for `"v2"` — a completely unrelated code path from the one the fixture is supposed to exercise. In the real, unmutated code the mismatch genuinely is caught by the prefix comparator first (confirmed by manual trace of the projections: committed row 1 is `("v2", d2, h2, "no_seat")`, raw row 1 (of the compared prefix) is `("v1", d1, h1, "seat")` — a real mismatch at position 1), so the *production* behaviour for this case is correct. But the **test itself does not isolate that mechanism**: it would still go green even if the prefix-comparison's differing-projection detection were broken, because the second, independent "no histogram for a suffix venue" guard papers over it. This is the same masking pattern as the `status == "failed"` case above, but here it lands squarely on one of PLAN.md's ~20 enumerated fixture obligations, which HANDOFF.md's acceptance criteria requires to be implemented with "its own non-vacuous test."
- Verified, from the code, several further acceptance-criteria claims directly (not solely from tests): `find_raw_log` checks `is_file()` for the single-file candidate and `is_dir()` + non-recursive `iterdir()` + `.suffix == ".csv"` for the directory candidate, matching "Candidate selection" exactly. `processed_count = len(committed_rows)` is computed fresh every call from `data/seatlog.csv`; `grep -rn "cursor"` across `build/` and the test file returns nothing — no stored cursor anywhere. The prefix comparison compares only `RawRow.project()` / `CommittedRow.project()`, both `(venue_id, day_of_week, hour, outcome)` — the 4-column projection, never `histogram_busyness`/`histogram_fetched_at`. Suffix stamping reads `histogram.get("last_success_at")` specifically (`grep` confirms `last_attempt_at` does not appear anywhere in `coarsen.py`). `_write_committed_atomic` builds the tmp path as `committed_path.parent / f".{committed_path.name}.tmp-{os.getpid()}"` (same directory), validates it via `parse_committed_rows(tmp_path)` before `os.replace(tmp_path, committed_path)` — matching "Atomic replacement" exactly.
- Cross-checked all ~20 of PLAN.md's enumerated fixture bullets against test names/bodies. All are represented by name and, on reading, all but the one flagged above genuinely exercise the described mechanism (confirmed for several via mutation, as above; for the rest via direct code trace). Two minor secondary notes, neither gating on its own:
  - The paired bullet "the committed-side counterparts of those two — a reorder **or substitution** of committed rows sharing a projection but carrying different stamps" is implemented with one test covering only the reorder variant (`test_committed_side_reorder_of_equal_projection_but_different_stamps_relocates_stamps`); no distinct committed-side substitution case exists. The comparator mechanism is identical either way (position-by-position equality), so this is a coverage-breadth note, not a vacuity finding.
  - `test_unmirrored_committed_side_edit_fails` contains two `write_committed`/`write_raw` calls that are immediately overwritten by two more before the assertion — dead code left over from constructing the fixture, with a comment acknowledging the false start. Harmless (the test does exercise the intended case after the overwrite) but sloppy.
- PLAN.md's fixture bullet "the failure occurs **before either fetcher runs** — asserted, not assumed" has no test in this file that asserts it via a fetcher call/mock, because `build/coarsen.py` contains no fetcher invocation of any kind at this stage (fetcher wiring is `build/refresh.py` step 7, explicitly out of scope per the brief and not yet built). The property currently holds trivially/by construction rather than by an assertion that would catch a future regression once refresh.py wires the fetchers in.

### Could not verify

- Whether the "committed-side counterparts... reorder or substitution" gap and the `test_unmirrored_committed_side_edit_fails` dead code were already known to the primary — I did not ask the primary; these are independent observations, not confirmed omissions.
- Real-world behaviour against actual iCloud-staged raw seat-log data — out of scope by design ("no live-network tests," fixture-only), not attempted.

### Not asked to check

- `build/refresh.py` orchestration/wiring, the fetchers, the frontend/generator, live refresh, and `web/ranking.js` / return-transport primitives — all explicitly excluded by the brief's scope exclusions, and I did not review them.
- Whether `venue_sources.json`'s actual current contents match `KNOWN_VENUE_IDS` used in tests, or any other real-data consistency check — `coarsen()` takes `known_venue_ids` as an injected parameter (wiring it from the real registry is `refresh.py`'s job, out of scope here).
- General code style/naming beyond what bears on the acceptance criteria.

### Status

GATE_FAIL

---

## Gate invocation 2 — 2026-09-03

- **Gate route**: `claude_sonnet`, effort high
- **Brief source**: generated from HANDOFF.md acceptance criteria + required verification

### Checks run

- **Re-read `PLAN.md`'s "The coarsening stage"** (lines 2052–2229) fresh against the current file — unchanged from invocation 1's reading; no new obligations or wording changes since.
- **Read `build/coarsen.py`** (349 lines, one line shorter than invocation 1's 350 — see below) and **`tests/python/test_coarsen.py`** (774 lines, 50 test functions via `grep -c "def test_"`) fresh, not diffed against memory.
- **Reran `.venv/bin/pytest tests/python/ -q`** myself: `128 passed in 1.07s`. Matches the claimed 128 passed / 0 failed exactly (78 pre-existing + 50 new, consistent with the 50 `def test_` count).
- **Reran `node --test tests/js/*.test.js`** myself: `# tests 178 / # pass 178 / # fail 0`. Matches the claimed 178 passed; `git diff --stat` confirms no `web/` file touched.
- **`git status --short`**: `M HANDOFF.md`, `?? build/coarsen.py`, `?? reviews/IMP-011-gate.md`, `?? tests/python/test_coarsen.py`. **`git diff --stat HEAD`**: `HANDOFF.md | 6 ++-3`. Scope confined exactly to the claimed files — `reviews/IMP-011-gate.md` is this report (untracked, as expected pre-close) and `reviews/LEDGER.md` remains untouched. Read the `HANDOFF.md` diff in full: it updates the required-verification line with the actual 128/178 counts, records the invocation-1 `GATE_FAIL` and its correction, and updates "Next action" — all accurate against what actually happened, no unearned claims.
- **`shasum -a 256 build/coarsen.py`** before any mutation: `868cf5023cca8cf3600229f14d0b5a78162c126937758716dac55c4af5b44dc6` — **identical** to invocation 1's own recorded post-restore hash. Confirms, independently of the primary's narrative, that `build/coarsen.py` was not touched between invocations; every claimed correction lives entirely in `tests/python/test_coarsen.py`, consistent with the diff scope above.
- **Re-verification 1 (gating finding)** — `test_insertion_or_deletion_shifting_a_differing_projection_fails`: read the fixture at lines 546–571; confirmed `venues_json` is now built via `write_venues_json` with explicit entries for **both** `"v1"` and `"v2"` (`d1`/`h1`/busyness 10 and `d2`/`h2`/busyness 20 respectively), replacing invocation 1's `default_venues_json(tmp_path)` which supplied `"v1"` only. Disabled the prefix-comparison raise in `coarsen()` (`build/coarsen.py:306-307`, replaced the `if`/`raise` body with `pass`) and reran `tests/python/test_coarsen.py`: **8 failed, 42 passed** — the same 7 named in invocation 1's report **plus** `test_insertion_or_deletion_shifting_a_differing_projection_fails` as the new 8th failure. This is exactly the expected delta: before the fix this test was among the 43 that still (wrongly) passed when the mechanism was disabled; now it correctly fails (`DID NOT RAISE`), proving it isolates the prefix-comparison mechanism rather than the unrelated missing-histogram guard. Restored the file and reran `shasum -a 256`: byte-identical to the pre-mutation hash above, and `pytest tests/python/ -q` reran green at `128 passed`.
- **Re-verification 2 (secondary note)** — `test_venue_histogram_status_failed_is_unusable`: read the fixture at lines 629–646; confirmed `last_success_at` is now `"2026-08-20T10:00:00+08:00"` (a real timestamp) alongside `"status": "failed"`, replacing invocation 1's implied `None`/absent value. Disabled just the `status == "failed"` branch in `_histogram_busyness_for` (`build/coarsen.py:241`, changed `if not histogram or histogram.get("status") == "failed":` to `if not histogram:`) and reran the full suite: **exactly 1 failed** (`test_venue_histogram_status_failed_is_unusable`, `DID NOT RAISE`), **49 passed** — non-vacuous, isolating the intended mechanism, where before this same mutation produced 0 failures (50 passed, per invocation 1). Restored and reran `shasum -a 256`: byte-identical; full suite green at `128 passed`.
- **Re-verification 3 (secondary note)** — `test_unmirrored_committed_side_edit_fails`: read the current body (lines 245–252) — exactly one `write_committed` call and one `write_raw` call (`grep -n "write_committed\|write_raw"` around those lines confirms), no leftover overwritten calls. The dead-code false start invocation 1 flagged is gone; the test is clean and still exercises the intended mirror case (committed row edited, raw untouched → fail).
- **Re-verification 4 (coverage-breadth note)** — read the docstring added to `test_committed_side_reorder_of_equal_projection_but_different_stamps_relocates_stamps` (lines 409–417) in full. Its argument: `data/seatlog.csv` retains no `occurred_at`, so a committed row's on-disk identity *is* its full 6-field tuple; among rows sharing one projection, "reorder two existing rows" and "substitute one row for a different equal-projection row" both simply select some permutation of the existing stamp-tuples across the shared positions, and any such permutation is reachable and expressible as a reorder — there is no committed-side analogue of the raw side's `occurred_at`-carrying distinction between "the same physical row moved" and "a different row with a matching projection took its place." Traced this against the project's own established epistemics: the same reasoning already underlies `test_coordinated_structural_deletion_passes_with_one_fewer_row`'s own docstring ("a 'before' state ... is never seen by `coarsen()` at all — it only ever reads the already-edited files on disk"), and against the raw-side case (`PLAN.md` line ~2116), where reorder and substitution *do* get two separate tests specifically because the raw file retains `occurred_at`, giving substitution a distinguishing hook that the committed side structurally lacks. Judged the reasoning **sound** — a distinct committed-side substitution fixture is not achievable as anything other than a relabelling of the existing reorder fixture, so no additional test was warranted.
- **Final independent sweep, beyond the four claimed fixes**: grepped `build/coarsen.py` and `tests/python/test_coarsen.py` for leftover debug/placeholder artifacts (`TODO`, `FIXME`, `XXX`, stray `print(`, `pdb`, `breakpoint()`, bare `pass` bodies) — none found. Confirmed `reviews/IMP-011-gate.md` was 42 lines before this section (invocation 1's content only, untouched, append-only respected). Re-confirmed via the `HANDOFF.md` diff that no claim there overstates what was actually verified (counts match the reruns above, the gate-result line accurately narrates invocation 1 and the correction). Since `build/coarsen.py` is byte-identical to invocation 1's own post-mutation restore hash, invocation 1's full sweep of the remaining ~20 `PLAN.md`-enumerated fixture bullets and its "not literally enumerated but required" note on the no-histogram-entry test still hold unchanged — production logic did not move, only the four bounded test-fixture edits above did, and each has now been independently re-verified rather than taken on the primary's word.

### Could not verify

- Real-world behaviour against actual iCloud-staged raw seat-log data — out of scope by design, not attempted (unchanged from invocation 1).

### Not asked to check

- `build/refresh.py` orchestration/wiring, the fetchers, the frontend/generator, live refresh, and `web/ranking.js` / return-transport primitives — all explicitly excluded by the brief's scope exclusions.
- General code style/naming beyond what bears on the acceptance criteria.
- Whether the primary's own claim of "re-verified after each gate-round fix" (per HANDOFF.md's required-verification line) was literally performed round-by-round on the primary's side — this gate only confirms the end state independently, not the primary's process.

### Status

GATE_PASS

---
