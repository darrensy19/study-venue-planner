# Gate record: ARCH-002

## Gate invocation 1 — 2026-09-01

- **Gate route**: `claude_sonnet`, effort high
- **Brief source**: generated from HANDOFF.md acceptance criteria + required verification

### Checks run

- **`git status` / `git diff` scope**: `git status --porcelain` shows exactly four modified paths — `CLAUDE.md`, `HANDOFF.md`, `PLAN.md`, `reviews/LEDGER.md`. No other path touched; no production file (`build/refresh.py`, `scraper/*`, `web/app.js`, `web/index.template.html`, `web/style.css`, `data/venue_sources.json`, any test file) created or modified. `scraper/places.py` and `build/phase0_*.py` exist but predate this diff and are Phase 0 artifacts, untouched.
- **No surviving `fetch_busyness(place_id)`**: repo-wide grep (`.py`, `.js`, `.md`) finds the string only inside prose that explicitly narrates it as the *earlier, corrected* signature — PLAN.md:1957 ("the earlier `fetch_busyness(place_id)` signature encoded an identity the busyness source cannot use. Corrected here.") and CLAUDE.md:95 ("the earlier `fetch_busyness(place_id)` signature was wrong..."). No function definition or call site uses it; `scraper/places.py` defines no `fetch_busyness`/`fetch_hours` at all (Phase 0 code, pre-dates the interface). The live signature is stated once in each file as `fetch_hours(source)` / `fetch_busyness(source)`.
- **No "venues/meta merge" in a `ranking.js` or `tests/js` context**: PLAN.md's pure-functions list (line 1738) now reads "backup strength, holiday policy, area grouping, the log→venue join. **The venues/meta merge is not among them** — it happens in Python at generation time." CLAUDE.md's `tests/js` bullet no longer lists the merge; it was moved to the `tests/python` bullet under "**Phase 1 orchestration adds**: the venues/meta merge and its ID-mismatch failure in both directions...". PLAN.md's own test-obligations section (line ~2320) also dropped "venues/meta merge" from the `tests/js` list and added "**the venues/meta merge at generation time**" to the `tests/python` list (line 2329). Consistent across both files.
- **No "the list lives in `data/venue_seeds.csv`"**: exact phrase does not occur (repo-wide grep on `*.md`, zero hits outside this gate's own brief quoting it). PLAN.md's "Venue list" section now reads "**The list of venues to *resolve* lives in `data/venue_seeds.csv`**" plus a new paragraph: "**It is not the fetch list.** ... Phase 1's canonical list of venues to fetch is **`data/venue_sources.json`**."
- **`venue_sources.json` schema checked against real data** — ran a Python check against the actual files:
  - `data/phase0/place_ids.csv`: 28 total rows, 28 with `match_status == confident` (matches the brief's "28 `confident` rows").
  - All four required fields (`proposed_venue_id`→`venue_id`, `place_id`, `resolved_name`, `resolved_address`) nonempty on every confident row: 0 empty-field violations.
  - `venue_id` unique across the 28 rows: True (28 values, 28 distinct). `place_id` unique: True (28 values, 28 distinct).
  - `data/venues_meta.json` key set vs. registry's `venue_id` set (from `place_ids.csv`): both sets have 28 members; `registry − meta = {}`, `meta − registry = {}` — **exact agreement**.
- **`node --test tests/js/*.test.js`**: ran directly. Output: `# tests 142`, `# pass 142`, `# fail 0`, `# cancelled 0`, `# skipped 0`. No regression.
- **`.cross-agent-workflow/audit_due.py reviews/AUDIT-LOG.md`**: output `NOT_DUE` — `sampling interval: 1 in 4`, `reason: no selected assignment is awaiting audit`.
- **`reviews/LEDGER.md` read back**: exactly one `ARCH-002` row present (`grep -c` → `1`), reading `| \`ARCH-002\` | 2026-09-01 | architecture/high-level | Phase 1 orchestration/UI-shell architecture — ... |`.
- **Acceptance clause: generation-time Python merge / no client-side merge / ID-mismatch failure** — confirmed in both files. PLAN.md ("Data contracts"): "**They are merged in Python, at generation time, by `id`.**... `ranking.js` receives that flat list and performs **no client-side meta merge**"; "**An ID mismatch between the two files is a generation contract failure**, not a runtime condition. A generated venue with no meta entry, or a meta entry with no generated venue, stops generation." CLAUDE.md line 89 states the identical rule in condensed form, including "the merge's tests belong to `tests/python/`, not `tests/js/`." No contradiction between the two.
- **Acceptance clause: `venue_sources.json` registry preconditions** — PLAN.md new section "The venue-source registry" states all four required nonempty fields, `venue_id`/`place_id` uniqueness, exact ID-set agreement "checked before any API call" and "again **during generation**", and bootstrap "generated from `data/phase0/place_ids.csv`... for every row whose `match_status` is `confident`", with `place_ids.csv` explicitly "read **once**... never read again" after bootstrap. Matches the brief verbatim.
- **Acceptance clause: `fetch_hours`/`fetch_busyness` signatures** — both PLAN.md (line 1955-1957) and CLAUDE.md (line 95) state the source-record signature, the busyness-is-search distinction, and the `resolved_name` + `resolved_address` query with the validated `data`-parameter retry, citing `DECISIONS.md`, 2026-08-29, "Popular Times coverage, take two" (verified that decision entry exists at DECISIONS.md:1264).
- **Acceptance clause: per-source `ok`/`stale`/`failed`** — PLAN.md new paragraph (after the status table): "A **first success** is `ok`... A **failure with no known-good yet** is `failed`, and carries **no data at all**... A **later failure** is `stale`, retaining the known-good and its original `last_success_at`... The two sources resolve independently." Matches "first-success known-good, no fabricated data on `failed`, independent source failure." Closure is stated as coming only from explicit `businessStatus` in the unchanged pre-existing line directly below and reaffirmed in the new registry section ("Only an explicit `businessStatus` may mark a venue closed").
- **Acceptance clause: full coarsening contract** — verified every named element is present in PLAN.md's new "The coarsening stage" section: the 3-column raw schema with an offset-bearing `occurred_at` example (`2026-08-28T14:05:00+08:00`); the two-location candidate selection (`data/seatlog.raw.csv` regular file, or non-recursive `*.csv` in `data/raw/`) with an explicit 0/1/many table; the cursor-free rule ("**there is no cursor**... derived, every run, from the committed output") with re-projection to `(venue_id, day_of_week, hour, outcome)` compared row-for-row; pre-fetch histogram stamping ("`histogram_fetched_at` from **that histogram's own `last_success_at`** — never `last_attempt_at`"); in-memory build with same-directory temp file ("builds the complete replacement `seatlog.csv` **in memory**... writes it to a temp file **in the same directory**... replaces atomically"); and a 13-item negative-path fixture list covering 0/1/2+ candidates, malformed rows (including a naive timestamp), shorter/mutated/reordered prefixes, no-deployed-`venues.json`, ordering-before-fetch, and no-partial-output.
- **Acceptance clause: Node return-validator bridge** — PLAN.md's new "The return-validator bridge" section states argv-only `subprocess.run([...])` with an explicit "**Never `shell=True`, never a constructed shell string**"; JSON-only stdout ("writes **structured JSON and nothing else** on stdout"); per-venue `invalid` continuing generation ("Malformed venue data is a result, not an error... generation continues"); and bridge failure (missing Node, nonzero exit, non-JSON output, missing per-venue status) stopping the refresh "**before the atomic replacement**."
- **Acceptance clause: `refresh.py` sole ownership, eight-step order, `make refresh`** — verified the eight numbered steps at PLAN.md lines 1961-1977 (coarsen → fetch → validate → merge → record statuses → validate return_transport → atomic replace → regenerate) are unchanged from baseline and match CLAUDE.md's prose restatement of the same order verbatim in sequence. New sentence added: "`build/refresh.py` is the sole writer of `data/venues.json` and `web/index.html`. Nothing else writes either, in any phase." `make refresh` text: "runs the whole pipeline and **never commits**... **The target is not added to the `Makefile` until the complete contract below is wired**."
- **Acceptance clause: `holidays.json` contract** — new paragraph: "`refresh.py` **never creates it and never modifies it**... Generation **fails visibly** if it is absent or malformed... **No `last_reviewed` field, no maintenance-status enum, and no UI state** for calendar staleness."
- **Acceptance clause: `ranking.js` entry point / taxonomy / refusals / fallback order** — new section "One entry point, pure, whole-dataset" lists the six ordered responsibilities (control resolution → travel-band parsing/arrivals → STEP 0 removal → evaluation → Plan B before ranking → grouping/refusals/ordering), an explicit ranked/unranked taxonomy table distinguishing explicit `null` vs. missing `access` key, both refusal-state sentences reproduced verbatim from the brief, and "Choosing among several fallbacks" with the seven-key table (`backup_strength → overall_tier → seat_confidence → travel burden → preference → surplus_mid → venue_id`) plus the three explicit constraints on `surplusSortKey()`-only comparison and `preference` never falling through to `venue_id`.
- **Acceptance clause: frontend/generation constraints + test obligations** — "Frontend architecture" section gained items 5 ("`app.js` renders; it decides nothing... no merge — is reimplemented there") and 6 (no `fetch()`/`localStorage`/backend/credentials/external JS/CSS). CLAUDE.md and PLAN.md's test-obligation lists both gained the same five-item Phase 1 addition (merge + ID-mismatch, registry preconditions, coarsening negative paths, bridge behaviour, `holidays.json` failure) in near-identical wording.
- **Acceptance clause: eight-step Phase 1 implementation order, no `IMP-###`** — verified the 8-item list at PLAN.md lines 2194-2201 contains no `IMP-###` token (grep confirms zero occurrences in the new PLAN.md content, and `reviews/LEDGER.md`'s diff adds only the `ARCH-002` row, no `IMP-###` row). The order-carries-no-ID sentence is explicit: "**No assignment IDs are allocated here** — each becomes its own assignment when it is opened." The acceptance-set restatement and the "live success does not prove..." sentence are both present verbatim: "**Live success does not prove the failure paths.**... **Partial-source failure, last-known-good retention, malformed-metadata handling and atomic replacement are not demonstrated by it**."
- **No production code written**: confirmed via `git status` (above) — the diff touches only `CLAUDE.md`, `HANDOFF.md`, `PLAN.md`, `reviews/LEDGER.md`.
- **Internal-contradiction sweep**: cross-checked (a) the two "venues/meta merge" statements in CLAUDE.md and PLAN.md — consistent; (b) the eight-step refresh order in both files — consistent, same sequence and step content; (c) the new coarsening/registry text against the pre-existing `seatlog.csv` schema, `venues.json` generated-file contract, and the Phase 0 "frozen artifacts" decision — consistent (`data/phase0/` explicitly cited as frozen and read-once, `seatlog.csv`'s existing six-column schema matches the new suffix-handling stamping description exactly); (d) no `IMP-###` allocated anywhere in the diff. Found no contradiction.

### Could not verify

- None. Every item in "Required verification" was run first-hand against the live repository state, not summarized from the brief's wording.

### Not asked to check

- Whether the design itself is technically sound, complete, or free of engineering defects (e.g., whether the coarsening prefix-comparison algorithm is actually race-free, whether the registry bootstrap script as described is implementable as specified, whether the seven-key fallback order is the *correct* order for the product). The brief scopes this gate to documentation and schema self-consistency only; substantive design review is explicitly deferred to `codex_sol_high` per `HANDOFF.md`'s verification route.
- Whether the actual author/model that produced this diff was Opus at high effort, as `HANDOFF.md`'s "Primary route" field claims. The runtime does not expose the model that generated a given diff to this subagent, and the brief did not ask for that confirmation.
- `README.md`'s separate Phase 1 staleness, and the flagged-but-unopened outbound-mirror `ARCH` — both are explicit scope exclusions in `HANDOFF.md` and were not examined.
- Whether `pytest`/`tests/python/` passes — there are no Python test files for Phase 1 yet (correctly, since no production code exists), and the brief's required verification names only `node --test`, not pytest, for this assignment.
- Whether `data/venue_sources.json` itself should exist yet — it correctly does not (explicit scope exclusion), so no check was run against a file that isn't supposed to exist.
- Prose quality, formatting consistency, or line-length/style conventions beyond what the acceptance criteria explicitly require.

### Status

`GATE_PASS`

---
