# Gate record: IMP-003

## Gate invocation 1 — 2026-08-31

- **Gate route**: `claude_sonnet`, effort high
- **Brief source**: generated from `HANDOFF.md` via `.cross-agent-workflow/gate_brief.py HANDOFF.md` (rerun myself; output matches the pasted brief byte-for-byte)

### Checks run

- **Baseline/implementation commit identity.** `git log --oneline -3 bf75ce0` confirms `bf75ce0` = "Correct stale baseline_seatability premise in Phase 1 acceptance". `git log --oneline -3 82dd79f` confirms `82dd79f` = "Implement relative_busyness banding and seat_confidence lookup (IMP-003)" and is `HEAD`, with `bf75ce0` as its immediate ancestor's ancestor (`90b8d5b` "Allocate IMP-003" sits between). `git status` shows only `HANDOFF.md` uncommitted (the routine `IMP-003` field update, e.g. baseline/artifact-under-review wording) — no other working-tree changes.
- **Diff scope and additivity.** `git diff bf75ce0 82dd79f --stat` touches exactly four files: `HANDOFF.md` (+28/-15, bookkeeping), `reviews/LEDGER.md` (+1, allocation entry), `tests/js/ranking.test.js` (+192/-0), `web/ranking.js` (+133/-0) — matching the brief's named artifact exactly, nothing else. Confirmed purely additive by reading the diff itself, not just the stat line: `git diff bf75ce0 82dd79f -- web/ranking.js | grep -E "^-[^-]"` and the same for `tests/js/` both return zero lines — the only `-` line in either diff is the `--- a/<file>` header. No pre-existing code (hours resolution, return-transport) is touched.
- **`node --version` / `node --test`, rerun independently.** `node --version` → `v18.15.0`. `node --check web/ranking.js` → clean, no output. `node --test tests/js/*.test.js` from repo root (glob form, not bare-directory): output ends `# tests 108`, `# pass 108`, `# fail 0`, `# cancelled 0`, `# skipped 0`, `# todo 0`. All 13 newly-titled `resolveBusynessBand`/`resolveSeatConfidence` subtests (103–108 and others by name) printed `ok`. **Passing, confirmed first-hand — matches the claimed 108 total, not merely trusted from the commit message.**
- **`resolveBusynessBand` (`web/ranking.js:564-606`) against `PLAN.md` "3. `relative_busyness`" (read directly, lines 205-238).**
  - **Open-hours filtering.** `openHourSet` (L528-541) builds an hour-of-day set from `venue.hours.regular_hours[weekday]`, handling `always_open` (all 24 hours) and an ordinary period via `floor(open/60)` .. `ceil(min(close,1440)/60)`, clipping to the same calendar day for a period crossing midnight. `resolveBusynessBand` filters `rawEntries` to `openHours.has(e.hour)` (L572) before any median/max computation — closed-hour buckets cannot leak in. Verified against the dedicated test (`tests/js/ranking.test.js:1226-1244`): with 8 real Monday-shaped closed-hour zero-fill entries at hours 0-7 and 2 at hours 22-23 present in the raw data, `coverageHours` reads 14 (not 30) and `medianUsed` reads 57.5 (not dragged toward 0) — i.e. the test would fail if the filter leaked closed hours in, and it passes.
  - **Coverage floor.** L574-576: `filtered.length < minHistogramHours` (default `MIN_HISTOGRAM_HOURS = 6`) returns `{band: "unknown", reason: "insufficient_coverage", coverageHours}`. Confirmed by test `tests/js/ranking.test.js:1295-1305` (3 open hours, floor 6 → `unknown`/`insufficient_coverage`/`coverageHours: 3`).
  - **`peak` precedence over `busy` — checked for the flat-curve trap specifically.** The condition at L595 is `delta >= n && arrivalEntry.busyness >= max - p` — `peak` is gated on the `busy` threshold (`delta >= n`) first, and only *then* checked for proximity to the max; it is not an independent "close to max" test. Verified this is not vacuous: the flat-curve test (`tests/js/ranking.test.js:1285-1293`, 8 hours all at busyness 50) asserts `band === "typical"` for every hour, with `delta === 0`. On a flat curve every value trivially equals the max, so `arrivalEntry.busyness >= max - p` (95 >= 45) is true at every hour — if the code used that clause alone (dropping `delta >= n`), every hour would misread `peak`, and this exact test would fail. Because the actual gate requires `delta >= n` (0 >= 15 is false) first, the test passes for the right reason, not degenerately. The `P`-boundary test (`tests/js/ranking.test.js:1268-1283`) separately confirms a value that clears `busy` but sits one point beyond `max - P` bands `busy`, not `peak`.
  - **Arrival-hour flooring.** L568: `arrivalHour = Math.floor(clockMinutesOfDay(arrivalAbs) / 60)`, where `clockMinutesOfDay` (pre-existing, `web/ranking.js:93-95`, unmodified — confirmed by the additive-only diff check above) returns the calendar-midnight offset in `[0, 1440)`. Confirmed by test `tests/js/ranking.test.js:1328-1342`: `:25` and `:59` past hour 10 both read hour 10's own bucket (`peak`), and exactly `11:00` reads hour 11's bucket (`quiet`) — never rounds or ceilings into the next hour.
- **`resolveSeatConfidence` (`web/ranking.js:624-635`) against `PLAN.md` "4. `seat_confidence`" (read directly, lines 240-273).**
  - **Explicit lookup and ladder clamping.** `SEATABILITY_LADDER`/`SEATABILITY_FROM_LEVEL`/`BUSYNESS_ADJUSTMENT` (L608-610) match `PLAN.md`'s table (`quiet`+1/`typical`+0/`busy`−1/`peak`−2) exactly, and `Math.min(4, Math.max(1, level + adjustment))` (L630) clamps both ends. Test `tests/js/ranking.test.js:1344-1356` iterates the full 4×5 baseline×band matrix (including `mixed`+`peak`→clamped to `poor`, and `dependable`+`quiet`→clamped-at-ceiling `dependable`) against an independently-recomputed expectation, not a copy of the source table.
  - **`unknown` baseline always `unknown`.** L625-627 short-circuits before the ladder is consulted at all. Test `tests/js/ranking.test.js:1358-1364` checks this across every band value including `unknown` itself.
  - **`unknown` busyness leaves baseline unchanged, flagged weak.** `BUSYNESS_ADJUSTMENT.unknown = 0` (L610) and `evidenceQuality` (L633) reads `"weak"` exactly when `busynessBand.band === "unknown"`, `"normal"` otherwise — a distinct field from the unchanged confidence value, so it cannot be mistaken for a determined `typical` reading. Confirmed by `tests/js/ranking.test.js:1366-1370` (unchanged confidence + `weak`) and `:1372-1377` (every determined band → `normal`).
- **Constants cross-checked against `DECISIONS.md`, not trusted from the code comment.** `BUSYNESS_N = 15`, `BUSYNESS_P = 5`, `MIN_HISTOGRAM_HOURS = 6` (`web/ranking.js:31-33`). `DECISIONS.md:1429` ("`N = 15`, set from measurement, not guessed"), `:1526-1531` ("`P = 5` stands, verified against the fix, not just carried over"), and `:623` (`MIN_HISTOGRAM_HOURS = 6`, provisional) are each the final, corrected entries in a chain of revisions — confirmed these are the values that survived, not an earlier superseded proposal (e.g. the initially-wrong `N = 25` at `DECISIONS.md:1403` was explicitly corrected to 15 at `:1429`).
- **Scope discipline.** `git diff bf75ce0 82dd79f -- web/ranking.js tests/js/ | grep -in "backup_strength|plan_a|plan_b|planA|planB|rankVenues|refresh\.py|app\.js"` returns zero matches — none of the excluded/deferred machinery appears anywhere in the diff.
- **No runtime read of `data/phase0/`.** `grep -rn "data/phase0" web/ tests/` and `grep -rn "readFileSync" web/ tests/` both return nothing — histogram/hours fixtures appear only as literal inline test data (`busynessVenue`/`makeVenue` helpers), never read from `data/phase0/` at runtime or in tests.
- **Independent spot-check of the commit message's `starbucks-centrepoint` claim — hand-verified against real Phase 0 data, not trusted.** `data/phase0/raw/details/seed_01.json` (place_id matches `starbucks-centrepoint` in `histograms.json`) gives Monday `regularOpeningHours` as `{open: 8:00, close: 22:00}` — i.e. `{open: 480, close: 1320}` in `resolveHours`'s existing minute contract. `openHourSet` on that period yields hours 8-21 (14 open hours), matching `data/phase0/histograms.json`'s real Monday entries at those hours: `[48,62,74,74,80,82,90,67,60,62,68,65,54,44]` for hours 8..21 respectively (hours 6,7,22,23 excluded as closed, each read a fabricated 0 in the source). Sorted: `[44,48,54,60,62,62,65,67,68,74,74,80,82,90]`; median of 14 values = `(65+67)/2 = 66`, max = `90`. **Hour 14** (value 90): `delta = 90-66 = 24 >= N(15)`, and `90 >= max-P (85)` → **`peak`**, matching the commit's claim. **Hour 21** (value 44): `delta = 44-66 = -22 <= -N(15)` → **`quiet`**, matching the commit's claim. Both hand-computed via a throwaway Python read of the two real data files (no script file left behind) — the commit message's numbers (median 66, peak at 14, quiet at 21) are correct against real data, not merely asserted.

### Could not verify

- Whether the real production `venues.json`/`build/refresh.py` pipeline will actually populate `venue.hours.regular_hours[weekday]` and `venue.popularTimes[weekday]` in exactly the shape `resolveBusynessBand` consumes — that pipeline does not exist yet (confirmed absent: no `build/refresh.py`, `app.js`, or `fetch_busyness.py`/`fetch_hours.py` anywhere in the repo), so this is necessarily verified only against the pre-existing `regular_hours` contract used elsewhere in `ranking.js` (`resolveHours`, `web/ranking.js:671`) and the hand-picked real Phase 0 fixture data above, not against a running end-to-end fetch-to-render pipeline.
- Whether `PLAN.md`'s deferred `very_quiet` band (mentioned as a possible future fifth band, explicitly not adopted here) is genuinely absent from every code path — confirmed by reading `BUSYNESS_ADJUSTMENT`/the four-branch band `if`/`else if` chain, which has no fifth branch, but I did not additionally grep the whole diff for the literal string `very_quiet` as a belt-and-suspenders check.
- Whether `CLAUDE.md`'s UI-facing requirement ("the UI must always show baseline and adjustment separately, never just the conclusion") is honored — there is no UI code in this diff to check; `resolveSeatConfidence`'s return shape (`{confidence, evidenceQuality}`) does not itself carry the baseline/band components back out to a caller, so a future UI layer will need to source those from its own inputs rather than the function's return value. Not a defect in this function taken alone (`PLAN.md`'s contract for `resolveSeatConfidence` itself is satisfied), but I did not chase this further since no rendering code exists yet to check it against.
- Runtime/model verification of which model actually authored the commit — not observable from this environment; not claimed either way.
- Whether `HANDOFF.md`'s prose (beyond the baseline/implementation commits and artifact identification, independently confirmed via `git log`/`git diff` above) accurately narrates every detail of the implementation — not re-derived beyond what this gate's checks cover.

### Not asked to check

- `backup_strength`, Plan A/B recalculation, and any top-level "rank all venues" function — explicitly deferred per the brief; confirmed absent from the diff (see Scope discipline above) but not otherwise evaluated.
- `build/refresh.py`, `app.js`, and any live busyness fetcher — explicitly excluded; confirmed none of these files exist in the repo (same as at `IMP-002`'s gate), their (non-existent) content was not evaluated.
- `tests/python/` and any Python-side validation of `venues_meta.json`'s `baseline_seatability` field shape — out of this assignment's `tests/js/`-scoped required verification.
- The eventual UI rendering of `seat_confidence`/`relative_busyness` (the "always show baseline and adjustment separately" requirement) — no UI code exists yet in this diff; see Could not verify above for the narrow point I did check.
- `PLAN.md`'s "5. `backup_strength`" section and everything downstream of it in the ranking pipeline — not this assignment's objective.

### Status

`GATE_PASS`

---
