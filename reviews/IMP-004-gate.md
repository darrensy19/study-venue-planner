# Gate record: IMP-004

## Gate invocation 1 — 2026-08-31

- **Gate route**: `claude_sonnet`, effort high
- **Brief source**: generated from `HANDOFF.md` via `.cross-agent-workflow/gate_brief.py HANDOFF.md` (brief supplied verbatim in the invoking prompt; not regenerated by me)

### Checks run

- **Baseline/implementation commit identity.** `git log --oneline -1 bdf7ada` confirms `bdf7ada` = "Implement backup_strength grading and Plan B recalculation (IMP-004)" and is `HEAD`. `git log --oneline -1 07e8b67` confirms `07e8b67` = "Close IMP-003: user-approved after round-2 review approval", with `04ba8d4` ("Allocate IMP-004") as the commit in between. `git status` shows only an uncommitted working-tree modification to `HANDOFF.md`, outside the reviewed diff.
- **Diff scope.** `git show 07e8b67..bdf7ada --stat` (both commits together) touches `HANDOFF.md`, `reviews/LEDGER.md` (allocation bookkeeping in `04ba8d4`), and `tests/js/ranking.test.js` (+233) / `web/ranking.js` (+241/-10) in `bdf7ada` — matching the brief's named artifact. `git diff 07e8b67 bdf7ada -- web/ranking.js tests/js/ranking.test.js` read in full (292 + 251 diff lines). No other file is touched; `build/refresh.py`, `app.js`, and any top-level ranking function are confirmed absent from the diff.
- **`node --test`, rerun independently.** `node --version` → `v18.15.0`. `node --test tests/js/*.test.js` (glob form) from repo root: `# tests 131`, `# pass 131`, `# fail 0`, `# cancelled 0`, `# skipped 0`, `# todo 0`. 17 new tests (1 `resolveFeasibilityAtArrivals`, 2 `resolvePlanBArrivals`, 1 `meetsConfidenceFloor`, 5 `resolveBackupStrength`, 8 `evaluatePlanBFallback`) plus the pre-existing 114 — matches the commit message's claimed baseline exactly (131 − 17 = 114).
- **PLAN.md sections read directly**: "5. `backup_strength`" (lines 275-301) and "Plan A and Plan B" / "Plan B is recalculated, not just second place" / "Plan B viability floor" (lines 1570-1616), plus the `CLAUDE.md` Plan A/Plan B and return-transport bullets, which restate the same contract.
- **Item 1 — refactor safety.** Read the diff hunk-by-hunk: `resolveFeasibility`'s body (departure→arrivals derivation, then `resolveBound` mid/upper, tier composition) is moved verbatim into the new `resolveFeasibilityAtArrivals`, with `resolveFeasibility` reduced to computing `departureAbs`/`arrivalMidAbs`/`arrivalUpperAbs` and delegating. Same pattern for `resolveOverallFeasibility` → `resolveOverallFeasibilityAtArrivals`: the hours/return composition body is unchanged text, only the departure-derived `arrivalMidAbs`/`arrivalUpperAbs` locals became parameters. No arithmetic differs — confirmed by diffing the moved bodies against their originals line-for-line. The test-file diff is purely additive (new imports appended to the existing import list, 230 new lines appended after the last existing test) — none of the pre-existing 114 test bodies were touched, and all 114 still pass (see above).
- **Item 2 — Plan B's two independent upper bounds.** `resolvePlanBArrivals`: `departureUpperAbs = planAArrivalUpperAbs + seatCheckBufferMinutes`, then `arrivalUpperAbs = departureUpperAbs + fallbackTravelMinutesUpper` — i.e. `planAArrivalUpperAbs + seatCheckBufferMinutes + fallbackTravelMinutesUpper`, the sum of two independently-supplied upper bounds, never a midpoint-derived arrival. Confirmed both by reading the code and by the dedicated test with concrete numbers (`planAArrivalUpperAbs: 1050, fallbackTravelMinutesUpper: 30, seatCheckBufferMinutes: 10` → `arrivalUpperAbs: 1090`, hand-checked: 1050+10+30=1090).
- **Item 3 — unverified-return capping.** `resolveBackupStrength`: when `overallTier` is not `robust`/`tight`, it checks `usableMinutesMid >= minSessionMinutes` and returns `salvage` with `reason: overallTier === "unverified" ? "unverified_return" : "short_session"` — an `unverified` tier can only reach `salvage`, never `strong` (the `strong` branch requires `overallTier === "robust" || "tight"`, checked first and returned unconditionally before the unverified case is reached). Exercised end-to-end in `evaluatePlanBFallback` by a realistic fixture (not mocked internals): an always-open fallback venue with `access.origin_a = {}` (a genuine empty-route object, not a stub of internal functions) forces `admissibleReturnModes` to return an empty set, which `resolveReturnBound` turns into `unverified`/`no_recorded_route` for both bounds, composing to `overallTier: "unverified"`. I hand-traced `resolveSeatConfidence("mixed", <unknown busyness>)` = `{confidence: "mixed"}` (no popular-times data → busyness `unknown` → baseline unchanged) and confirmed `usableMinutesMid: 180` (hours-only metrics, always-open, full duration) — both match the test's asserted `strength: "salvage"`, `reason: "unverified_return"`. Would a `strong` path have been reachable, the test's `overallTier: "unverified"` assertion or `strength` assertion would fail; they do not.
- **Item 4 — cycle-fallback exclusion.** `evaluatePlanBFallback` checks `if (fallbackMode === "cycle" && !bicycleWithYou) return { strength: "none", reason: "cycle_fallback_unviable" };` as its first statement, before `resolvePlanBArrivals` or `resolveOverallFeasibilityAtArrivals` is called and before any property of `fallbackVenue` is read. The dedicated test passes a `tripwireVenue` object whose `hours`, `access`, and `baseline_seatability` are getters that `throw` if read — the test passing (no throw) is direct proof no venue data is read, not an inference from the final `strength` value alone.
- **Item 5 — return-capped vs hours-capped floor, hand-recomputed.** `resolveBackupStrength` reads `usableMinutesMid` from its caller (`evaluatePlanBFallback` passes `overall.usableMinutesMid`, i.e. `resolveOverallFeasibilityAtArrivals`'s own output) and never touches `durationMinutes` directly. For the dedicated test ("the salvage/none floor reads the return-capped usable minutes..."): venue is `always_open` Monday, `access.origin_a = {transit: {}}` (schedule-bound only, no schedule-free mode), `return_transport.origin_a.transit.default.last_departure_band = "21:40-21:50"`. `arrivalAbs = 20:50 (1250)`; Plan B arrivals resolve to `1250` for both bounds (`planAArrivalMidAbs/UpperAbs = arrivalAbs − 10`, `+10` seat-check buffer, `+0` fallback travel). Session end = `1250 + 180 = 1430` (23:50), past `RETURN_CORE_UNTIL_MINUTES` (1290/21:30), so the core-span waiver does not apply and the actual `return_transport` timetable is read. `normaliseBand("21:40-21:50")` → `lo=1300, hi=1310` (both already ≥ `RETURN_SERVICE_DAY_START_MINUTES`=240, no day-shift). `lastDepartureEdge(band, "mid") = floor((1300+1310)/2) = 1305`; `lastDepartureEdge(band, "upper") = lo = 1300`. Upper margin (`1300−1430 = −130`) fails `robust`; mid shortfall (`1430−1305=125`) exceeds `toleranceMinutes` (10), so return tier is `shorter`, composing (against hours-side `robust`, always-open) to `overallTier: "shorter"` — matches the test. `combineBindingLimit`: hours side is `COVERED` (always-open reaches past the required end), return outcome is `last_departure` at `1305` → `bindingConstraint: "last_departure", bindingLimitAbs: 1305`. `boundBindingMetrics`: `usableMinutes = min(1305, 1250+180=1430) − 1250 = 55`. This matches the test's asserted `usableMinutesMid: 55` — independently recomputed by hand from the fixture, not merely re-derived from the implementation. Had the code used the raw hours-capped duration (180) instead, the assertion would read 180 and fail; 55 < 90 correctly drives `strength: "none"`, `reason: "below_minimum_minutes"`.
- **Item 6 — confidence floor / unknown handling.** `SEATABILITY_LADDER = { poor:1, mixed:2, usually_available:3, dependable:4 }` has no `unknown` key (pre-existing, unmodified). `meetsConfidenceFloor("unknown", "mixed")`: `level = SEATABILITY_LADDER["unknown"] = undefined`, so the function returns `false` via its `level !== undefined && ...` guard — confirmed directly by the dedicated unit test. Wiring: `evaluatePlanBFallback` always derives `confidence` from `resolveSeatConfidence(fallbackVenue.baseline_seatability, busyness)` and passes it straight into `resolveBackupStrength`, which calls `meetsConfidenceFloor` first and returns `{strength: "none", reason: "confidence_below_floor"}` before either the `strong` or `salvage` branch is reached — an `unknown`-confidence fallback (whether from `baseline_seatability: "unknown"` short-circuiting in `resolveSeatConfidence`, unmodified pre-existing code) can never reach `strong` or `salvage`. **Gap**: no test exercises `baseline_seatability: "unknown"` end-to-end through `evaluatePlanBFallback` itself — the closest coverage is (a) the direct `meetsConfidenceFloor("unknown", "mixed")` unit test and (b) an end-to-end `evaluatePlanBFallback` test using `baseline: "poor"` for the same `confidence_below_floor` code path. The `unknown` case is provably wired by reading the two functions together, but not demonstrated by a single end-to-end test the way `poor` is. Not blocking — the logic is unit-tested and the wiring is a one-line, un-branched pass-through — but noted as a minor coverage gap.
- **Item 7 — acceptance-criteria-to-test mapping.** All six brief items have direct test coverage: the dual-bound arrival chain (`resolvePlanBArrivals` tests, 2), independent resolution through the same machinery / no shared-departure shortcut (`resolveFeasibilityAtArrivals` test showing mid/upper landing on different calendar dates; `evaluatePlanBFallback` tests feeding two independently-resolved arrivals into `resolveOverallFeasibilityAtArrivals` directly), the three-way `backup_strength` grading on `overall_tier` and confidence floor (`resolveBackupStrength` ×5, `evaluatePlanBFallback` strong/salvage/none cases), `unverified`-return capping at `salvage` (item 3 above), the `cycle`-mode exclusion (item 4 above), and the return-capped-never-hours-capped floor (item 5 above). No acceptance-criteria item is untested.
- **Scope discipline.** No band-string ("N-Mm") parsing of `fallbacks[].travel_band`/`access[][].band` appears anywhere in the diff — `evaluatePlanBFallback`'s signature takes `fallbackTravelMinutesMid`/`fallbackTravelMinutesUpper` as pre-resolved numbers, exactly mirroring `resolveOverallFeasibility`'s existing `travelMinutesMid`/`travelMinutesUpper` parameter shape. No fallback-selection/ranking logic exists — `evaluatePlanBFallback` evaluates exactly one given candidate. `build/refresh.py` and `app.js` do not exist in the repo; no top-level "rank all venues" function is touched or added.

### Could not verify

- Whether the real production pipeline (not yet built — no `build/refresh.py`/`app.js`) will supply `fallbacks[].travel_band` and `access[][].band` in the shape this diff assumes for the future band-parsing step — that parsing is explicitly out of scope for this assignment and does not exist yet anywhere in the repo.
- Runtime/model verification of which model actually authored the commit — not observable from this environment; not claimed either way.
- Whether `HANDOFF.md`'s prose (beyond the baseline/implementation commits and artifact identification, independently confirmed via `git log`/`git diff` above) accurately narrates every detail of the implementation — not re-derived beyond what this gate's checks cover. Note: the working tree currently has an uncommitted modification to `HANDOFF.md`, outside the `07e8b67..bdf7ada` diff under review; not evaluated as part of this gate.

### Not asked to check

- Selecting or ranking the best of several fallback candidates for one venue — explicitly deferred to the top-level ranking function per the brief; confirmed absent from the diff (see Scope discipline above).
- Band-string ("N-Mm") parsing for `fallbacks[].travel_band` / `access[][].band` — explicitly deferred; confirmed absent (see Scope discipline above).
- `build/refresh.py`, `app.js`, and any top-level ranking function — explicitly excluded; confirmed none of these exist in the repo, their (non-existent) content was not evaluated.
- `tests/python/` and any Python-side data validation — out of this assignment's `tests/js/`-scoped required verification.
- UI presentation of `backup_strength`/Plan B (labelling a `salvage` option with its actual duration, "If full: Plan B" layout, etc.) — no UI code exists in this diff.
- The uncommitted working-tree change to `HANDOFF.md` — outside the reviewed diff.

### Status

`GATE_PASS`

---
