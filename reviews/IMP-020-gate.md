# Gate record: IMP-020

## Gate invocation 1 — 2026-09-06

- **Gate route**: `claude_sonnet`, effort high
- **Brief source**: generated from HANDOFF.md acceptance criteria + required verification (mechanical brief, `.cross-agent-workflow/gate_brief.py HANDOFF.md`, reproduced verbatim in the invocation prompt)

### Checks run

- **Diff scope confirmed**: `git diff b81c55f --stat -- web/ranking.js tests/js/ranking.test.js` = 200 lines changed in `ranking.js` (176 ins/24 del once measured directly), 318 lines added in the test file — matches the artifact-under-review claim. `git status --short` also shows `BACKLOG.md`, `HANDOFF.md`, `reviews/LEDGER.md`, `web/index.html` modified and an untracked `.claude/` — the first three are primary bookkeeping outside the declared artifact scope (not reviewed for content), `web/index.html` is the declared regenerated artifact (checked separately below).
- **Read in full**: the entire diff hunk of `web/ranking.js` (`boundBindingMetrics`, `resolveOverallFeasibilityAtArrivals`, `resolveBound`, `resolveFeasibilityAtArrivals`, `deriveLatestLeaveAtState`, `stripFreshnessFields`, `resolveVenueNaming`, the `rankVenues()` per-venue loop and its Plan B/candidate assembly, `bestAlternative`/`refusalInstant`) and all 27 new tests plus the 2 updated tests in `tests/js/ranking.test.js`.
- **Governing spec sections read**: PLAN.md "Requested end vs achievable end", "Evidence freshness and a first-ever failed source", "The returned presentation shape", "Choosing among several fallbacks", "Naming and presentation hierarchy", and the "Phase 1 review-response slice order" table's slice-1b row. Cross-checked against `CLAUDE.md`'s condensed equivalents.
- **`.venv/bin/pytest tests/python/ -q`**: `190 passed in 1.85s` — matches the claimed unchanged count, no regressions.
- **`node --test tests/js/*.test.js`**: `# tests 233`, `# pass 233`, `# fail 0` — matches the claimed count, up from 206 baseline (233 − 27 new = 206).
- **`make generate` offline reproducibility**: ran with `http_proxy`/`https_proxy` pointed at an unlistening local port (`127.0.0.1:9`) so any real network call would hang/fail rather than silently succeed; ran twice. Both runs produced `web/index.html` with identical MD5 `b5fb88331fa693765bd07882ba41109b`; a byte-for-byte `diff` between the two run outputs was empty. A third comparison against the pre-existing (already-committed) `web/index.html` in the working tree was also byte-identical, confirming the checked-in regenerated file is exactly what `make generate` reproduces deterministically offline.
- **Non-vacuity spot-check 1 — `achievableSessionEndMid`**: reverted the computation to the always-requested-end form (`arrivalMidAbs + durationMinutes` unconditionally, dropping the `usableMinutesMid` dependency and the `undefined` case). Reran `node --test tests/js/ranking.test.js`: 2 new failures (`achievableSessionEndMid: a shorter-tier candidate's achievable end is the close-derived instant...` and `...undefined exactly when usableMinutesMid is`), 226/228 passed. Restored the file from a pre-check backup; `md5` matched the pre-revert hash (`019d5f7221e28cb83f5a1a430fece962`).
- **Non-vacuity spot-check 2 — `bindingLimitMid` propagation**: hardcoded the final candidate-assembly line to `bindingLimitMid: undefined`, bypassing the `resolveBound`→`boundBindingMetrics`→`resolveOverallFeasibilityAtArrivals` plumbing. Reran the suite: 4 new failures (all four `bindingLimitMid:` tests), 224/228 passed. Restored; MD5 matched.
- **Non-vacuity spot-check 3 — `deriveLatestLeaveAtState` comparison direction**: flipped `>=` to `<` in `return latestLeaveAt >= departureAbs ? "future" : "past"`. Reran: 2 new failures (`future` and `past` direct-function tests), 226/228 passed. Restored; MD5 matched.
- **Non-vacuity spot-check 4 — `stripFreshnessFields` reverted to `return {}`** (this is the check the acceptance criteria call out by name as needing to be *proven* non-vacuous, not merely asserted): with the function body replaced by `return {};`, reran the suite. Result: the **direct synthetic-object test failed** (`stripFreshnessFields: deletes hoursStatus/histogramStatus at every depth...`, 1 failure, 227/228 passed) — confirming that test alone catches the vacuous projector. Separately confirmed the actual point of the requirement: the **structural stale-data round-trip test still passed** (`the stale-data invariant: flipping only hoursStatus/histogramStatus to stale changes nothing once both are stripped` — `ok`) even with the broken implementation, because both the `ok` and `stale` results collapse to `{}` and trivially deep-equal. This is exactly the failure mode the direct test exists to catch that the round-trip test cannot. Restored the file; MD5 matched the pre-revert hash, and `git diff --stat web/ranking.js` after restoration was identical to the diff-stat measured before any spot-check began.
- **Working-tree integrity after all four reverts**: `git status --short` and `git diff --stat` (full repo) after the last restoration match what was observed before spot-checking began — no residual change from the revert/restore cycles.
- **`app.js` blast-radius check (not itself in the acceptance-criteria text, but within the diff's blast radius per WORKFLOW.md's diff-first rule)**: read `web/app.js` directly. It defines its own local `displayName(venueId)` (title-cases the raw ID) and never reads a `.name` field off any `rankVenues()` output at all — not before this diff, not after. Every place `app.js` reads from `removed`/`travelUnknown`/`planB`/candidates uses `venueId`, `mode`, `strength`, `overallTier`, `usableMinutesMid`, `latestLeaveAt`, `sessionEndMidAbs`, `travelMinutesMid` (on the candidate, a pre-existing top-level field, not `planB.travelMinutesMid`) — all preserved, none renamed or removed. Confirms the primary's claim that `app.js` needs no changes and that every new field is additive; the new `displayName`/`disambiguatedLabel`/`hoursStatus`/`histogramStatus`/`achievableSessionEndMid`/`bindingLimitMid`/`latestLeaveAtState`/`bestAlternative`/`refusalInstant`/`planB.travelMinutesMid` fields are all inert as far as the current renderer goes (correctly deferred to slice 2 per the scope exclusion).
- **Hard-filtered/aggregate-only invariant**: read the "missing access key entirely" branch directly — it still only increments `hardFilteredCount` and `continue`s, never touches `removed`/`travelUnknown`/naming, matching "hard-filtered venues stay aggregate-only" from both PLAN.md and the acceptance criteria.
- **Reserved freshness-field-name collision check**: grepped `web/ranking.js` for every occurrence of `hoursStatus`/`histogramStatus` — all are either the `FRESHNESS_FIELD_NAMES` set definition, doc comments, or genuine freshness reads/assignments (candidate, removed, travelUnknown, planB); no other field in the diff reuses either name at any depth.
- **By-reference propagation check**: confirmed `groups.ranked`/`groups.shorter`/`groups.unverified`/`alternatives`/`planA`/`bestAlternative` are all filters over the same `candidates` array (never reconstructed objects), so every new candidate field (naming, freshness, `achievableSessionEndMid`, `bindingLimitMid`, `latestLeaveAtState`) propagates to all of them automatically — satisfies "every candidate/`planA`/`bestAlternative` carries..." by construction, not by a per-surface duplication that could drift.
- **`refusalInstant` judgement call**: formed an independent view (see "Not asked to check" below) rather than deferring silently.

### Could not verify

- None. Every item in the mechanical brief's "Required verification" was independently reproduced first-hand (pytest, node --test, double offline `make generate`, and revert-based non-vacuity spot-checks).

### Not asked to check

- **`BACKLOG.md`, `HANDOFF.md`, `reviews/LEDGER.md` diff content**: these show as modified in `git status` but are outside the brief's declared "Artifact under review" (`web/ranking.js`, `tests/js/ranking.test.js`, `web/index.html`). I did not audit their prose for accuracy — e.g. whether `HANDOFF.md`'s own acceptance-criteria/verification text matches what was actually done, beyond using it as the brief's source text. That cross-check is implicitly what this gate invocation itself performs on the acceptance-criteria and verification lines, but I did not separately diff-review `BACKLOG.md`/`LEDGER.md`'s prose edits.
- **Only 4 of the 7 non-vacuity items were independently re-executed as revert-and-restore experiments** (`achievableSessionEndMid`, `bindingLimitMid` propagation, `deriveLatestLeaveAtState`'s comparison direction, and `stripFreshnessFields`'s vacuous-projector case — the one the acceptance criteria single out by name). The brief's required-verification list names three more the primary claims to have reverted (`bestAlternative`/`refusalInstant` hardcoded to null/undefined, `disambiguatedLabel`'s area-suffix condition, `planB.travelMinutesMid`); for those three I verified by reading the implementation and confirming a corresponding test exists that reads the actual computed value (not a stub) and would fail against a hardcoded/vacuous substitute — but I did not personally re-run the revert-and-restore cycle on those three. The task instructions frame 3 as the floor ("pick at least 3... and actually revert them"), and I did 4, prioritizing `stripFreshnessFields` since the acceptance criteria call it out specifically; this is disclosed rather than silently treated as equivalent to having executed all seven.
- **The `refusalInstant` definition is a judgement call the brief itself flags for reviewer confirmation, not gate resolution.** My own reading: PLAN.md's older refusal-message wording ("No option with a verified way home for a session ending at HH:MM", line ~1830) predates this diff and already refers to a single, venue-independent "session ending at HH:MM" — which only the *requested* nominal end (`departureAbs + durationMinutes`, travel excluded) can supply under `no_verified_return`, since by definition no candidate there has a verified return to hang a per-venue achievable end off. I consider `departureAbs + durationMinutes` a reasonable and reasonably well-supported reading of that pre-existing wording, not an arbitrary guess — but it is still a judgement call (the alternative reading, some travel-inclusive instant, is not obviously wrong either, and PLAN.md gives no formula, only the illustrative example) and I did not treat it as settled; it should still get explicit reviewer confirmation as the brief itself requests.
- **`web/index.html`'s content was checked only for offline-reproducibility (byte-identical across two runs and against the working-tree copy), not diffed field-by-field against the new `ranking.js` output** — i.e. I did not manually inspect that the regenerated HTML actually embeds the new fields correctly inside its data payload beyond trusting that `build/generate.py` serializes whatever `rankVenues()`/the venue JSON produces. This is a reasonable trust boundary (generate.py is unchanged by this diff and not part of the artifact under review) but is stated explicitly rather than silently assumed.

### Status

`GATE_PASS`

---
