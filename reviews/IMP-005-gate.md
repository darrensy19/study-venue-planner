# Gate record: IMP-005

## Gate invocation 1 — 2026-08-31

- **Gate route**: `claude_sonnet`, effort high
- **Brief source**: generated from `HANDOFF.md` via `.cross-agent-workflow/gate_brief.py HANDOFF.md` (brief supplied verbatim in the invoking prompt; not regenerated by me)

### Checks run

- **Baseline/implementation commit identity.** `git log --oneline -1 12f7388` confirms `12f7388` = "Update README Status section: baseline_seatability assessed, ranking.js core built". `git log --oneline -1 c1954cc` confirms `c1954cc` = "Implement travel-band string-to-minutes parser (IMP-005)" and is `HEAD`. `git log --oneline 12f7388..c1954cc` shows exactly two commits in between/at HEAD: `863b7a2` ("Allocate IMP-005…") then `c1954cc`. `git status` shows a clean working tree, nothing uncommitted.
- **Diff scope.** `git diff 12f7388 c1954cc --stat` touches only `HANDOFF.md` (+13/-13, from the allocation commit `863b7a2`), `reviews/LEDGER.md` (+1, allocation bookkeeping), `tests/js/ranking.test.js` (+35), `web/ranking.js` (+32). `git show 863b7a2 --stat` confirms the allocation commit touches only `HANDOFF.md`/`reviews/LEDGER.md`; `git show c1954cc --stat` confirms the implementation commit touches only the two named artifacts, `tests/js/ranking.test.js` (+35) and `web/ranking.js` (+32), both purely additive (no lines removed in either file). Read `git diff 12f7388 c1954cc -- web/ranking.js tests/js/ranking.test.js` in full (67 diff lines): the only production change is one new exported function, `normaliseTravelBand`, appended after `lastDepartureEdge`; the only test change is one new import plus 7 new `test()` blocks appended after the `lastDepartureEdge` tests. No wiring into `resolveOverallFeasibility` or `evaluatePlanBFallback` — neither function's diff hunk appears anywhere in the change. `build/refresh.py` and `app.js` do not exist in the repo (confirmed absent, not touched or created). No fallback-selection/ranking logic (no loop over `fallbacks[]`, no comparison between multiple candidates) appears anywhere in the diff.
- **PLAN.md sections read directly.** "Time, dates and hours resolution" → "Travel: two derived values" (lines 493-501): `travel_minutes_mid = band midpoint`, `travel_minutes_upper = band upper edge`, and "The same derivation applies to `fallbacks[].travel_band`" (line 501) — confirms mid = midpoint, upper = upper edge, one shared derivation for both fields. "Data contracts" → `data/venues_meta.json` field table (lines ~1800-1810): `**access** — ordinal rank plus a coarse band, never exact minutes. **Missing** mode key = not viable. Explicit **`null`** = not yet measured.` — confirms the explicit-null-vs-missing-key distinction claimed in the brief. The `fallbacks` field description (same table) reads only `**fallbacks** — hand-picked links to plausible nearby venues only. Not a matrix; most venues will have zero, one or two.` and the worked example shows only a complete entry, `{"venue_id": "coffee-bean-beauty-world", "mode": "walk", "travel_band": "5-10m"}` — no null case for `travel_band` is stated anywhere I could find (grepped every `band`-adjacent line in `PLAN.md`), matching the brief's claim that this is untreated in the contract and therefore correctly left as "malformed is a rejection" territory rather than guessed at.
- **`node --test`, rerun independently.** `node --version` → `v18.15.0`. `node --test tests/js/*.test.js` (glob form) from repo root: `# tests 138`, `# pass 138`, `# fail 0`, `# cancelled 0`, `# skipped 0`, `# todo 0`, no errors or warnings in the output. IMP-004's gate recorded 131 as the prior total; 138 − 131 = 7, matching the 7 new `test()` blocks added for `normaliseTravelBand` (ordinary band, even-width band, explicit null, 5 malformed-string cases in one test, equal/inverted edges in one test, 3 non-string/non-null cases in one test).
- **Code read directly** (`web/ranking.js`, new `normaliseTravelBand` function): `if (band === null) return { kind: "not_measured" };` handles the explicit-null case before any parsing. `const m = typeof band === "string" ? /^([0-9]+)-([0-9]+)m$/.exec(band) : null;` rejects any non-string, non-null input (numbers, `undefined`, objects) by construction — `typeof` guard means it never reaches `.exec`. The regex `^([0-9]+)-([0-9]+)m$` is fully anchored, requires one or more digits on each side, a literal `-` separator, and a literal trailing `m` with nothing after it — no negative sign is matchable (`-` only appears as the required separator, and a leading `-` fails to match `[0-9]+` at position 0), no decimals, no trailing/leading whitespace tolerated. `if (!(lo < hi)) return { kind: "malformed", … }` rejects `N >= M`. On success: `{ kind: "present", mid: Math.floor((lo + hi) / 2), upper: hi }`.
- **Every acceptance-criteria item checked against both code and tests, with independent hand-recomputation of the numeric formula:**
  - `"N-Mm" → mid=(N+M)/2, upper=M`, floored: hand-recomputed two examples not lifted from the test file's own assertions — `"12-18m"`: `lo=12, hi=18`, `(12+18)/2=15` (exact, no flooring needed) → expect `{mid:15, upper:18}`; tracing the code confirms `Math.floor(15)=15`, `upper=18`. `"7-14m"`: `lo=7, hi=14`, `(7+14)/2=10.5` → floor → `10`; tracing the code confirms `Math.floor(10.5)=10`, `upper=14`. Both match the code's formula exactly. Cross-checked against the test file's own two odd-width cases: `"15-20m"` → `(15+20)/2=17.5→17`, `upper=20`, asserted as `{mid:17, upper:20}` — matches; `"5-10m"` → `(5+10)/2=7.5→7`, `upper=10`, asserted as `{mid:7, upper:10}` — matches. Even-width case `"10-20m"` → `(10+20)/2=15` exact, asserted `{mid:15, upper:20}` — matches.
  - `access[][].band` explicit `null` → not-measured sentinel, never throws: `normaliseTravelBand(null)` returns `{kind:"not_measured"}` by the first line of the function, before the regex or any property access — cannot throw on `null`. Test `"explicit null is not_measured, distinct from malformed"` asserts exactly this.
  - `fallbacks[].travel_band` has no stated null case, treated as required: confirmed against `PLAN.md` above — no null case exists in the contract for this field. The function itself does not special-case a caller-specific "required" rule (correctly out of scope, since wiring is excluded) — it reports `not_measured` for any `null` input regardless of which field it came from, and the JSDoc explicitly states "a caller for which null is never legitimate (e.g. a `fallbacks[].travel_band` entry…) must reject `not_measured` itself; this function only reports what the value means, not whether it's acceptable in a given context." This is a correct and honest scoping — the "reject it" enforcement doesn't exist yet because the caller wiring is excluded from this assignment, and the function doesn't overreach into guessing which field it's being called for.
  - Malformed/non-numeric/`N>=M`/missing-`"m"` → rejected, fail-closed, never coerced: verified via the regex and `lo<hi` guard above, and via 11 distinct test assertions across 3 `test()` blocks covering: missing `"m"` suffix, trailing `" min"`, pure garbage, empty string, leading-negative, equal edges, inverted edges, a number, `undefined`, and a plain object — all assert `.kind === "malformed"`, none throw.
  - Pure function, no DOM: `normaliseTravelBand` reads only its single parameter, calls no browser/DOM API, and is a plain synchronous function — consistent with the rest of `ranking.js`.
  - Covered by `tests/js`: all of the above are directly exercised in `tests/js/ranking.test.js`; see the `node --test` run above.
- **Scope exclusions, re-confirmed directly against the diff** (not merely from the commit message): no hunk in `web/ranking.js`'s diff touches `resolveOverallFeasibility` or `evaluatePlanBFallback` — the entire production diff is the one new function block; `build/refresh.py` and `app.js` remain absent from the repo; no code iterating or comparing multiple `fallbacks[]` entries exists anywhere in the diff.

### Could not verify

- Whether the floored-midpoint convention for an odd-width travel band (as opposed to, say, rounding or truncating differently) is itself mandated anywhere in `PLAN.md` — the "Travel: two derived values" section says only "band midpoint," not explicitly "floored." The implementation's choice to floor is consistent with the project's "integer minutes" invariant (stated at the top of "Time, dates and hours resolution") and mirrors the existing floored-midpoint convention in `lastDepartureEdge` for return bands, but I could not find contract text in `PLAN.md` that pins down flooring (vs. rounding) specifically for travel bands. Not treated as a discrepancy — it is the only integer-valid reading and matches an established sibling convention — but noted since it isn't verbatim-specified.
- Untested input shapes not named in the acceptance criteria (e.g. leading zeros like `"05-10m"`, or a string with internal whitespace like `" 5-10m"`) — the regex would reject leading/trailing whitespace as malformed and would accept leading zeros as valid digits, but neither is covered by an explicit test. Not blocking — the acceptance criteria's listed malformed cases (non-numeric, `N>=M`, missing `"m"`) are all covered.
- Runtime/model verification of which model actually authored the commits — not observable from this environment; not claimed either way.

### Not asked to check

- Wiring `normaliseTravelBand` into `resolveOverallFeasibility`/`evaluatePlanBFallback` — explicitly excluded per the brief; confirmed absent from the diff (see Scope exclusions above).
- `build/refresh.py`, `app.js` — explicitly excluded; confirmed absent from the repo entirely.
- Selecting/ranking among several fallbacks for one venue — explicitly excluded; confirmed no such logic exists in the diff.
- `tests/python/` and any Python-side data validation — out of this assignment's `tests/js/`-scoped required verification.
- Whether real `venues_meta.json` data (not yet built into a pipeline) will actually supply `"N-Mm"`-shaped strings in practice — no fetcher/build step exists yet to check against.

### Status

`GATE_PASS`

---
