# Gate record: IMP-015

## Gate invocation 1 — 2026-09-06

- **Gate route**: `claude_sonnet`, effort high
- **Brief source**: generated from HANDOFF.md acceptance criteria + required verification

### Checks run

- **Artifact identity**: confirmed `docs/superpowers/specs/2026-09-05-review-response-design.md` sha256 is `1805cfd9433b21db564d16582a700f7b2e6031219d38ff845a1c2b3156ceda50`, matching HANDOFF.md's stated revision-5 hash exactly.
- **Diff scope**: `git diff d99f982 -- PLAN.md CLAUDE.md` touches only `PLAN.md` and `CLAUDE.md`; `git log --oneline -5` confirms `d99f982` is HEAD, so no other file changed and no code was touched — consistent with "no code" in the objective.
- **§3 Plan A eligibility (Decisions 1-5)**: all five present and non-contradictory. PLAN.md's new "Plan A eligibility" section (lines 1793-1799) states the `overall_tier ∈ {robust, tight}` AND `seat_confidence ≥ mixed` test, the "no duration floor, no new constant" withdrawal of `PLAN_A_MIN_SESSION_MINUTES` with the same `PLAN_B_MIN_SESSION_MINUTES`-analogy reasoning as the design, the filter-never-resort rule with the established sort order, `shorter`/`bestAlternative` treatment, and the "closed when you'd arrive" rendering rule. `grep -n "PLAN_A_MIN_SESSION_MINUTES"` returns nothing in either file — the withdrawn constant was not reintroduced as adopted.
- **§4 result states (Decisions 6-10, 34-40)**: PLAN.md's new "Result states" section (lines ~1810-1852) reproduces the six-state table verbatim in substance (order, condition, `resultState`, meaning, fix), the totality/disjointness argument from `overallTier()`, the `nothing_evaluable` four-diagnostic table, the `invalid_request` validated-fields list and payload contract, and Decision 40's control-contract description (single source, static export available pre-render, deep immutability, `step` as UI-only). CLAUDE.md's corresponding bullets (new lines 16-20) match. No contradiction found.
- **`refusals` stale wording**: `grep -n "refusals" PLAN.md CLAUDE.md` returns only the generic English word ("Grouping, refusals and final ordering", "refusals included", "expressing two refusals at once") — no trace of the old three-boolean `refusals` field shape.
- **`FEASIBILITY_TOLERANCE_MINUTES` constant table**: `PLAN.md:328` still reads `| FEASIBILITY_TOLERANCE_MINUTES = 15 | constant (provisional) | ranking.js |` — unchanged by this diff (not present in the `git diff` hunks touching that region), matching Decision 31's instruction that only ownership (already correctly stated as `ranking.js`) is being confirmed, not the value retuned.
- **§5 achievable end (Decisions 11-12)**: PLAN.md's "Requested end vs achievable end" section reproduces the three-named-instants table and the `latest_leave_at` four-state table verbatim in substance, including the `UNDETERMINED`/`COVERED` non-fabrication rule.
- **§6 freshness/failed-source (Decisions 13-15, 36-39)**: PLAN.md's "Evidence freshness and a first-ever failed source" section covers separate-never-merged signals, `hoursStatus`/`histogramStatus` as reserved names, the structural recursive-deletion invariant, the two-malformed-shapes totality requirement for `resolveHours`, the before-`business_status`-gate ordering, and the `resolved_name` registry fallback for unlabelled removals. The projector's own dedicated non-vacuity test (part of Decision 15) is correctly placed in the testing sections instead (CLAUDE.md line 140 and PLAN.md's "Review-response coverage" paragraph) rather than the architecture section — appropriate, not an omission.
- **§7 publication (Decisions 16-18)**: PLAN.md's refresh step list (new step 0 preflight, rewritten steps 7-8) and CLAUDE.md's corresponding two bullets state validate-both-before-replacing-either via the staged-path seam with no generator signature change, the preflight-before-first-API-call requirement, and the new `make generate` no-network target. Matches.
- **§8-9 presentation/duration (Decisions 19-27)**: PLAN.md's "Naming and presentation hierarchy" and "The returned presentation shape" sections reproduce the three-surface table, the always-visible set, the two naming fields (`displayName`/`disambiguatedLabel`), the label-vocabulary contract, day-marker/absolute-instant rule, DOM-free focus preservation, and the no-framework restatement. Decision 27's duration-bounds enforcement is represented through the control-contract mechanism (duration bounds `[180,360]` in the "Invalid requests" paragraph, form built from the same contract) rather than restating literal `min:180`/`max:360` twice — consistent with Decision 40's own point that a value should not be declared in two places.
- **§10 returned shape (Decisions 32-33)**: PLAN.md's "The returned presentation shape" table lists every "added" row from the design's Decision 32 audit (achievable duration/end, `metricsBasis`, binding limit, latest-leave state+meaning, freshness, Plan B transfer time, `bestAlternative`, `resultState`, empty-board diagnostics, invalid-control explanation, day-marked refusal instant) and states Decision 33's bestAlternative-is-pipeline-named rule.
- **§11-12 test strategy (Decisions 28-30, acceptance criteria)**: CLAUDE.md's "Review-response coverage" paragraph and PLAN.md's matching testing-section paragraph cover the DOM-stub bounded scope, `app.js` importability, and most of §12's named acceptance-criteria groups (Plan A eligibility, result states, `nothing_evaluable` diagnostics, `invalid_request`/control-contract sentinel test, requested-vs-achievable end, the two-level stale-data non-vacuity, failed-source handling with both malformed shapes, publication robustness, and the three new manual-checklist items verbatim from §12's "Manual checklist additions"). **One named acceptance-criteria group is missing: "Tolerance ownership"** (design lines 922-927) — its three bullets (`rankVenues()` with no `toleranceMinutes` matches an explicit `15`; no `FEASIBILITY_TOLERANCE_MINUTES` declaration remains in `app.js`, asserted against the source; an explicit override still takes effect) do not appear anywhere in PLAN.md or CLAUDE.md. Confirmed by exhaustive grep for `toleranceMinutes`, `tolerance ownership`, `declaration remains`, and `no longer declares` across both files — zero hits outside the design doc itself.
- **§13 slice order**: PLAN.md's "Phase 1 review-response slice order" table reproduces slices 0, 0b, 1a, 1b, 2, 3, 4, 5 with matching "touches"/"why here" content, and the closing "outbound is not a fix for the zero-usable-minutes defect" sentence. Slice 3's design-doc text still names it `(IMP-015)` (a stale forward-reference, since IMP-015 was reassigned to this documentation assignment) — PLAN.md's transcription correctly **omits** that now-incorrect ID rather than propagating it, describing the work by function only (`outbound_admissible` + `resolve_outbound_service`, `validate_outbound_transport`, `outbound_transport` hand-curation). This is the correct behaviour, not an omission. Design table's row 6 (Phase 2 seat logging) is not repeated in the slice table, but PLAN.md's existing `### Phase 2 — Seat logging` heading immediately follows the inserted table in the document structure, so no information is lost.
- **Scope-creep / invented-decisions check**: read every diff hunk; found no code changes, no new assignment IDs invented, no revival of `PLAN_A_MIN_SESSION_MINUTES` as adopted, and no content that doesn't trace to a numbered Decision or an existing already-correct fact (e.g., the final-artifact-replacement order `data/venues.json` then `web/index.html` in the rewritten step 8 simply preserves the pre-existing textual order from before this diff, which the design leaves unspecified — not a new decision).

### Could not verify

- None. Both files under review were read in full for the diffed regions plus targeted greps across the whole file to rule out drift or duplication elsewhere; the design document was read in full (1149 lines); the baseline commit and artifact hash were confirmed directly against the filesystem and `git log`.

### Not asked to check

- Code correctness — this assignment touches no code (`web/ranking.js`, `web/app.js`, `build/refresh.py`, `build/generate.py` are all unchanged per `git diff d99f982 --stat`, verified to include only `PLAN.md` and `CLAUDE.md`).
- Test suite results — no tests were added or run as part of this documentation-only assignment, and none were expected to be.
- Prose style, wording quality, or whether the transcription is maximally concise — only completeness and non-contradiction against the approved design were in scope.
- `DECISIONS.md` — the HANDOFF note defers the `IMP-015` renumbering record to when slice 3 actually opens; not this assignment's scope.

### Status

`GATE_FAIL`

**Reason**: the design's §12 "Tolerance ownership" acceptance-criteria group (design lines 922-927) is not reflected anywhere in `PLAN.md` or `CLAUDE.md`. This is a real, checkable omission under the stated acceptance criterion "every §2-§13 decision in the revision-5 design is reflected in `PLAN.md`/`CLAUDE.md`, without omission" — every other named §12 acceptance-criteria group (Plan A eligibility, result states, `nothing_evaluable` diagnostics, `invalid_request`, requested-vs-achievable end, stale-data invariant, failed-source handling, DOM stub, publication robustness, manual checklist) has a corresponding transcribed bullet in CLAUDE.md's "Review-response coverage" paragraph (`CLAUDE.md:140`) and/or PLAN.md's matching paragraph (`PLAN.md:2800` ff. and the JS test bullet at `PLAN.md:2777`), but this one group has none. Fix: add a "Tolerance ownership" bullet (or fold its three assertions into the existing Plan A eligibility bullet) to both files' test-strategy sections.

---

## Gate invocation 2 — 2026-09-06

- **Gate route**: `claude_sonnet`, effort high
- **Brief source**: generated from HANDOFF.md acceptance criteria + required verification; scoped to invocation 1's single `GATE_FAIL` reason plus a final full re-pass over the design's §12 named groups

### Checks run

- **Fix location, PLAN.md**: `PLAN.md:2801` (inside `## Testing`, `PLAN.md:2718`, within the "Review-response coverage" list beginning at `PLAN.md:2800`) now reads: "**tolerance ownership, as a placement contract, not a behaviour** — `rankVenues()` called with **no** `toleranceMinutes` produces the same tiers as an explicit `15`; **no `FEASIBILITY_TOLERANCE_MINUTES` declaration remains in `app.js`**, asserted against the source itself rather than against behaviour; and an explicit override still takes effect, so test control is retained". This states all three required assertions: the no-explicit-tolerance-matches-15 equivalence, the no-declaration-in-`app.js` claim asserted against source, and the explicit-override-still-works claim.
- **Fix location, CLAUDE.md**: `CLAUDE.md:140` (inside `## Testing`, `CLAUDE.md:135`) is its own "Review-response coverage" bullet, opening: "**Review-response coverage** (2026-09-05, `ARCH-004`): tolerance ownership is a **placement** contract, not a behaviour — `rankVenues()` with no explicit `toleranceMinutes` must match an explicit `15`, **no `FEASIBILITY_TOLERANCE_MINUTES` declaration may remain in `app.js`** (asserted against the source, not against behaviour), and an explicit override must still take effect." Same three assertions present, same source-not-behaviour framing.
- **Cross-check against design source**: `docs/superpowers/specs/2026-09-05-review-response-design.md` lines 918-927 (`## 12. Acceptance criteria`, "Tolerance ownership" group) state exactly these three bullets — `rankVenues()` with no `toleranceMinutes` produces the same tiers as an explicit `15`; no `FEASIBILITY_TOLERANCE_MINUTES` declaration remains in `app.js` (asserted against the source, "placement contract, not a behaviour"); an explicit override still takes effect (test control retained). Both files' new text tracks this substance and framing; no paraphrase drift, no added or dropped assertion.
- **Not a duplicate/contradiction**: `grep -n -i tolerance PLAN.md CLAUDE.md` shows the new bullets are additions alongside the pre-existing `FEASIBILITY_TOLERANCE_MINUTES` architecture-section references (`PLAN.md:328,621,625,654,659,707,713,729,1500,1503,2677,2746,2942,2951`; `CLAUDE.md:35,75,118,139`) — none of those pre-existing lines were altered, and none already stated the ownership-test criterion before this fix (confirmed in invocation 1's exhaustive grep, which found zero hits for `toleranceMinutes`/"tolerance ownership" outside the design doc).
- **Final full pass over design §12** (lines 901-1094, all ten named groups: Plan A eligibility, Tolerance ownership, Result states, `nothing_evaluable` diagnostics, `invalid_request`, Requested vs achievable end, Stale-data invariant, Runtime rendering (DOM stub), Failed-source handling and fallback naming, Publication robustness, Manual checklist additions) re-read in full against `PLAN.md`'s "Review-response coverage" paragraph (`PLAN.md:2800-2805`) plus its adjoining pytest paragraph, and `CLAUDE.md:139-141`. Every group has corresponding transcribed content; Tolerance ownership was the only gap and is now closed. No new gap surfaced.

### Could not verify

- None new. Same evidentiary basis as invocation 1 (both files read in full for the relevant regions, design doc §12 read in full a second time) plus the two new bullets read directly at their cited line numbers.

### Not asked to check

- Everything invocation 1 already listed as out of scope (code correctness, test suite results, prose style, `DECISIONS.md`) remains out of scope here.
- Re-verification of §3-§11 and §13 content beyond the final full-pass sanity check above — invocation 1's line-by-line diff of those sections already passed and nothing in this round's diff touches them (confirmed no other diff hunks exist beyond the tolerance-ownership addition: the only new content in either file's Testing section relative to invocation 1's read is the two quoted bullets above).

### Status

`GATE_PASS`

**Reason**: the sole `GATE_FAIL` defect from invocation 1 — the missing "Tolerance ownership" acceptance-criteria group — is now present, correctly worded, and correctly placed in both `PLAN.md`'s and `CLAUDE.md`'s `## Testing` sections. The final full re-pass over the design's §12 named groups against both files' coverage finds no other omission or contradiction. All other invocation-1 checks (artifact identity, diff scope, §3-§11, §13, scope-creep) are unaffected by this round's diff and stand as passed.

---
