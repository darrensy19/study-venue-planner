# Gate record: IMP-008

## Gate invocation 1 — 2026-09-03

- **Gate route**: `claude_sonnet`, effort high
- **Brief source**: `.cross-agent-workflow/gate_brief.py HANDOFF.md` — generated mechanically from `HANDOFF.md`'s `## Current assignment` block, not from the primary's prose

### Checks run

- **`git status --short` / `git diff` scope**: `M HANDOFF.md`, `M reviews/LEDGER.md`, `M scraper/fetchers.py`, `M tests/python/test_fetchers.py`; new files `scraper/busyness.py`, `scraper/serpapi.py`, `tests/python/test_busyness.py`, and 8 new fixtures under `tests/python/fixtures/serpapi_*.json`. `git rev-parse HEAD` → `be3d3c2aec1b80b865abae1fdae68d845bf9aac7`, matching the brief's stated baseline commit exactly. `git diff --stat -- scraper/ tests/python/ HANDOFF.md reviews/LEDGER.md` shows only the 4 modified files above (204 insertions/19 deletions total) — every new/untracked path falls under `scraper/` or `tests/python/`, both in scope. No `web/`, `build/`, or `BACKLOG.md` path touched. One untracked file, `reviews/IMP-008-gate.md` itself, pre-existed this invocation with content whose `Brief source` line read `HANDOFF.md`'s `## Current assignment` block ... read directly — not the primary's prose" rather than the mechanical `gate_brief.py` script — a non-compliant earlier attempt per the anti-laundering rule. It is overwritten in full by this file and is not counted as part of the artifact under review (it is the gate's own deliverable, not primary output).
- **`HANDOFF.md` diff read in full**: fully replaces the `IMP-007` block (state `completed`) with `IMP-008` (state `draft`); objective, scope exclusions, acceptance criteria, required verification, and baseline commit all match the gate brief verbatim.
- **`reviews/LEDGER.md` diff read in full**: exactly one new row appended — `| \`IMP-008\` | 2026-09-03 | implementation | Phase 1 step 2: SerpApi transport/parser and fetch_busyness(source) |`.
- **`scraper/serpapi.py` read in full**: `SerpApiError(RuntimeError)` carries `status`/`body`, mirroring `PlacesError`'s shape in `scraper/places.py`. `_get()` raises it on a non-200 status or an `"error"` key in the parsed JSON body, redacting the API key from the response text first via `_redact_key`. `search_maps(query_text, api_key)` calls `engine=google_maps&type=search&q=...`; `place_by_data(data_param, api_key)` calls `engine=google_maps&type=place&data=...`. No file write anywhere in the module — only `requests.get` and return.
- **`scraper/busyness.py` read in full**: `BusynessValidationError(ValueError)` is a distinct exception type from `SerpApiError`, raised explicitly rather than guessed through, per its own docstring's contrast with a legitimate empty `{}` result. `extract_histogram(payload)` reads `payload["place_results"]["popular_times"]["graph_results"]` with `or {}` guards at every level of the chain, so a missing key at any level and an explicitly empty dict both collapse to `{}` rather than raising — confirmed against `test_extract_histogram_empty_when_no_popular_times` and `test_extract_histogram_empty_when_no_place_results_at_all`. `_parse_hour` via `TIME_RE` maps `"12 AM" -> 0`, `"12 PM" -> 12`, `"6 AM" -> 6` (`hour % 12` then `+12` for PM) — confirmed against `test_extract_histogram_maps_noon_and_midnight_correctly`. Unparseable times and entries missing `busyness_score` are skipped, not defaulted — confirmed against `test_extract_histogram_skips_unparseable_time_and_missing_score` (a `"not a time"` entry and a scoreless `"7 AM"` entry both dropped, leaving only the one valid bucket). Buckets are sorted by hour. No file write anywhere in the module.
- **`scraper/fetchers.py`'s new `fetch_busyness(source, api_key)` read in full and hand-traced against every acceptance-criteria clause**:
  - **Full source record, never a bare Place ID**: `query_text = f"{source['resolved_name']}, {source['resolved_address']}"` — reads only `resolved_name`/`resolved_address` off the record; `source['place_id']` is never referenced in this function. Confirmed against `test_fetch_busyness_returns_histogram_when_search_collapses_with_data`'s assertion `seen_queries == [("Starbucks Centrepoint, 176 Orchard Rd, Singapore 238843", "fake-api-key")]`, hand-checked against `SOURCE`'s literal `resolved_name`/`resolved_address` values in `tests/python/test_fetchers.py` — byte-for-byte match of the f-string's output.
  - **Collapsed `place_results` shape**: `if place_results:` branch calls `extract_histogram(search)` immediately; a non-empty result returns without ever calling `place_by_data` — confirmed by the test's `fail_place_by_data` monkeypatch that raises `AssertionError` if invoked, and the test passes, proving the retry is genuinely never spent on this path.
  - **`local_results` list shape**: `else:` branch reads `search.get("local_results") or []`; a non-empty list takes `candidate = local_results[0]` (`extract_histogram` is never called on the search response here, since a `local_results` item carries no inline `popular_times` — consistent with `scraper/serpapi.py`'s own module docstring and `DECISIONS.md`). Confirmed against `test_fetch_busyness_follows_local_results_candidate_via_data_param`, which asserts `calls == ["search", "data"]` — the retry is unconditionally spent, matching the code path exactly.
  - **Data-param retry fires only on an empty first response, built from `data_id` + `gps_coordinates`**: when `place_results` is truthy but `extract_histogram(search)` returns `{}` (falsy), `candidate = place_results` (the same object just examined) is reused as the retry-parameter source, rather than re-fetching or discarding it. `data_id = candidate.get("data_id")`; `lat, lng = coords.get("latitude"), coords.get("longitude")` where `coords = candidate.get("gps_coordinates") or {}`. `data_param = f"!4m5!3m4!1s{data_id}!8m2!3d{lat}!4d{lng}"` — matches `DECISIONS.md`'s validated format exactly (`!4m5!3m4!1s{data_id}!8m2!3d{lat}!4d{lng}`). Confirmed against `test_fetch_busyness_retries_via_data_param_after_empty_first_response`'s `assert data_param.startswith("!4m5!3m4!1s")` and `calls == ["search", "data"]` — the retry is skipped when the first response has data (previous bullet) and fired exactly when it doesn't.
  - **Confirmed-absent only after both routes return empty**: when `extract_histogram(retry)` (the second call) also returns `{}`, `fetch_busyness` returns `{}` directly — no third call, no exception. Confirmed against `test_fetch_busyness_confirmed_absent_only_after_both_routes_empty`, which uses the real trimmed `serpapi_search_collapsed_confirmed_absent`/`serpapi_data_retry_confirmed_absent` fixtures (the actual SingHealth Tower payloads named in `DECISIONS.md`'s "Popular Times coverage, take two" as the two genuine absences) and asserts `calls == ["search", "data"]` and `result == {}`.
  - **Malformed-candidate failure**: `if not data_id or lat is None or lng is None:` raises `BusynessValidationError` before building `data_param` — uses `is None` rather than a falsy check for `lat`/`lng`, correctly distinguishing an absent coordinate from a legitimate `0.0` (moot for Singapore's ~1.3°N venues, but not a latent bug). Confirmed against `test_fetch_busyness_raises_on_candidate_missing_data_id_or_coordinates` using `serpapi_local_results_missing_fields_synthetic` (a candidate with no `data_id` key at all), matched via `pytest.raises(BusynessValidationError, match="missing data_id")`, and the `place_by_data` monkeypatch is set to raise `AssertionError("unreachable")` if called — the test passes, proving `place_by_data` is genuinely never reached.
  - **No-search-match failure**: an empty `place_results` and an empty/missing `local_results` (`serpapi_no_match_synthetic`, which carries neither key) raises `BusynessValidationError` naming `source['venue_id']`, before any retry attempt — confirmed against `test_fetch_busyness_raises_on_no_search_match`, `match="no search match"`, `place_by_data` again wired to raise `AssertionError` if reached.
  - **Transport failure propagation**: `search_maps` is the first call in the function body with no `try`/`except` anywhere in `fetch_busyness` or in `scraper/busyness.py`'s `extract_histogram` — a `SerpApiError` raised by `search_maps` (or, by the same code shape, by `place_by_data`) propagates uncaught to the caller. Confirmed against `test_fetch_busyness_propagates_transport_failure_and_writes_nothing`: `search_maps` monkeypatched to raise `SerpApiError("SerpApi returned 500", status=500, body="server error")`, asserted via `pytest.raises(SerpApiError)`, and `tmp_path`'s directory listing before/after is asserted equal (via `monkeypatch.chdir(tmp_path)`) — genuine no-file-write proof, the same pattern used in `test_fetch_hours_propagates_transport_failure_and_writes_nothing`.
  - **No file writes**: `scraper/fetchers.py`, `scraper/busyness.py`, and `scraper/serpapi.py` read in full — no `open()`, no `Path.write_*`, no `json.dump` anywhere in any of the three files under review.
- **`.venv/bin/pytest tests/python/ -v`**: **62 passed, 0 failed** (full list read). Baseline before this diff (per `IMP-007-gate.md`) was 50; this diff adds exactly 12 new tests — 5 in `test_busyness.py` (collapsed-with-data, empty, no-place-results, skip-unparseable/missing-score, noon/midnight mapping) and 7 in `test_fetchers.py` (search-collapse-with-data, data-param-retry-succeeds, confirmed-absent-after-both-empty, local-results-candidate-via-retry, no-search-match, malformed-candidate, transport-failure-propagation) — 50 + 12 = 62, matching exactly. All pre-existing `test_hours.py` (30), `test_venue_sources.py` (13), `test_bootstrap_venue_sources.py` (3) tests present and passing, confirming no regression outside the diff's own files.
- **`tests/python/test_fetchers.py` diff read in full**: purely additive — the two changed lines are only the module docstring update (mentioning `fetch_busyness` and IMP-008) and new imports (`BusynessValidationError`, `SerpApiError`); every pre-existing `fetch_hours` test body is byte-identical, followed by 7 new test functions appended at the end, each read in full above.
- **Every fixture read in full** (8 files): `serpapi_search_collapsed_with_data.json` (real trimmed Starbucks Centrepoint payload with a populated `graph_results` for Monday/Tuesday, matching the histogram literal asserted in the first test), `serpapi_search_collapsed_empty.json` and `serpapi_data_retry_found.json` (the real Fusionopolis pair from `DECISIONS.md`'s retracted-design-3 example — first response empty, retry finds data), `serpapi_search_collapsed_confirmed_absent.json` and `serpapi_data_retry_confirmed_absent.json` (the real SingHealth Tower pair — both routes empty, the one genuine confirmed-absent case in `DECISIONS.md`), and the three files explicitly labelled `_synthetic` in their own JSON body (`serpapi_local_results_synthetic`, `serpapi_no_match_synthetic`, `serpapi_local_results_missing_fields_synthetic`) each carrying a `"_synthetic"` key explaining why no real Phase 0 payload exercises that branch — matching the module docstring's own claim in `tests/python/test_busyness.py` and `scraper/serpapi.py`.
- **`node --test tests/js/*.test.js`**: `# tests 142`, `# pass 142`, `# fail 0`, `# cancelled 0`, `# skipped 0` — no regression, expected since this diff touches no `web/` file.
- **`DECISIONS.md`'s "Popular Times coverage, take two" read in full** (the section named in `HANDOFF.md`'s route trigger): confirms three retracted designs (bare `place_id` lookup; always-retry via a wrong parameter name, `data_cid`; trusting an empty first response as confirmed absence) and the validated design — search once, retry via `data` built from `data_id` + `gps_coordinates` only on an empty first response, absence confirmed only after both calls return empty. The diff's `fetch_busyness` implements exactly this sequence, and its retry-fixture pairs (Fusionopolis, SingHealth Tower) are the same venues named in that history.
- **Scope exclusions respected**: `git diff --stat` (above) confirms no `build/refresh.py`, `web/ranking.js`, coarsening, frontend/generator, or `BACKLOG.md` path touched. `grep -rn "phase0_busyness" scraper/ tests/python/ HANDOFF.md` finds no import or edit of `build/phase0_busyness.py` — the only hit is `HANDOFF.md`'s own scope-exclusion sentence naming the file, not a reference to it in code.

### Could not verify

- Whether the actual model/effort that produced this diff matches `HANDOFF.md`'s claimed `claude_sonnet`, effort high — not observable from this subagent, as with every prior gate.
- Whether the fixture payloads are faithful trims of the real SerpApi responses they claim to be (vs. the three explicitly `_synthetic` ones, which are self-labelled and not at issue) — no live SerpApi access in this gate, consistent with the project's no-live-network testing policy; taken on the fixtures' own docstring claim in `tests/python/test_busyness.py` that they are "small trimmed real Phase 0 SerpApi responses."

### Not asked to check

- Whether the retry design itself (search-then-data-param, absence confirmed only after both) is the objectively best approach versus an alternative — the acceptance criteria specify this exact design, citing `DECISIONS.md`, and the diff implements exactly that; no broader design judgment was requested.
- `build/refresh.py` orchestration (step 7), the return-validator bridge (step 3), `ranking.js` entry point (step 4), coarsening (step 5), frontend/generator (step 6), any live API call, or the design/quality of frozen `build/phase0_busyness.py` — all explicit scope exclusions in the brief, not examined beyond confirming no import or edit occurred.
- Prose quality, formatting, or style beyond what the acceptance criteria explicitly require.

### Status

`GATE_PASS` — `scraper/serpapi.py`, `scraper/busyness.py`, and `fetch_busyness` in `scraper/fetchers.py` match every acceptance-criteria clause, each hand-traced against the actual code and confirmed with fixture-based tests: a full source record is accepted and only `resolved_name`/`resolved_address` are read for the search query; the search-collapse (`place_results`) and `local_results` shapes are both handled; the `data`-parameter retry is built from `data_id` + `gps_coordinates` and fires only when the first response's extracted histogram is empty, never spent when the first response already carries data; absence is returned as confirmed (`{}`) only after both the search and the retry come back empty, matching the two real venues named in `DECISIONS.md`'s "Popular Times coverage, take two"; a malformed candidate (missing `data_id`/coordinates) and a no-match search both raise `BusynessValidationError` rather than guessing, traced to reach the caller uncaught; a `SerpApiError` transport failure propagates uncaught and is confirmed (via a `tmp_path`/`chdir` before/after directory check) to write no file; none of `scraper/fetchers.py`, `scraper/busyness.py`, or `scraper/serpapi.py` performs any file write. `.venv/bin/pytest tests/python/` passes 62/62 (50 pre-existing + 12 new, all read and independently confirmed to construct the specific case each claims), and `node --test tests/js/*.test.js` remains 142/142 with no regression. `git diff --stat` confines the change to `scraper/`, `tests/python/`, `HANDOFF.md`, and `reviews/LEDGER.md`, `HEAD` matches the brief's stated baseline commit exactly, and no scope-excluded path (`build/`, `web/ranking.js`, `BACKLOG.md`) or frozen `build/phase0_busyness.py` was touched or imported. A pre-existing, non-compliant `reviews/IMP-008-gate.md` (sourced from `HANDOFF.md`'s prose rather than the mechanical `gate_brief.py` script) was found at the start of this invocation and is superseded in full by this record.

---
