# Gate record: IMP-009

## Gate invocation 1 — 2026-09-03

- **Gate route**: `claude_sonnet`, effort high
- **Brief source**: `.cross-agent-workflow/gate_brief.py HANDOFF.md` — generated mechanically from `HANDOFF.md`'s `## Current assignment` block, not from the primary's prose

### Checks run

- **`git rev-parse HEAD`**: `0d5712bca2784eb43351fc6333d63ddb1ed4eb60`, matching the brief's stated baseline commit `0d5712b` exactly.
- **`git status --short`**: `M HANDOFF.md`, `M reviews/LEDGER.md`; untracked `build/return_validator_bridge.py`, `build/validate_return_transport.mjs`, `tests/python/test_return_validator_bridge.py`. `git diff --stat` confirms only `HANDOFF.md` (30 lines changed) and `reviews/LEDGER.md` (1 line added) among tracked files. Every path — modified and untracked — falls exactly under the brief's named scope: the new bridge script, the Python wrapper, `tests/python/`, `HANDOFF.md`, `reviews/LEDGER.md`. No `build/refresh.py`, `web/ranking.js`, coarsening, or frontend/generator path touched. `git diff -- web/ranking.js` is empty, confirming `validateReturnTransport()` and its existing `tests/js` coverage are genuinely unmodified.
- **`HANDOFF.md` diff read in full**: fully replaces the `IMP-008` block (state `completed`) with `IMP-009` (state `draft`); objective, scope exclusions, acceptance criteria, required verification, and baseline commit all match the mechanically generated gate brief verbatim, byte-for-byte.
- **`reviews/LEDGER.md` diff read in full**: exactly one new row appended — `| \`IMP-009\` | 2026-09-03 | implementation | Phase 1 step 3: Node return-validator bridge (validateReturnTransport export + subprocess.run bridge) |`.
- **`build/validate_return_transport.mjs` read in full (30 lines)**: imports `validateReturnTransport` from `../web/ranking.js` (line 12) — the real export, confirmed present unmodified at `web/ranking.js:577` (`git diff -- web/ranking.js` empty). Reads `process.argv[2]` as the metadata path; on the success path (lines 20-30) the only stdout write is `process.stdout.write(JSON.stringify(status))` at line 29 — no other `stdout.write` call exists anywhere in the file, so on success it writes structured JSON and nothing else. Error paths (missing argv, unreadable/unparseable file) write to `stderr` only and exit nonzero (2 or 1) — they never touch `stdout`, keeping the two channels genuinely separate. No `writeFileSync`, `fs.write`, or any file-write call anywhere in the file (grepped).
- **`build/return_validator_bridge.py` read in full (73 lines)**: `subprocess.run([node_path, str(script_path), str(meta_path)], capture_output=True, text=True, timeout=30)` — the metadata path is passed as a list element; no `shell=True` and no constructed shell string anywhere in the file (grepped for `shell=True` and every `subprocess.*` call — the one call at line 46 is the only invocation). `BridgeError` is raised for exactly four conditions, each traced: `FileNotFoundError` on a missing Node executable (lines 52-53), `result.returncode != 0` (lines 57-58), `json.JSONDecodeError` on non-JSON stdout (lines 62-63), and `missing = expected_venue_ids - status.keys()` non-empty (lines 68-70) — this last check independently re-derives the expected venue-id set from the metadata file itself (line 41) rather than trusting the subprocess, so a per-venue status omission is caught even if the Node script's own coverage were ever wrong. A per-venue `invalid` entry inside a well-formed status dict is returned normally (line 72) — never raises. No file-write call anywhere in the file (grepped).
- **`tests/python/test_return_validator_bridge.py` read in full (114 lines), each of the 9 tests hand-traced**:
  - `test_round_trip_against_live_meta_stamps_every_venue`: calls `validate_return_transport(REAL_META_PATH)` with no overrides — genuinely exercises the real Node binary and the real `data/venues_meta.json` (28 venues per `CLAUDE.md`), asserting the returned key set equals the file's own venue-id set and every entry is `{"state": "ok"}` (no `return_transport` populated in the live file yet, consistent with `PLAN.md`'s current state). A genuine round-trip, not a stub.
  - `test_per_venue_invalid_is_a_result_not_an_exception`: constructs a venue with `last_departure_band: "23:5-00:00"`. Traced against `normaliseBand` (`web/ranking.js:129-143`): `"23:5-00:00".split("-")` yields exactly 2 parts, but `normaliseEdge("23:5")` fails to parse (missing leading zero on minutes) and returns `null`, so `normaliseBand` returns `{kind:"malformed", ...}` — a genuine malformed band, not a vacuous fixture. The call returns (no exception) with `status["broken"]["state"] == "invalid"` and `status["fine"] == {"state": "ok"}` — confirms the per-venue case is a result, distinguishable from the "fine" venue in the same call.
  - `test_meta_path_with_spaces_and_shell_metacharacters_is_handled_safely`: creates a directory literally named `"odd dir; echo pwned && $(whoami)"` and passes its path through. Since `subprocess.run` receives a list (no shell), the OS treats the whole string as one path component — this is a genuine behavioral proof that no shell interpretation occurs, not merely a static grep for `shell=True`. Passes.
  - `test_never_writes_any_file`: snapshots `tmp_path` directory listing before and after the call; asserts equality. Genuine no-file-write proof.
  - `test_node_missing_raises_bridge_error`: passes `node_path="definitely-not-a-real-node-binary"`, a nonexistent binary name. `subprocess.run` raises `FileNotFoundError` when the executable cannot be found, caught and reraised as `BridgeError` matching `"node executable not found"`. Genuinely exercises the missing-Node path, not a mock.
  - `test_nonzero_exit_raises_bridge_error`: writes a real stub `.mjs` file whose body is `process.stderr.write("boom"); process.exit(1);` and passes it as `script_path` (default `node_path="node"`, so the real Node binary executes the real stub). Confirmed the stub genuinely exits 1 — `pytest.raises(BridgeError, match="bridge exited 1")` passes, and the code path taken is `result.returncode != 0` at line 57.
  - `test_non_json_stdout_raises_bridge_error`: stub body `process.stdout.write("not json");` — exits 0 but stdout is non-JSON text. Matches `"not valid JSON"`; genuinely exercises `json.loads` raising `JSONDecodeError` at line 62.
  - `test_stdout_not_a_json_object_raises_bridge_error`: stub writes `"[1, 2, 3]"` — valid JSON but a list, not an object. Exercises the `isinstance(status, dict)` guard at line 65-66. Not one of the four acceptance-criteria-named broken-bridge conditions verbatim, but a defensible extra robustness case covered by the same defensive code; does not weaken coverage of the four named conditions, which each have their own dedicated test.
  - `test_missing_status_for_a_venue_raises_bridge_error`: meta has venues `v1`, `v2`; stub stdout supplies status only for `v1`. Matches `"missing status for.*v2"` — genuinely exercises the `missing = expected_venue_ids - status.keys()` branch with a real, non-empty difference.
- **`.venv/bin/pytest tests/python/ -v`**: **71 passed, 0 failed** (full list read). Per `IMP-008-gate.md`, the pre-`IMP-009` count was 62; this diff adds exactly the 9 tests in `test_return_validator_bridge.py` enumerated above — 62 + 9 = 71, matching exactly and matching the brief's "62 pre-existing + new tests" framing. All pre-existing `test_hours.py` (30), `test_venue_sources.py` (13), `test_bootstrap_venue_sources.py` (3), `test_busyness.py` (5), `test_fetchers.py` (11) tests present and passing, confirming no regression outside the diff's own file.
- **`node --test tests/js/*.test.js`**: `# tests 142`, `# pass 142`, `# fail 0`, `# cancelled 0`, `# skipped 0` — matches the brief's stated 142 exactly, no regression, consistent with the diff touching no `web/` file.
- **Scope exclusions respected**: confirmed above via `git status`/`git diff --stat` — no `build/refresh.py`, `web/ranking.js`, coarsening, or frontend/generator path touched; `git diff -- web/ranking.js` is empty, confirming `validateReturnTransport()`'s own logic and its existing `tests/js` coverage are untouched.

### Could not verify

- Whether the actual model/effort that produced this diff matches `HANDOFF.md`'s claimed `claude_sonnet`, effort high — not observable from this subagent, as with every prior gate (`Runtime model verification` constant, `WORKFLOW.md`).

### Not asked to check

- `build/refresh.py` orchestration wiring (step 7, not yet built), the `ranking.js` pipeline entry point (step 4), the coarsening stage (step 5), frontend/generator (step 6), or any change to `validateReturnTransport()`'s own logic — all explicit scope exclusions in the brief, confirmed not touched but not otherwise examined.
- Whether the bridge design itself (a Node subprocess invoked from Python, rather than a JS reimplementation of `validateReturnTransport` in Python) is the objectively best architecture — the acceptance criteria specify this exact design (`PLAN.md`, "The return-validator bridge"), citing the single-implementation requirement, and the diff implements exactly that; no broader design judgment was requested.
- Prose quality, formatting, or style beyond what the acceptance criteria explicitly require.

### Status

`GATE_PASS` — `build/validate_return_transport.mjs` writes structured JSON and nothing else to stdout on success (traced: the only `stdout.write` call in the file is on the success path; all error paths write to `stderr` and exit nonzero), reads the metadata path from `process.argv[2]`, and imports and calls the real, unmodified `validateReturnTransport()` export from `web/ranking.js` (`git diff -- web/ranking.js` is empty). `build/return_validator_bridge.py`'s `subprocess.run` call passes the metadata path as a list element, never `shell=True` or a constructed shell string — confirmed both by grep and by a genuine behavioral test using a path containing shell metacharacters. A per-venue `invalid` status is returned as ordinary data, confirmed against a genuinely malformed `last_departure_band` hand-traced through `normaliseBand`. Each of the four broken-bridge conditions named in the acceptance criteria (Node missing, nonzero exit, non-JSON stdout, a venue omitted from the output) raises `BridgeError` and is exercised by a dedicated test that genuinely constructs that condition (a nonexistent binary name; a stub that actually exits 1; a stub that actually writes non-JSON; a stub that actually omits one venue's status) — none of the nine tests in `tests/python/test_return_validator_bridge.py` merely asserts its name. Neither new implementation file writes any file (grepped and behaviorally confirmed via a directory-snapshot test). `.venv/bin/pytest tests/python/` passes 71/71 (62 pre-existing + 9 new, matching exactly), and `node --test tests/js/*.test.js` remains 142/142 with no regression. `git diff --stat` confines the change to the new bridge script, the Python wrapper, `tests/python/`, `HANDOFF.md`, and `reviews/LEDGER.md`; `HEAD` matches the brief's stated baseline commit exactly; no scope-excluded path was touched.

---
