# Review record: IMP-014

## Assignment

- **Assignment ID**: `IMP-014`
- **Work type**: `implementation`
- **Primary route**: `claude_sonnet` — Sonnet, effort high
- **Reviewer route**: `codex_terra` — Terra, effort medium
- **Baseline commit**: `01bd0f4`
- **Reviewed diff or commit**: committed range `01bd0f4..2ef1eb3` — `DECISIONS.md`, `HANDOFF.md`, `data/venues.json`, `reviews/IMP-014-gate.md`, `reviews/LEDGER.md`, and `web/index.html`
- **Scope**: Phase 1 step 8: first live refresh and manual acceptance. Includes the generated live data/artifact, the two prescribed ranking scenarios, and generated-artifact assertions. Excludes another billable refresh, hand-curated return-transport data, and the outbound-mirror architecture.
- **Finding disposition schema**: `factual-assessment/v1`

## Review round 1 — 2026-09-03

- **Lifecycle state reviewed**: `review_requested`
- **Reviewer route**: `codex_terra`
- **Runtime model verification**: the runtime does not expose the selected model; the user-selected Terra-medium route is relied on.
- **Reviewed artifact**: committed range `01bd0f4..2ef1eb3`, with `data/venues.json` and `web/index.html` as the generated live-refresh artifacts.

### Resolution of prior findings

None — first review round.

### Findings

No findings.

### Verification performed

- Read `HANDOFF.md` in full first, then `reviews/IMP-014-gate.md`, the Phase 1 step-8 contract in `PLAN.md`, and the load-bearing `CLAUDE.md` “Getting home” non-negotiables.
- Inspected `git log --oneline 01bd0f4..2ef1eb3`, the full declared diff, and diff-scoped assignment records. The worktree was clean; the diff contains the two stated generated artifacts and the stated assignment records only. `git diff --check 01bd0f4..2ef1eb3` passed.
- Parsed the committed embedded data independently: 28 unique venues, all `OPERATIONAL`, and all 28 stamped `hours.status: ok`, `histogram.status: ok`, and `return_transport_status.state: ok`.
- Ran `.venv/bin/pytest tests/python/ -q` — 188 passed, 0 failed.
- Ran `node --test tests/js/*.test.js` — 184 passed, 0 failed.
- Re-ran both required scenarios through `rankVenues()` against the JSON embedded in the committed `web/index.html`:
  - Scenario A (`origin_b`, transit, 2026-09-03, leave 09:00, 5h) yields Plan A `starbucks-chinatown-food-street`, `robust`, `returnBasis: core_span`, plus the strong Plan B `starbucks-singhealth-tower`.
  - Scenario B (`origin_b`, walk, 2026-09-03, leave 23:00, 5h) yields no Plan A and the exact verified-return refusal time `04:07`.
- Independently checked the generated artifact: exactly three application-JSON blocks parse; no external script, stylesheet, unresolved local import, `fetch()`, `localStorage`, raw `</script>` in embedded JSON, or absolute asset path; exactly one module block.

### Could not verify

- The real-iPhone Safari result first-hand, because this reviewer does not have the physical device. The documented local-HTTP-server Safari check in `HANDOFF.md` closes the prior Chromium-substitution gap; its claimed device result was not treated as a Chromium equivalent.
- The live Google Places and SerpApi network behavior first-hand, because repeating it would spend another API call. The committed live output, timestamps, and status fields were inspected instead.

### User decisions required

None.

### Recommendation

`APPROVE`

```text
Run as: Claude Sonnet, effort high   (/model sonnet; effort via --effort or settings.json)
Why: IMP-014 round-1 reviewer recommendation is APPROVE with no findings; reconcile the primary-owned lifecycle record only.
Conversation: CONTINUE — same primary through closure of this assignment.
Action: Reconcile HANDOFF.md to review_complete
Goal: Phase 1 step 8 (`PLAN.md`'s "Phase 1 implementation order" item 8, lines 2281-2305) — run `make refresh` live for the first time and complete manual acceptance

Read `reviews/IMP-014.md` and reconcile `HANDOFF.md` to `review_complete` from its `APPROVE` recommendation. Do not edit assignment artifacts, commit, push, or close the assignment. Then stop and state that the user’s decision is required.
```
