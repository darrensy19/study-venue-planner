# Gate record: IMP-019

## Gate invocation 1 — 2026-09-06

- **Gate route**: `claude_sonnet`, effort high
- **Brief source**: generated mechanically from `HANDOFF.md` via `.cross-agent-workflow/gate_brief.py HANDOFF.md`

### Checks run

- **Diff scope vs brief**: `git diff --stat -- web/ranking.js web/app.js tests/js/ranking.test.js tests/js/app.test.js` matches the "Artifact under review" list exactly (251/+33−/220/+15− lines respectively). `git status` also shows `BACKLOG.md` (pre-existing, unrelated BL-003 closure note — ignored per instructions), `HANDOFF.md`, `reviews/LEDGER.md` (one new `IMP-019` row, expected), and `web/index.html` (regenerated build output, not hand-edited — consistent with `make generate` having been run). Untracked `.claude/` ignored (tooling, not repo content).
- **Baseline**: confirmed `HEAD` == `ff00831` (`git rev-parse HEAD` and `git rev-parse ff00831` both `ff00831f34624d8c94052e8a62c2c2ae92357785`). All four artifact files are working-tree diffs against this exact baseline, not a separate commit.
- **PLAN.md read**: "Plan A eligibility" (~1791), "Result states" (~1813, including the ordering table and the diagnostic-total `nothing_evaluable` table), "Evidence freshness and a first-ever failed source" (~1866, failed-source half only), "One entry point, pure, whole-dataset" (~1874, including the ranked/unranked taxonomy table), the "Invalid requests" / "One ranking-owned control contract" paragraphs (~1841–1843), and the slice table (~2669–2683). **Slice-table cross-check**: 1a is explicitly "Result-state machine, Plan A eligibility, control-contract export + validation, tolerance ownership, failed-source diagnosis, naming on every removed/travelUnknown entry" against `web/ranking.js`; 1b is explicitly "Presentation shape: achievable end, binding limit, latest-leave state, candidate freshness, ... bestAlternative" — confirms the assignment's own scope claim rather than trusting it.
- **Full diff read**: `web/ranking.js` (251 lines), `web/app.js` (33 lines), `tests/js/ranking.test.js` (220 lines, ~20 new tests + 3 rewritten fixtures), `tests/js/app.test.js` (15 lines, 1 new source-text test) — all read in full.
- **Required verification, independently run**:
  - `.venv/bin/pytest tests/python/ -q` → **190 passed**, 0 failed.
  - `node --test tests/js/*.test.js` → **203 passed**, 0 failed, 0 cancelled/skipped.
- **Non-vacuity spot-checks** (each reverted, tests rerun to confirm failure, then restored and re-verified byte-identical via `diff` against the originally-captured patch, then full suite rerun green):
  1. Plan A eligibility floor: `groups.ranked.find((c) => meetsConfidenceFloor(...))` → `groups.ranked[0] ?? groups.shorter[0] ?? null` — **2 tests failed** as expected. Restored; confirmed identical to original diff; suite green.
  2. Control validation: `validateControls(controls)` result forced to `null` — **2 tests failed** as expected (`invalid_request` tests). Restored; confirmed identical; suite green.
  3. `resolveHours` totality: removed both early `if (!hours) {...}` / `if (!hours.regular_hours) {...}` returns, restoring the old throwing behaviour — the two `resolveHours: totality...` tests failed with **thrown `TypeError`s** ("Cannot read properties of undefined (reading 'current_hours_valid_from')" / "...'mon')"), exactly as expected, no other test affected (197/199 passed vs 199/199). Restored; confirmed identical; suite green.
  - **Recovery note**: during step 1's restore, `git checkout -- web/ranking.js` was used and — because there was no commit boundary, only a working-tree diff against `HEAD` — this discarded the entire uncommitted diff back to baseline `ff00831`, not just the one-line revert. Recovered immediately via `git apply` of the diff captured at the start of this gate (before any reverts), then verified byte-for-byat identical via `diff` against that same captured patch. All subsequent reverts used direct Python string-replacement + `diff`-verified restores instead of `git checkout`. Final verification (below) re-confirms all four files are still byte-identical to the patches captured at gate start.
- **`CONTROL_CONTRACT` deep-freeze**: read `deepFreeze()` and the `CONTROL_CONTRACT` definition. Direct test via `node --input-type=module -e '...'` importing `web/ranking.js`: `origins.push(...)`, `duration.min = 0`, adding a new top-level field, `modes.push(...)`, and `leaveAt.max = 0` **all threw `TypeError`**, and `Object.isFrozen()` returned `true` at every level (top-level, `duration`, `origins`, `leaveAt`). (First attempt via plain `node -e` without `--input-type=module` gave false negatives on plain-assignment mutations — that script itself runs as non-strict CommonJS, where assigning to a frozen object's property silently no-ops rather than throwing; `Array.prototype.push` always throws regardless of caller strictness, which is why the array mutations "passed" even in that broken first attempt. Re-ran under a genuine strict-mode ES module context, where all five mutation attempts throw. This is a genuine confirmation, not an artifact of the test harness.)
- **`resultState` totality/disjointness**: read the six-outcome chain — the early `invalid_request` return, then `if (planA) / else if (ranked.length>0) / else if (shorter.length>0) / else if (unverified.length>0) / else` producing `plan_a` / `no_low_risk_option` / `session_does_not_fit` / `no_verified_return` / `nothing_evaluable`. Matches `PLAN.md`'s ordering table exactly. Manually re-derived arrival/close arithmetic for two of the rewritten fixtures ("upper-bound NONE ranked..." and "robust beats tight..."): `access` band `"0-380m"` → mid=190 via `normaliseTravelBand` (`floor((0+380)/2)`), upper=380; with `leaveAtMinutes=900`, `durationMinutes=180`, close at `1270`: mid arrival 1090 (inside), `requiredEndMid` 1090+180=1270 == close (inclusive boundary, exact 0 surplus per `PLAN.md`); upper arrival 1280 ≥ 1270 (`NONE`) — matches both fixtures' inline comments exactly. No discrepancy found between a rewritten fixture's comment and its actual numbers.
- **Scope discipline**: grepped the whole `web/ranking.js` diff for `hoursStatus`, `histogramStatus`, `achievable_session_end`/`achievableSessionEnd`, `binding_limit`, `bestAlternative`, `resolved_name` — none present. `web/app.js`'s diff is exactly the `FEASIBILITY_TOLERANCE_MINUTES` removal plus the `resultState`-keyed `REFUSAL_TEXT` map replacing the old `refusals.*` booleans — no wording/vocabulary/hierarchy redesign beyond that stopgap; `ORIGINS`/`MODES` arrays in `app.js` remain untouched (not wired to `CONTROL_CONTRACT` — correctly out of scope, see Not asked to check).
- **`make generate`, network blocked, twice**: `https_proxy=http://127.0.0.1:1 http_proxy=http://127.0.0.1:1 .venv/bin/python3 -m build.generate`, run twice. Both exit 0, both wrote `web/index.html`, and the two outputs are **byte-identical** (`cmp`). Confirmed `web/ranking.js`'s diff was unaffected by this (`diff` against the captured patch, still identical).
- **Naming fallback**: every `removed.push`/`travelUnknown.push` call site (8 total, via `grep`) carries `name`, sourced once per loop iteration as `const name = venue.name ?? venue.id;` — confirmed by reading the surrounding code, not just the call sites.
- **Tolerance ownership**: `app.js` no longer declares `FEASIBILITY_TOLERANCE_MINUTES` (removed the `const` and its two use sites); `rankVenues()`'s destructuring default is `toleranceMinutes = FEASIBILITY_TOLERANCE_MINUTES` (module-level constant `15` in `ranking.js`). New source-text test in `tests/js/app.test.js` asserts against the source itself, matching `CLAUDE.md`'s "Review-response coverage" requirement that this is a placement contract, not merely a behavioural one.

### Could not verify

- None — every required verification item and every non-vacuity spot-check named in the brief ran to completion with a definite result.

### Not asked to check

- `app.js`'s `ORIGINS`/`MODES`/duration-bound form arrays are not wired to read `CONTROL_CONTRACT` — they remain independently declared, exactly as before this diff. `PLAN.md`'s "One ranking-owned control contract" section describes this wiring as the eventual state ("`app.js` builds every control from it"), but the brief's acceptance criteria for `IMP-019` only require `CONTROL_CONTRACT` to exist, be frozen, and be the sole source `rankVenues()`'s own validation reads — not that `app.js` consume it yet. `HANDOFF.md`'s scope exclusions confirm this is deliberately deferred (no `app.js` redesign beyond the `resultState` stopgap). Flagging this as a live drift risk for whoever picks up that wiring, not as a defect in this assignment.
- No review of `web/index.html`'s regenerated content beyond the byte-identical double-run check — not named in the brief's required verification beyond that, and `web/index.html` is generated build output, not part of the "Artifact under review" list.
- No assessment of whether Slice 1a's public-shape migration (`resultState` replacing `refusals`) is itself the right design — that was `ARCH-004`'s job, already approved; this gate only checks the implementation against the approved contract.
- Did not independently re-derive every one of the ~20 new tests' fixture arithmetic — spot-checked the two flagged as reusing a distinctive wide travel band (`"0-380m"`) since those carry the highest risk of a comment/number mismatch per the assignment's own flagged risk area; the remainder were read for logical correctness (correct venue shape, correct assertion against the stated acceptance criterion) but not hand-recomputed minute-by-minute.

### Status

`GATE_PASS`

---
