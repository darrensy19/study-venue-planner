# Gate record: IMP-007

## Gate invocation 1 — 2026-09-03

- **Gate route**: `claude_sonnet`, effort high
- **Brief source**: `.cross-agent-workflow/gate_brief.py HANDOFF.md` — generated mechanically from `HANDOFF.md`'s `## Current assignment` block, not from the primary's prose

### Checks run

- **`git status --porcelain` / `git diff` scope**: exactly `M HANDOFF.md`, `M reviews/LEDGER.md`, `M scraper/hours.py`, `M tests/python/test_hours.py` — no other path touched. Matches the brief's named scope exactly (`BACKLOG.md` is explicitly deferred to assignment close per the brief; its absence here is correct, not a gap). `git diff -- HANDOFF.md reviews/LEDGER.md` read in full: `HANDOFF.md`'s assignment block fully replaced `IMP-006` (state `completed`) → `IMP-007` (state `draft`), all fields consistent with the gate brief (objective, scope exclusions, acceptance criteria, required verification, baseline commit `92490855...`); `LEDGER.md` gained exactly one new row.
- **`scraper/hours.py`'s `_decompose_current_period` read in full and traced against the acceptance criteria**:
  - The loop now computes `entry_open`/`entry_close` before deciding whether to emit, and `continue`s when `entry_close == entry_open` — exact equality, not `<=` or `>=`.
  - Traced why this can only fire as intended: on the anchor day (`offset == 0`), `entry_open = open_minutes` (< 1440) and `entry_close = real_close_abs = span_days*1440 + close_minutes`; for `span_days >= 1` this is always `>= 1440 > open_minutes`, so the skip can never trigger on the anchor day (the `span_days == 0` anchor-day zero-duration case is already caught earlier by the pre-existing `HoursValidationError("zero-duration period...")` check, unmodified by this diff). On every non-anchor day (`offset > 0`), `entry_open = 0`, so the skip fires exactly when `entry_close == 0`, i.e. exactly when the close lands on that date's midnight boundary — the literal "reaches but does not touch" case named in the acceptance criteria.
  - **Close-before-open malformed shape is not swallowed**: traced a same-day period with `close_minutes < open_minutes` (e.g. open 10:00, close 08:20, `span_days == 0`). The pre-existing zero-duration guard only raises on `close_minutes == open_minutes`, so `close_minutes < open_minutes` reaches the loop. There `entry_open = open_minutes`, `entry_close = real_close_abs = close_minutes` (since `span_days == 0`), and `entry_close != entry_open` (500 != 600 in the example), so the skip condition does **not** fire — the malformed `{open: 600, close: 500}` entry is still emitted, not dropped. This confirms the skip is scoped to exact-zero-length only, not to any close-before-open shape.
  - `continues_beyond_window` propagation is unchanged: still set inside the `if close_truncated:` block, after the skip `continue`, on every entry that is actually appended — a skipped (never-appended) entry cannot carry it, and no other entry's propagation logic was touched.
  - No other function in `scraper/hours.py` was touched by the diff (confirmed via `git diff` — the only hunk is inside `_decompose_current_period`'s loop body).
- **`.venv/bin/pytest tests/python/ -v`**: 50 passed, 0 failed. Full list read. `test_bootstrap_venue_sources.py` (3), `test_fetchers.py` (4), `test_venue_sources.py` (13) present and passing, unrelated to this diff. `test_hours.py` has 30 test functions (`grep -c '^def test_' tests/python/test_hours.py` → 30), all passing.
- **Existing tests genuinely unmodified**: `git diff --stat -- tests/python/test_hours.py` → `66 insertions(+)`, **zero deletions** — confirms every pre-existing test function is byte-identical; the diff is purely additive (3 new test functions appended at end of file, read in full in the diff hunk).
- **The three new tests read in full and confirmed to construct what they claim**:
  - `test_current_period_closing_exactly_at_midnight_emits_no_entry_on_next_date`: replaces the `08-29` `currentOpeningHours` period with `open: 08-29 07:30 (day 6), close: 08-30 00:00 (day 0)` — the exact reproduction named in the brief (`day_gap == 1`, close exactly at midnight). Asserts `2026-08-29`'s periods == `[{open: 450, close: 1440, always_open: false}]` (one entry, anchored, real distance to close) and `2026-08-30`'s periods == `[{open: 480, close: 1320, always_open: false}]` — i.e. exactly the fixture's own unrelated `08-30` period and nothing contributed by the `08-29` span. Traced the fixture (`tests/python/fixtures/ordinary_venue.json`): its original `08-30` period is untouched by the test's `_replace_period_for_date` helper (which filters only on `open.date == 08-29`), so this assertion is a genuine no-extra-entry proof, not a coincidence of an empty date.
  - `test_current_multiday_span_closing_exactly_at_midnight_skips_only_the_final_entry`: replaces `08-29`'s period with `open: 08-29 07:30, close: 08-31 00:00` (`span_days == 2`, i.e. `day_gap >= 2`, close still exactly at midnight). Traced the arithmetic by hand: offset 0 → `08-29`: `{450, 2880}` (anchor, real remaining distance); offset 1 → `08-30`: `{0, 1440}` (interior day, not skipped, `1440 != 0`); offset 2 → `08-31`: `entry_close == entry_open == 0` → skipped. Test asserts exactly this: `08-29` gets `[{450,2880}]`, `08-30` gets **two** entries `[{480,1320} (its own original period), {0,1440} (the span-in interior entry)]` — confirming append-not-overwrite alongside the fix — and `08-31` gets only its own original `[{480,1320}]`, no trailing zero-length entry. This is genuinely the `day_gap >= 2` case, distinct from the `day_gap == 1` first test, and confirms only the *final* offset is skipped, not the whole decomposition.
  - `test_current_period_not_closing_at_midnight_is_unaffected`: parses the unmodified fixture and asserts `08-29`'s periods are still `[{open: 480, close: 1320, always_open: false}]` — the fixture's ordinary (non-midnight-closing) period, confirming the fix does not over-trigger on periods that were never affected by GAP 2.
- **`node --test tests/js/*.test.js`**: `# tests 142`, `# pass 142`, `# fail 0`, `# cancelled 0`, `# skipped 0`. No regression — expected, since this fix is Python-only (`scraper/hours.py`) and does not touch `web/ranking.js` or any JS test.
- **`reviews/LEDGER.md` read back**: `grep -c 'IMP-007' reviews/LEDGER.md` → `1`; the single row reads `| \`IMP-007\` | 2026-09-03 | implementation | Fix GAP 2: currentOpeningHours decomposition emits a zero-length entry on a period closing exactly at 00:00 |`.

### Could not verify

- None. Every check above was run against the live repository state directly — commands executed and their actual output read in full, `scraper/hours.py` and the new test bodies read in full and hand-traced against concrete inputs, `git diff`/`git status` read directly rather than summarized.

### Not asked to check

- Whether skipping the zero-length entry is the objectively best design versus an alternative (e.g. flagging it some other way) — the acceptance criteria specify skip-on-`open==close`, and the diff implements exactly that; no broader design judgment was requested.
- `fetch_busyness`/SerpApi, the return-validator bridge, `ranking.js`, coarsening, frontend/generator, `build/refresh.py` orchestration, the `Makefile` target, any live API call, `regularOpeningHours` decomposition (`_parse_regular_hours`), truncation/window-validation/materialisation logic beyond what the emit-or-skip decision touches — all explicit scope exclusions in the brief, not examined.
- `BACKLOG.md`'s `BL-001` status flip — explicitly deferred to assignment close per the brief, correctly absent from this diff.
- Whether the actual model/effort that produced this diff matches `HANDOFF.md`'s claimed `claude_sonnet`, effort high — not observable from this subagent.
- Prose quality, formatting, or style beyond what the acceptance criteria explicitly require.

### Status

`GATE_PASS` — the fix in `scraper/hours.py`'s `_decompose_current_period` matches the acceptance criteria precisely: an entry is skipped only on exact `open == close` (traced to fire only on non-anchor days when the close lands exactly on that date's midnight), a close-before-open same-day malformed shape is traced to still be emitted rather than silently swallowed, and `continues_beyond_window` propagation is unaffected. `tests/python/test_hours.py`'s diff is purely additive (66 insertions, 0 deletions) — all pre-existing tests are byte-unchanged, and all 50 tests in `tests/python/` pass, including the 3 new ones, each read in full and hand-traced to confirm it constructs the specific case it claims (the exact `08-29 07:30 → 08-30 00:00` reproduction, a `day_gap >= 2` span with only the final entry skipped and interior days still self-contained, and a non-midnight control case). `node --test tests/js/*.test.js` remains 142/142 with no regression. The change set is confined to exactly `scraper/hours.py`, `tests/python/test_hours.py`, `HANDOFF.md`, `reviews/LEDGER.md` — matching the brief's named scope, with `BACKLOG.md` correctly deferred. `reviews/LEDGER.md` has exactly one `IMP-007` row.

---
