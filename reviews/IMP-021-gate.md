# Gate record: IMP-021

## Gate invocation 1 — 2026-09-06

- **Gate route**: `claude_sonnet`, effort high
- **Brief source**: generated from HANDOFF.md acceptance criteria + required verification (mechanical brief, `.cross-agent-workflow/gate_brief.py HANDOFF.md`, reproduced verbatim in the invocation prompt)

### Checks run

- **Diff scope confirmed**: `git status --short` at baseline `5e79252` shows `web/app.js`, `tests/js/app.test.js`, `web/index.html` modified, plus `BACKLOG.md`, `HANDOFF.md`, `reviews/LEDGER.md` (bookkeeping, outside declared scope) and an untracked-then-staged `.claude/codex-arch-004-round2-prompt.md` (unrelated artifact, not reviewed). `git diff 5e79252 -- web/ranking.js` and `-- web/style.css` are both empty (0 lines) — confirms the scope exclusions ("no `ranking.js` change; no `style.css` change") hold exactly.
- **Read in full**: the entire `web/app.js` diff (all new label-vocabulary maps, `formatInstant`/`formatBindingLimit`/`latestLeaveClause`/`freshnessWarningTexts`, `seatConfidenceLine`/`bindingLine`/`metricsLine`/`renderCandidateCard`/`renderPlanBSummary`/`renderRemovalNotice`/`renderAlternatives`, the new `renderResultState` switch, and `render()`'s `ctx` construction) and the entire `tests/js/app.test.js` diff (fixture changes plus all 14 new tests).
- **Governing spec sections read in full**: PLAN.md's "Result states: one discriminated outcome, not independent refusal flags" (1813-1843), "Requested end vs achievable end" (1845-1865), "Evidence freshness and a first-ever failed source" (1866-1873), "The returned presentation shape" (1926-1947), "Naming and presentation hierarchy" (2086-2104); cross-checked against CLAUDE.md's condensed equivalents (resultState, achievable/requested end, stale-data invariant, Plan A/Plan B presentation).
- **`.venv/bin/pytest tests/python/ -q`**: `190 passed in 1.6xs` — matches HANDOFF's claim of an unchanged Python count (this assignment touches no Python).
- **`node --test tests/js/*.test.js`**: `# tests 247`, `# pass 247`, `# fail 0` — up from 233 at the IMP-020 baseline (247 − 233 = 14 new tests, matching the diff's 14 new `test(...)` blocks).
- **`make generate` offline reproducibility**: ran twice with `http_proxy`/`https_proxy` pointed at `127.0.0.1:9` (unlistening) so any real network call fails rather than silently succeeding. Both runs produced `web/index.html` with identical MD5 `0aa7ff1d47a9eaad6a7f0a62750abf18`; `diff` between the two run outputs was empty. `git diff --stat -- web/index.html` before and after the two regenerations is identical (224 insertions / 70 deletions against baseline, unchanged), confirming the working-tree `web/index.html` is exactly what `make generate` reproduces deterministically offline, and that it correctly inlines the new `web/app.js` (the diff shows the new label-vocabulary maps and `formatInstant`/`formatBindingLimit`/`latestLeaveClause`/`freshnessWarningTexts` present verbatim inside the embedded `<script type="module">` block).
- **`app.js` has no public/shared contract of its own**: confirmed by reading the full file — it imports only `rankVenues`, `CONTROL_CONTRACT`, `dateFromAbs` from `ranking.js` (all three pre-existing, unchanged exports; `dateFromAbs` already existed at `web/ranking.js:84` before this diff and is newly imported, not newly created). `app.js` exports only `state` and `render`, both pre-existing. Nothing in `ranking.js` changed (confirmed above), so nothing this diff could have broken on that side.
- **Six `resultState` branches**: `renderResultState`'s `switch` (web/app.js:398-453) has exactly six `case` labels — `plan_a`, `invalid_request`, `no_low_risk_option`, `session_does_not_fit`, `no_verified_return`, `nothing_evaluable` — matching PLAN.md's table exactly, each with its own distinct render body, no `default` clause. Read each branch: `invalid_request` renders every `result.violations` entry with control/supplied/permitted (line 406-410); `session_does_not_fit` renders the refusal plus, when `result.bestAlternative` is present, a "not a recommendation" note followed by a full candidate card (418-427); `no_verified_return` renders `result.refusalInstant` through `formatInstant` (day-marked) rather than pre-stringified (428-437); `nothing_evaluable` renders all four of `snapshotEmpty`/`hardFilteredCount`/`travelUnknown.length`/`removed.length` as independent `if` pushes into one `lines` array, never an `else if` chain that would suppress simultaneous causes (438-450).
- **Day markers**: `formatInstant(abs, referenceDate)` (96-105) calls `dateFromAbs(abs)` (a real calendar-date computation in `ranking.js`, not raw-minute arithmetic) and appends `(${date})` only when it differs from `referenceDate`; used at every instant-rendering call site: `bindingLine`'s "Leave the venue by" (217), `metricsLine`'s "ends ~" (238) and `latestLeaveClause` (116-129), and `no_verified_return`'s `refusalInstant` (429). `ctx.referenceDate` is `state.controls.departureDate`, the request's own anchor (469), never re-derived.
- **Naming**: `renderCandidateCard` reads `candidate.displayName` and a separate `candidate.area` line (250-251, no local reconstruction); `renderPlanBSummary` reads `planB.disambiguatedLabel` inline with no area line (291-292); `renderRemovalNotice` and the travel-unknown list both read `.disambiguatedLabel` (301, 487). No call to `.split("-")` or any venue-id string transform remains anywhere in the file (confirmed by reading the whole file — the old local `displayName(venueId)` function and `venueById()` helper are both deleted from this diff).
- **Label-vocabulary maps are used at call sites, not merely declared**: `SEATABILITY_LABELS` is read for `baselineSeatability` and `seatConfidence.confidence` in `seatConfidenceLine` (202, 204); `BUSYNESS_LABELS` for `busynessBand.band` (203); `RETURN_BASIS_LABEL` for `returnBasis` in `bindingLine` (214, pre-existing table, unmoved); `FRESHNESS_LABELS` for `hoursStatus`/`histogramStatus` in `freshnessWarningTexts` (133-138).
- **Freshness — candidate side only, confirmed non-vacuous**: `freshnessWarningTexts(candidate)` is called and appended in `renderCandidateCard` (258-260).
- **`achievableSessionEndMid` / `usableMinutesMid === undefined`**: `metricsLine` (227-242) branches on `candidate.usableMinutesMid === undefined` first, rendering exactly "closed when you'd arrive" and never touching `achievableSessionEndMid`; otherwise it reads `candidate.achievableSessionEndMid` (never `sessionEndMidAbs`, which no longer appears anywhere in the file) through `formatInstant`.
- **Plan B salvage wording**: `renderPlanBSummary` (286-298) reads `planB.travelMinutesMid` for the transfer-time text and, on the `salvage` branch, renders `"...not the ${requested} you asked for (${overallTier})"` — matching CLAUDE.md's "Plan A / Plan B" exact pattern.
- **Existing 3 preserved tests re-verified passing and unmodified in intent**: `renderWith()` is a new shared helper factoring out the previously-duplicated stub-doc setup in all three (import test, CONTROL_CONTRACT test, form-resubmission test) — read each; assertions are unchanged from baseline, only the setup boilerplate moved. The `FEASIBILITY_TOLERANCE_MINUTES` source-grep test is untouched.
- **`makeVenue()`'s `name: "Test Venue"` fixture field — proven necessary, not incidental**: removed the field from `makeVenue()` (leaving `id: "test-venue"` alone) and reran `node --test tests/js/app.test.js`: exactly one failure, `render() produces a controls form and a Plan A card for an always-open venue` (regex `/Plan A: Test Venue/` no longer matches, since `ranking.js`'s `resolveVenueNaming` falls back to the raw `venue.id` — `"test-venue"`, not title-cased — when `name` is absent). Restored; `diff` against the pre-check backup was empty and the full suite returned to 247/247.

### Non-vacuity spot-checks (reverted, reran, restored — 6 performed, 3+ required)

Backed up `web/app.js` to a scratch copy before each check; every restoration below was verified by `diff` against that backup (empty) and by rerunning the full `node --test tests/js/*.test.js` (247/247) and `.venv/bin/pytest tests/python/ -q` (190 passed) at the end.

1. **Day markers**: replaced `formatInstant`'s body with `return formatClockFromAbs(abs);` (dropping the `dateFromAbs` comparison entirely). Reran `node --test tests/js/app.test.js`: 2 failures — `day markers: an achievable end and binding limit...` and `resultState no_verified_return: renders refusalInstant as a day-marked absolute instant...` (17/19 passed). Restored; diff empty.
2. **Naming**: replaced `renderCandidateCard`'s heading line with a local `venueId.split("-").map(...).join(" ")` reconstruction, bypassing `candidate.displayName`. Reran: 1 failure — `naming: Plan A shows displayName plain plus a separate area line; a same-named removed venue shows disambiguatedLabel inline` (18/19 passed). Restored; diff empty.
3. **Freshness (candidate)**: replaced `freshnessWarningTexts` with `return [];`. Reran: 1 failure — `freshness: a stale hours source and a failed histogram source are both always-visible on the candidate` (18/19 passed). Restored; diff empty.
4. **Freshness (Plan B) — this is the finding below**: left `freshnessWarningTexts` intact but deleted the `for (const freshText of freshnessWarningTexts(planB)) {...}` loop from `renderPlanBSummary` only (candidate-side freshness untouched). Reran: **0 failures, 19/19 passed.** No test exercises a stale/failed `hoursStatus`/`histogramStatus` on a `planB` object. Restored; diff empty.
5. **`no_low_risk_option` resultState text — this is the finding below**: replaced the rendered text in that `case` with the literal string `"WRONG TEXT"`. Reran: **0 failures, 19/19 passed.** No test renders or asserts against the `no_low_risk_option` branch at all (`ranking.test.js` exercises the *ranking* logic that produces this state at line 2347, but no `app.test.js` test ever renders it). Restored; diff empty.
6. **"of the Xh you asked for" duration-shortfall note — this is the finding below**: in `metricsLine`, replaced the conditional `durationNote` computation with a hardcoded `const durationNote = "";`. Reran: **0 failures, 19/19 passed**, including the test titled `metrics: a shorter-than-requested session states 'of the Xh you asked for', reading achievableSessionEndMid` (tests/js/app.test.js:283-297) — its body only asserts `assert.ok(card, "expected a rendered card for the shorter candidate")` and never inspects `card.textContent` for the phrase its own title names. Restored; diff empty.

### Findings

1. **Vacuous test, `tests/js/app.test.js:283-297`.** The test titled `"metrics: a shorter-than-requested session states 'of the Xh you asked for', reading achievableSessionEndMid"` never asserts that text. Its only assertion is `assert.ok(card, "expected a rendered card for the shorter candidate")`. Spot-check 6 above deleted the entire duration-shortfall-note mechanism in `metricsLine` and this test still passed. This is exactly the failure mode CLAUDE.md's "Review-response coverage" paragraph names explicitly for this project ("each must assert the positive rendering, not merely the absence of the wrong text, which passes on a renderer that shows nothing at all") — here it is weaker still, since even an absence-of-wrong-text check is missing. The underlying `web/app.js` mechanism (227-237) reads correctly on inspection, but it is unverified by any test.
2. **Untested resultState branch, `web/app.js:415-417` / no corresponding test in `tests/js/app.test.js`.** `no_low_risk_option` is one of the six `resultState` values the acceptance criteria require to "render distinct, correct text," and `ranking.test.js:2347` already produces this state from `rankVenues()`, but no `app.test.js` test ever renders a result with `resultState: "no_low_risk_option"` or asserts on `RESULT_STATE_TEXT.no_low_risk_option`. Spot-check 5 replaced the rendered text with `"WRONG TEXT"` and the full suite still passed 19/19.
3. **Untested Plan B freshness, `web/app.js:294-296` / no corresponding test in `tests/js/app.test.js`.** The acceptance criteria state stale/failed `hoursStatus`/`histogramStatus` must be "always visible, on any candidate or Plan B view carrying them" — candidate-side freshness has a direct test (`freshness: a stale hours source...`, line 376) but no test ever sets `hoursStatus`/`histogramStatus` to a non-`ok` value on a `planB` object and checks it renders. The one test touching `planB`'s freshness fields (`Plan B salvage: states its actual duration...`, line ~419) sets both to `"ok"`, which never exercises the warning-rendering branch. Spot-check 4 deleted the freshness-rendering loop inside `renderPlanBSummary` specifically and the full suite still passed 19/19.

None of the three findings is a functional defect in the shipped `web/app.js` as read — in each case the code itself appears correct on direct inspection, and I have no evidence any of the three mechanisms is actually broken. All three are test-coverage gaps against acceptance criteria the project's own documented testing culture (CLAUDE.md's "Review-response coverage" paragraph, and this gate's own task instructions naming exactly these two risk areas — Plan B freshness and non-vacuous positive-content assertions) treats as required, not optional. A future regression in any of the three mechanisms would ship silently green.

### Could not verify

None. Every item in the mechanical brief's "Required verification" was independently reproduced first-hand (pytest, node --test, double offline `make generate` with a byte-identical working-tree match), and every acceptance-criterion clause was traced to specific code and either confirmed correct by direct reading or spot-checked by reverting the mechanism and observing the expected test failures (or, for the three findings above, the expected failures did not occur).

### Not asked to check

- **`BACKLOG.md`, `HANDOFF.md`, `reviews/LEDGER.md`, `.claude/codex-arch-004-round2-prompt.md`**: outside the declared "Artifact under review." Not audited for prose accuracy.
- **`ranking.js`'s own already-tested behavior** (e.g., that `resolveVenueNaming`, `dateFromAbs`, or the `resultState` taxonomy compute correctly) — verified unchanged by this diff (`git diff` empty) and already gated under IMP-020; re-verifying its internals is outside this assignment's scope.
- **Progressive-disclosure restructuring and focus-preservation** — explicitly Slice 4 per the scope exclusions; not evaluated.
- **Manual iPhone / Home Screen acceptance checklist** — out of scope for an automated gate per CLAUDE.md's own testing section ("Anything touching the network is out of scope for automated tests").

### Status

GATE_FAIL

---

## Gate invocation 2 — 2026-09-06

- **Gate route**: `claude_sonnet`, effort high, fresh context (no memory of invocation 1's own reasoning — re-derived independently from HANDOFF.md and this file).
- **Brief source**: this invocation's task instructions named the three invocation-1 findings directly and asked for their specific re-verification, in addition to the standard mechanical-brief checks.

### Checks run

- **`HANDOFF.md` read in full**: "Gate result" line records invocation 1 as `GATE_FAIL` — "No functional defect found; 3 test-coverage gaps... All 3 fixed with real assertions; full suite reran green (`.venv/bin/pytest`: 190 passed; `node --test`: 249/249; `make generate` ×2 byte-identical)." Baseline commit unchanged at `5e79252`.
- **`reviews/IMP-021-gate.md` invocation 1 read in full**: findings 1/2/3, all six non-vacuity spot-checks (1-6), and the full "Checks run" list re-derived independently below rather than assumed.
- **`git diff 5e79252 -- web/app.js` read in full (406 lines)**: read the complete resulting `web/app.js` (524 lines) directly, independent of trusting invocation 1's line citations. Every specific claim in invocation 1's record checks out against the file as it stands right now: the `renderResultState` switch is at lines 398-453 with exactly six `case` labels (`plan_a`, `invalid_request`, `no_low_risk_option`, `session_does_not_fit`, `no_verified_return`, `nothing_evaluable`) and no `default`; `bindingLine`'s "Leave the venue by" clause is at line 217; `metricsLine` (227-242) branches on `usableMinutesMid === undefined` before touching `achievableSessionEndMid`; `renderPlanBSummary` (286-298) reads `planB.disambiguatedLabel`, `planB.travelMinutesMid`, and the salvage wording "not the ${…} you asked for"; `freshnessWarningTexts` (131-140) is called from both `renderCandidateCard` (258) and `renderPlanBSummary` (294). This is the same code invocation 1 described — nothing in `web/app.js` changed between invocations, consistent with the primary's claim that only test coverage needed fixing. `grep -n '\.split("-")\|venueById(' web/app.js` returns nothing, confirming no locally-reconstructed name was reintroduced.
- **`git diff 5e79252 -- web/ranking.js`** and **`-- web/style.css`**: both 0 lines. Scope exclusions hold exactly, as invocation 1 also found.
- **`git diff 5e79252 -- tests/js/app.test.js` read in full**: on top of invocation 1's 14 new tests, this diff adds a `fullVenue()` fixture builder (an extension of `makeVenue()`, taking id/name/area/baseline/businessStatus/access/returnStatus/hours-window fields/popularTimes/fallbacks/`hoursStatus`/`histogramStatus`) and 2 new tests, plus rewrites the assertions of 1 existing test in place. Net test count: 249 (up from invocation 1's 247), matching HANDOFF's claimed +2.
- **`.venv/bin/pytest tests/python/ -q`**: `190 passed in 1.52s` — unchanged, matches HANDOFF's claim (this assignment touches no Python).
- **`node --test tests/js/*.test.js`**: `# tests 249`, `# pass 249`, `# fail 0` — matches HANDOFF's claim exactly.
- **`make generate` offline reproducibility**: ran twice with `http_proxy`/`https_proxy` pointed at `127.0.0.1:9` (unlistening). Both runs produced `web/index.html` with identical MD5 `0aa7ff1d47a9eaad6a7f0a62750abf18` — the same MD5 invocation 1 recorded, as expected since neither `app.js` nor `ranking.js` changed since then. `diff` between the two fresh runs was empty, and `diff` between either fresh run and the working-tree `web/index.html` (backed up before regenerating) was also empty — the checked-in file is exactly what `make generate` reproduces deterministically offline.
- **Independent sanity-check 1 (not merely trusting invocation 1's record) — six `resultState` branches**: read `renderResultState`'s switch directly (398-453) and counted six `case` labels with no `default`, each with its own distinct render body (`invalid_request` lists every `result.violations` entry; `session_does_not_fit` renders the refusal plus a "not a recommendation" note and full card when `bestAlternative` is present; `no_verified_return` renders `result.refusalInstant` through `formatInstant`; `nothing_evaluable` pushes all four diagnostics independently, no `else if` chain). Confirmed correct by direct reading, not by re-running invocation 1's described check.
- **Independent sanity-check 2 — day-marker mechanism**: read `formatInstant` (101-105) directly: it calls `dateFromAbs(abs)` (imported from `ranking.js`, a real calendar-date computation, not raw-minute math) and appends `(${date})` only when it differs from `referenceDate`. Traced every call site by grep/read: `bindingLine` (217), `metricsLine`'s "ends ~" (238), `latestLeaveClause` (119, 121), and the `no_verified_return` branch (429) — all four instant-rendering call sites use it, none stringify a bare clock time from a potentially-different-day instant.
- **Independent sanity-check 3 — naming mechanism**: read `renderCandidateCard` (250-251: `candidate.displayName` heading, separate `candidate.area` paragraph), `renderPlanBSummary` (291-292: `planB.disambiguatedLabel` inline, no area line), `renderRemovalNotice` (301) and the travel-unknown list (487) (both `disambiguatedLabel`). Grepped the whole file for `.split("-")` and `venueById(` — zero matches, confirming no local name reconstruction exists anywhere.

### Finding-by-finding closure verification

1. **Vacuous "of the Xh you asked for" test — CLOSED.** `tests/js/app.test.js`'s `metrics: a shorter-than-requested session...` test (now lines ~283-299) was rewritten in place: it still confirms `resultState === "plan_a"` and `planA.tier === "tight"`, but now also asserts `assert.match(card.textContent, /Usable: 2h 50m of the 3h you asked for/)` and `assert.match(card.textContent, /ends ~12:57/)` — the exact phrase the test's own title names, not merely `assert.ok(card, ...)`. Spot-check 3 below (hardcoding `durationNote` to `""`) now fails exactly this test — confirmed by direct rerun.
2. **Untested `no_low_risk_option` branch — CLOSED.** A new test, `resultState no_low_risk_option: renders its own distinct refusal text`, builds a `baseline: "poor"` fixture (mirroring `ranking.test.js`'s own `no_low_risk_option` fixture, per the test's own comment), asserts `resultState === "no_low_risk_option"` and `planA === null`, then asserts the refusal box matches `/No low-risk option found for the requested session/` and `/Try a different time, or accept the risk/` — the literal text of `RESULT_STATE_TEXT.no_low_risk_option`. Spot-check 2 below (hardcoding this branch's text to `"WRONG TEXT"`) now fails exactly this test — confirmed by direct rerun.
3. **Untested Plan B freshness — CLOSED, and verified non-synthetic.** A new test, `Plan B freshness: a stale hours source on the fallback itself is always-visible, not just on the primary candidate`, builds a fallback venue via `fullVenue({ ..., hoursStatus: "stale", histogramStatus: "failed" })`, which the new fixture helper writes onto `v.hours.status` / `v.histogram = { status }` — i.e. onto the actual venue object consumed by `rankVenues()`, not a synthetic `planB` object bypassing the pipeline. Traced this through `web/ranking.js:1996-1997` (`hoursStatus: planBFallbackVenue?.hours?.status, histogramStatus: planBFallbackVenue?.histogram?.status` inside the `planBView` construction) to confirm the fixture's fields genuinely flow into the real `planB` the renderer receives — this is not a test that could pass on a stub. The test asserts the resulting Plan A card matches `/Hours evidence: stale/` and `/Busyness evidence: unavailable/`, and its own comment correctly notes the primary candidate itself carries no freshness issue, so these warnings can only originate from `renderPlanBSummary`. Spot-check 1 below (deleting the Plan B freshness loop) now fails exactly this test — confirmed by direct rerun.

### Non-vacuity spot-checks (reverted, reran, restored — all 3 required mechanisms, the same ones invocation 1 reverted)

Backed up `web/app.js` to a scratch copy before each check (MD5 `6f8477ff87377615838be2404c057904`); every restoration below was verified both by `md5` matching that backup and, after all three, by a final `node --test tests/js/*.test.js` (249/249) and `.venv/bin/pytest tests/python/ -q` (190 passed).

1. **Plan B freshness loop deleted** (`renderPlanBSummary`'s `for (const freshText of freshnessWarningTexts(planB)) {...}` removed, candidate-side `freshnessWarningTexts` untouched): reran `node --test tests/js/app.test.js` — **1 failure**, `Plan B freshness: a stale hours source on the fallback itself is always-visible...` (20/21 passed). This is the opposite of invocation 1's result (0/19 failures on the same revert) and confirms the gap is now closed. Restored; MD5 matched.
2. **`no_low_risk_option` text hardcoded to `"WRONG TEXT"`**: reran — **1 failure**, `resultState no_low_risk_option: renders its own distinct refusal text` (20/21 passed). Opposite of invocation 1's 0/19-failure result. Restored; MD5 matched.
3. **`durationNote` hardcoded to `""` in `metricsLine`**: reran — **1 failure**, `metrics: a shorter-than-requested session states 'of the Xh you asked for', reading achievableSessionEndMid` (20/21 passed). Opposite of invocation 1's 0/19-failure result. Restored; MD5 matched.

All three of invocation 1's demonstrated gaps now produce the expected test failure when the underlying mechanism is broken — the defining property of a non-vacuous test. `web/app.js` itself needed no changes to reach this state, consistent with invocation 1's conclusion that the renderer was already correct and only undertested.

### Scan for new coverage problems introduced by the fix

- Read the full diff of `tests/js/app.test.js` a second time specifically looking for a new vacuous assertion or a weakened existing one. Found none: the two new tests both assert against the specific literal text of the mechanism they exercise (not merely "a card exists" or "no wrong text"), and the one rewritten test strengthened its assertions without removing any prior check (`resultState`/`tier` assertions were kept, not dropped).
- The new `fullVenue()` helper is additive (a superset of `makeVenue()`'s parameters); it did not require changing any of the pre-existing tests that still call `makeVenue()` directly, and the three originally-shared-boilerplate tests (`renderWith()`-based) are unchanged in assertion content from invocation 1's own record.
- No new resultState branch, label map, or day-marker call site was touched by this diff (confirmed by the `web/app.js` diff being empty since invocation 1) — so there is no new production-code surface this diff could have left uncovered.
- One residual, pre-existing style observation, not a coverage gap: the `Plan B salvage` test constructs its candidate/planB object by hand rather than through `rankVenues()`, as its own comment explains (avoiding duplicating `ranking.test.js`'s salvage-floor coverage). This is a deliberate, documented boundary choice from invocation 1's diff, unchanged here, and does not affect the closure of any of the three findings.

### Could not verify

None. Every required verification item was independently reproduced first-hand (pytest, node --test, double offline `make generate` with byte-identical match against both the other run and the working tree), and each of the three findings was closed by locating its specific new/changed test, confirming its assertions target the exact mechanism named, and reverting that mechanism to confirm the test now fails where it previously didn't.

### Not asked to check

- **`BACKLOG.md`, `HANDOFF.md`, `reviews/LEDGER.md`, `.claude/codex-arch-004-round2-prompt.md`**: outside the declared artifact under review, as in invocation 1. Not audited for prose accuracy; not edited by this gate.
- **Everything invocation 1 already scoped out** (`ranking.js`'s own already-tested internals, progressive-disclosure/focus-preservation restructuring reserved for Slice 4, the manual iPhone acceptance checklist) — unchanged by this diff, so re-litigating them here would add nothing.
- **The `Plan B salvage` test's hand-built-candidate boundary choice**: noted above as a style observation, not evaluated further since it predates this invocation's diff and is not one of the three findings this gate was asked to re-check.

### Status

GATE_PASS
