# Review record: IMP-001

## Assignment

- **Assignment ID**: `IMP-001`
- **Work type**: `implementation`
- **Primary route**: `claude_sonnet` — Sonnet, effort `high`
- **Reviewer route**: `codex_terra` — Terra, effort `medium`
- **Baseline commit**: `f6aa4f0`
- **Reviewed diff or commit**: commit `1677066` — `web/ranking.js` (new) and `tests/js/ranking.test.js` (new); supporting ESM declaration in `package.json`
- **Scope**: `resolve_hours`, `effective_close`, and feasibility-tier logic in `web/ranking.js` plus its JavaScript tests. Excludes `fetch_hours.py`, `fetch_busyness.py`, `build/refresh.py`, `app.js`, `index.template.html`, and `seat_confidence`/`backup_strength`/Plan A-B work.

## Review round 1 — 2026-08-30

- **Lifecycle state reviewed**: `review_requested`
- **Reviewer route**: `codex_terra`
- **Runtime model verification**: the runtime does not expose the selected model; the user's route selection is relied on unless the interface reports otherwise.
- **Reviewed artifact**: commit `1677066` against baseline `f6aa4f0`; `git status --short --branch` reported `main...origin/main [ahead 6]` with no uncommitted paths.

### Resolution of prior findings

None — first review round.

### Findings

#### `IMP-001-R1-F01` — Low — Declared test command fails with the default Node runtime

- **Status at issuance**: `open`
- **Evidence**: `HANDOFF.md:20` requires `node --test tests/js/`. With the default `node` at `/Users/darrensy/.nvm/versions/node/v24.19.0/bin/node` (`v24.19.0`), that exact command exits non-zero: `Cannot find module '/Users/darrensy/Projects/study-venue-planner/tests/js'`. The identical directory command passes on the separately installed Node `v18.15.0`, and `node --test tests/js/*.test.js` passes all 38 tests on both versions. `package.json` declares ESM but neither a test script nor a supported Node version.
- **Impact**: the assignment's required verification is dependent on an unstated PATH-selected Node version, so a normal current-runtime rerun reports a false failure and is not reproducible as declared.
- **Recommended correction**: make the required test invocation version-independent (for example, `node --test tests/js/*.test.js`) and record that exact command in `HANDOFF.md`; rerun it with the default `node` before returning to review. A project-level test script or explicit engine policy is also acceptable if it makes the declared verification deterministic.

### Non-blocking observations

- `web/ranking.js:223-230` returns `COVERED` for a regular-authority period crossing midnight when `requiredEndAbs` is at or before the midnight boundary. This agrees with the governing lazy-walk contract: no exact close is needed or claimed before the boundary. The case lacks an isolated test, but it is a precision choice rather than a correctness defect.
- The two unrepresented `window handling` cases — materialising all seven dates and rejecting interior truncation — are fetcher/data-validation obligations assigned to the absent `fetch_hours.py` and `tests/python/` (`CLAUDE.md:103-104`). They are explicitly excluded from `IMP-001`; their absence is a scope boundary, not a blocker for this implementation.

### Verification performed

- Preflighted `AGENTS.md`; `WORKFLOW.md` Roles, Choosing a route, and Lifecycle sections; `HANDOFF.md`; the Phase 1 hours decision model in `plan.md`; and `reviews/IMP-001-gate.md`. Confirmed `HANDOFF.md` names `codex_terra` and `review_requested` for `IMP-001`.
- Inspected `git status`, commit `1677066`, and the targeted diff against `f6aa4f0`; `web/ranking.js` and `tests/js/ranking.test.js` are new in the reviewed commit. `git diff --check f6aa4f0 1677066` passed.
- Reran the declared `node --test tests/js/`: it failed under default Node `v24.19.0` as reported in `IMP-001-R1-F01`; reran it under available Node `v18.15.0`, where all 38 tests passed. The explicit-file and glob forms each passed all 38 tests under the default Node; the glob also passed under Node 18.
- Confirmed `web/ranking.js` contains no imports, DOM/global browser references, storage access, or `fetch()` calls. Inspected every surplus use: direct `kind`/`minutes` access is limited to `passesFeasibility`, `finiteShortfall`, `sortKey`, and `display`; tier logic consumes surplus only through the required accessors.
- Independently traced source authority, active-period selection, lazy cross-date walk, inclusive exact-close boundary, `NONE` versus `UNKNOWN`, `AT_LEAST(0)` tagging, and upper-bound-only robust tiering against `plan.md` and `CLAUDE.md`. Added direct runtime probes for the regular midnight precision case and current-authority 24-hour boundary, both returning the contract-consistent `COVERED` outcome.

### Could not verify

None.

### User decisions required

None.

### Recommendation

`CHANGES_REQUESTED`

---

## Primary response to review round 1 — 2026-08-30

- **Lifecycle state addressed**: `changes_requested`
- **Primary route**: `claude_sonnet` — Sonnet, effort high
- **Reviewed artifact after response**: `HANDOFF.md` only (`Required verification` field); no change to `web/ranking.js`, `tests/js/ranking.test.js`, or any other assignment artifact

### Finding dispositions

#### `IMP-001-R1-F01` — `accepted`

- **Factual assessment**: `confirmed` — independently reproduced before making any change. Ran the
  declared `node --test tests/js/` under this session's default `node` (`/Users/darrensy/anaconda3/bin/node`,
  `v18.15.0`): passed, 38/38. Then ran the same directory-form command under
  `~/.nvm/versions/node/v24.19.0/bin/node` (the exact binary the reviewer's environment resolved as
  default): failed — `✖ tests/js (37.766291ms)`, `'test failed'`. Ran `node --test tests/js/*.test.js`
  under both Node 18.15.0 and Node 24.19.0: 38/38 passing on both. Confirms the finding exactly as
  reported — the directory-form invocation is Node-version-dependent, the glob form is not.
- **Independent verification**: see above — reproduced the failure on Node 24.19.0 myself rather than
  taking the review record's evidence on faith, per this repo's cross-agent verification rule.
- **Response**: the recommended correction (switch the declared required-verification command to the
  glob form) is the minimal fix and does not touch `ranking.js` behavior. Applied as recommended; did
  not pursue the alternative (a `package.json` test script / engine policy) since the glob form alone
  makes the declared command deterministic without adding new project surface.
- **Changes made**: `HANDOFF.md` — `Required verification` field: `node --test tests/js/` →
  `node --test tests/js/*.test.js`. No other field's substance changed beyond bookkeeping
  (`Review record`, `Next action`).
- **Verification**: `node --test tests/js/*.test.js` rerun under this session's default `node`
  (v18.15.0): `# tests 38`, `# pass 38`, `# fail 0`. Also reran under Node 24.19.0 (the version that
  originally failed): `tests 38`, `pass 38`, `fail 0`. Both clean.

### User-directed revisions

- None.

### User decisions required

- None.

### Re-review readiness

- **Ready for re-review**: yes
- **Reason**: the single finding is corrected and independently reverified on both the originally
  failing Node version and this session's default; no other artifact changed; scope was not
  broadened.

---

## Review round 2 — 2026-08-30

- **Lifecycle state reviewed**: `review_requested`
- **Reviewer route**: `codex_luna`
- **Runtime model verification**: the runtime does not expose the selected model; the user's route selection of `codex_luna`, effort medium, is relied on. This is the targeted round-2 route selected by `WORKFLOW.md`'s exact-text/deterministic-proof rule.
- **Reviewed artifact**: correction delta from round 1: `HANDOFF.md`'s `Required verification` field and the appended `Primary response to review round 1` in this record.

### Resolution of prior findings

| Finding ID | Status | Evidence |
| --- | --- | --- |
| `IMP-001-R1-F01` | `resolved` | `HANDOFF.md` now declares `node --test tests/js/*.test.js`; the command passed 38/38 independently under Node `v24.19.0` and `/Users/darrensy/anaconda3/bin/node` `v18.15.0`. |

### Findings

No findings.

### Verification performed

- Confirmed `HANDOFF.md` remains `review_requested` and its `Required verification` field reads exactly `node --test tests/js/*.test.js`.
- Reran `node --test tests/js/*.test.js` with default Node `v24.19.0`: 38 tests passed, 0 failed. Reran the same command with available Node `v18.15.0`: 38 tests passed, 0 failed.
- Inspected `git status --short --branch` and `git diff --name-status`. The correction delta after the pre-gate commit contains only the `HANDOFF.md` field-level reconciliation; `reviews/IMP-001.md` is the untracked append-only review record. `git diff 1677066 -- web/ranking.js tests/js/ranking.test.js` is empty, so neither implementation file nor JavaScript test changed. The added `reviews/IMP-001-gate.md` shown versus `1677066` is the mandatory pre-gate record that existed before round 1, not part of this correction delta.

### Could not verify

None.

### User decisions required

None.

### Recommendation

`APPROVE`

---
