# Review record: IMP-022

## Assignment

- **Assignment ID**: `IMP-022`
- **Work type**: `implementation`
- **Primary route**: `claude_sonnet` — Sonnet, effort high
- **Reviewer route**: `codex_terra` — Terra, effort medium
- **Baseline commit**: `cc93fa5`
- **Reviewed diff or commit**: `cc93fa5..bf4bbf5` — `web/ranking.js`, `build/refresh.py`, `build/outbound_validator_bridge.py`, `build/validate_outbound_transport.mjs`, `tests/js/ranking.test.js`, `tests/js/app.test.js`, `tests/python/test_outbound_validator_bridge.py`, and `tests/python/test_refresh.py`; generated `web/index.html` was also verified.
- **Scope**: Slice 3 outbound mirror: `outbound_admissible`, `resolve_outbound_service`, and `validate_outbound_transport`, their refresh bridge/stamping integration and tests. `data/venues_meta.json` hand-curation is blocked on the user and is not a finding; return-leg logic and Slice 4 UI disclosure are excluded.
- **Finding disposition schema**: `factual-assessment/v1`

Round-1 shadow-trial scope declaration: the changed-path scope is the tracked `cc93fa5..bf4bbf5` implementation/test diff named above; there are no in-scope untracked additions at this review point. The generated-artifact inventory is `web/index.html`, SHA-256 (`shasum -a 256`) `696ea26710994269b5120baec57f6011f59306aca424c7843f7c58848d608962`, captured in round 1. Discovery surfaces are: Python test discovery over `tests/python/`; Node test glob discovery over `tests/js/*.test.js`; `make generate` invoking `build/generate.py` over the generator's declared data/web inputs and emitting `web/index.html`; and the outbound bridge spawning Node over the explicitly supplied metadata path, whose JSON venue IDs are its validation domain. This declaration authorizes skipping nothing: every declared check runs in every round.

## Review round 1 — 2026-09-06

- **Lifecycle state reviewed**: `review_requested`
- **Reviewer route**: `codex_terra`
- **Runtime model verification**: Per the standing constant in `WORKFLOW.md`: the runtime does not expose the selected model; the user's route selection is relied on.
- **Reviewed artifact**: `cc93fa5..bf4bbf5` at `bf4bbf5de3598e3c300f2998ea8d9707872729dd`.

### Resolution of prior findings

None — first review round.

### Findings

No findings.

### Verification performed

- Read the required assignment context, both full gate invocations, the governing outbound-mirror contract, the complete named diff, and the relevant callers/bridges.
- Independently confirmed the current tests cover both gate fixes: all four pipeline removal reasons assert visible text, and the bridge rejects both inconsistent rollup directions, including `rollup: "ok"` with an invalid per-origin/mode entry.
- `.venv/bin/pytest tests/python/ -q` — 209 passed.
- `node --test tests/js/*.test.js` — 286 passed, 0 failed.
- `http_proxy=http://127.0.0.1:9 https_proxy=http://127.0.0.1:9 make generate` — completed offline; `web/index.html` SHA-256 remained `696ea26710994269b5120baec57f6011f59306aca424c7843f7c58848d608962`.
- `git diff --check cc93fa5 bf4bbf5` and current `git diff --check` — clean.

### Could not verify

None.

### User decisions required

None.

### Recommendation

APPROVE

```text
Run as: Claude Sonnet, effort high
Why: IMP-022 round-1 review recommends APPROVE with no findings; only the primary may reconcile the lifecycle record.
Conversation: CONTINUE
Action: Reconcile HANDOFF.md to review_complete
Goal: Slice 3 (`PLAN.md`'s "Phase 1 review-response slice order") — `outbound_admissible`, `resolve_outbound_service`, `validate_outbound_transport` in `web/ranking.js`/`build/refresh.py`, plus `outbound_transport` hand-curation in `data/venues_meta.json`

Read `reviews/IMP-022.md` round 1. Reconcile only `HANDOFF.md` from `review_requested` to `review_complete`, record the APPROVE recommendation, then stop and state that the assignment awaits the user's approval decision. Do not alter implementation artifacts, commit, push, or activate anything.
```
