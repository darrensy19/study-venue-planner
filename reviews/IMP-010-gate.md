# Gate record: IMP-010

## Gate invocation 1 — 2026-09-03

- **Gate route**: `claude_sonnet`, effort high
- **Brief source**: mechanically generated from `HANDOFF.md`'s `## Current assignment` block, not from the primary's prose

### Checks run

- **`git rev-parse HEAD`**: `c4737d917bd064b7dd40af9797010d52752ef395`, matching the brief's stated baseline commit `c4737d9` exactly.
- **`git status --short`**: `M HANDOFF.md`, `M reviews/LEDGER.md`, `M tests/js/ranking.test.js`, `M web/ranking.js` — no untracked files. `git diff --stat`: `HANDOFF.md` (28 lines), `reviews/LEDGER.md` (1 line), `tests/js/ranking.test.js` (441 lines), `web/ranking.js` (368 lines). Every touched path falls exactly inside the brief's named scope; nothing else touched.
- **`git diff -- web/ranking.js` inspected in full**: every changed line is a pure addition after the pre-existing `resolveFeasibilityAtArrivals` (the diff hunk starts with `@@ -1332,3 +1332,371 @@`, and `git diff | grep '^-' ` returns only the `--- a/web/ranking.js` header line — zero real deletions). Grepped each of the six excluded settled functions (`resolveOverallFeasibilityAtArrivals`, `resolveBackupStrength`, `evaluatePlanBFallback`, `resolveBusynessBand`, `resolveSeatConfidence`, `validateReturnTransport`) against the diff: each appears only as a call site inside the new code (`evaluatePlanBFallback` at line 1484, `resolveBusynessBand` at 1639, `resolveSeatConfidence` at 1640), never redefined. Confirms the new code is additive and the settled primitives are untouched, as required.
- **New code read in full (`web/ranking.js:1336-1702`)**: `rankVenues(snapshot, controls)` at line 1565 takes exactly `(snapshot, controls)`, destructures `{venues, holidays}` and the control fields, and performs no `fetch`, `document`, `window`, or filesystem access anywhere in the new block (grepped for all four — none present). `CLOSING_BUFFER_DEFAULT_MINUTES = 30` (line 1344) matches `PLAN.md`'s "New constants" for `closing_buffer_minutes: null` fallback semantics referenced in `CLAUDE.md`.
- **The 14-row taxonomy (`PLAN.md` lines 1585-1602, "The ranked and unranked taxonomy") checked row by row against the code and against a dedicated test, one row at a time**: all 14 rows have their own `rankVenues`-level test in `tests/js/ranking.test.js:1911-2080` (`overall_tier robust/tight`, `shorter`, `unverified`-barred-from-Plan-A, access-explicit-null, access-missing-hard-filter, `return_transport_status` absent/invalid, hours `UNKNOWN`, contradictory hours, upper-bound `NONE`, midpoint `NONE`, neither-bound, preference missing/malformed, preference duplicated, non-`OPERATIONAL`) — traced each against the corresponding code branch at `web/ranking.js:1582-1668` and confirmed the branch produces the classification the taxonomy row requires. All 14 pass.
- **The 8-key ranking order** (`PLAN.md` "The ranking pipeline" 1-8; items 1-2 are the hard filter and per-bound openness, folded into `overall_tier` before `compareCandidates` runs; items 3-8 are the sortable keys): `compareCandidates` (`web/ranking.js:1421-1435`) compares, in order, `tier` (via `TIER_RANK`), `seatConfidence.confidence` (via `CONFIDENCE_RANK`), `backupStrength` (via `BACKUP_RANK`), `travelMinutesMid`, `preference`, then `surplusMid` **exclusively through `surplusSortKey()`** (line 1431-1432: `surplusSortKey(a.surplusMid ?? AT_LEAST_0)`, never a raw `.minutes` read or a direct comparison of the tagged value), with `venueId` as a final stable guard. Matches `PLAN.md` exactly.
- **The 7-key fallback-selection order** (`PLAN.md` "Choosing among several fallbacks"): `compareFallbacks` (`web/ranking.js:1449-1464`) implements keys 1-5 and 7 (`backup_strength`, `overall_tier`, `seat_confidence`, fallback travel burden from Plan A, `preference`, `venue_id`) and explicitly omits key 6 (`surplus_mid`). The code's own comment justifies this by quoting `PLAN.md`'s claim that keys 6-7 are "unreachable between two distinct ranked venues" once `preference` has decided. **Independently re-read `PLAN.md` lines 1632-1644 to check this reasoning rather than accept the comment**: `PLAN.md`'s argument for keys 6-7 being unreachable rests on "every surviving venue holds a unique preference" — but that guarantee comes only from `validatePreferenceSnapshot`, which is run once over the whole `venues` array (line 1575) and used only to filter the **primary per-venue loop** (line 1583). A venue referenced only as somebody's `fallbacks[].venue_id` is fetched raw from `venueById` (built at line 1574 from the **entire, unfiltered** venue list) in `selectPlanBFallback` (line 1479) and its `preference` is read directly (line 1506) with **no re-check against `invalidPreference`**. So `PLAN.md`'s uniqueness guarantee, which is stated for "every surviving venue" in the whole-dataset ranking, does not actually extend to a venue that only appears as a fallback target — `compareFallbacks`'s own preference guard (`a.preference !== undefined && b.preference !== undefined`, line 1460) is defensive code written precisely because the implementer knew this could be missing. This makes the key-6-omission comment's citation of `PLAN.md` imprecise (the cited guarantee doesn't cover this case), though I did not find a live test scenario where it changes an actual ranking outcome, since `venue_id` still supplies a deterministic order as key 7.
- **`bicycle_with_you` threading**: `resolvedMode === "cycle"` (line 1619) uses the outbound mode *after* `resolveOutboundMode`'s wet-weather substitution (line 1592), matching `CLAUDE.md`'s "`bicycle_with_you` is `outbound_mode == "cycle"`" rule read against the *resolved* mode. Threaded unchanged into `selectPlanBFallback`'s params (line 1643) and from there into `evaluatePlanBFallback` (line 1490) — never re-derived from `fallbacks[].mode`. Confirmed by test `control resolution: raining substitutes the outbound mode and its access band per venue` (line 2212) and by reading `evaluatePlanBFallback`'s own `cycle_fallback_unviable` branch (line 890), which is a pre-existing, untouched settled function.
- **Refusal messages**: `rankVenues` returns `refusals: {noLowRiskOption: boolean, noVerifiedReturn: "HH:MM"|null}` (lines 1686-1692, 1694-1701) — two structurally distinct fields (a boolean and a formatted-time-or-null) computed from genuinely independent conditions (`qualifying.length === 0` for the first; `ranked` and `shorter` both empty **and** `unverified` non-empty for the second), so the two can never be substituted for each other. `app.js` (scope-excluded from this assignment) is what turns these into the literal sentences quoted in `PLAN.md`'s "The tool is allowed to say no" — the entry point supplies exactly what a renderer needs without re-deriving anything, consistent with `PLAN.md`'s "presentation-ready... `app.js` renders it and re-derives none of it."
- **Alternatives grouped by area**: confirmed at lines 1680-1684 (`(alternatives[c.area] ??= []).push(c)`), and by test `alternatives are grouped by area, excluding Plan A, order preserved within each group` (line 2199), hand-traced: `plan-a`/North, `alt-north`/North, `alt-south`/South — `alternatives.North` correctly contains only `alt-north` (Plan A excluded) and `alternatives.South` only `alt-south`.
- **Six tests hand-verified by tracing arithmetic/logic against the actual code** (a mix of taxonomy, ordering, fallback-selection and refusal cases, per the assignment):
  1. `taxonomy: an upper-bound NONE is ranked, failing robust while the midpoint still evaluates to tight` (line 2006) — venue open `[0, 925)` on `2026-08-31`; `leaveAtMinutes=900`, band `10-30m` gives `mid=20, upper=30`. Mid arrival `920 < 925` (inside), upper arrival `930 >= 925` (no active period, `NONE`). Traced: this is a genuine upper-`NONE`/mid-active split, not vacuous; confirmed `2026-08-31` is in fact a Monday (`node -e "new Date('2026-08-31T00:00:00Z').getUTCDay()"` → `1`), matching the fixture's `regular.mon` assumption.
  2. `ranking order: overall_tier decides first — robust beats tight regardless of every later key` (line 2084) — confirmed `2026-08-31` and `2024-01-01` (used elsewhere as `BASE_CONTROLS.departureDate`) are both Mondays, so the "robust" venue's default `regular.mon` always-open fixture genuinely resolves on these dates rather than silently falling through to the `closed()` default every other weekday gets. The robust venue (`preference: 99`, `baseline: "poor"`) genuinely outranks the tight venue (`preference: 1`, `baseline: "dependable"`) only because tier is compared first — a real test of key-1 dominance, not a coincidence of the other keys already agreeing.
  3. `fallback selection: among several fallback links, the one with the best backup_strength wins` (line 2159) — traced `weak-fallback` (`baseline: "poor"`, no busyness override) to `seat_confidence` staying `poor` (busyness `unknown` never promotes to `typical`, per the untouched `resolveSeatConfidence`), below `PLAN_B_MIN_CONFIDENCE` (`mixed`), giving `strength: "none"`; `strong-fallback` (`baseline: "dependable"`, always-open) genuinely reaches `strength: "strong"`. `compareFallbacks`'s key 1 (`backup_strength`) correctly picks `strong-fallback`. Reproduced independently in a scratch script (see below) rather than trusting the assertion alone.
  4. `refusals: no candidate reaching mixed confidence yields noLowRiskOption...` (line 2178) — `baseline: "poor"`, no `popularTimes` → busyness `unknown` → confidence stays `poor`, below `mixed` → `qualifying.length === 0` → `noLowRiskOption: true`. Confirmed via the scratch reproduction below (see the `qualifying` filter at line 1686).
  5. `refusals: every qualifying candidate being unverified yields noVerifiedReturn with the session end time` (line 2185) — hand-computed the session end: `leaveAtMinutes 1200 + travelMid 7 (floor((5+10)/2)) + duration 180 = 1387` minutes `= 23:07`, matching the asserted `"23:07"` exactly; confirmed the venue's only access mode (`transit`) is schedule-bound with no `return_transport` block at all, so `resolve_return_service` yields `MISSING` on every admissible mode, outside the core span (07:00-21:30) and the pre-dawn gap, yielding `unverified` — not a stub value.
  6. `control resolution: raining substitutes the outbound mode and its access band per venue` (line 2212) — dry run reads `access.origin_a.cycle` (`5-10m`, mid `7`); wet run substitutes via `wet_weather_mode.origin_a.cycle: "transit"` and reads `access.origin_a.transit` (`20-30m`, mid `25`). Both figures match the assertions exactly, and the two runs genuinely produce different values, proving the substitution is live rather than a no-op.
  None of the six were vacuous or mistargeted.
- **`node --test tests/js/*.test.js`**: `# tests 173`, `# pass 173`, `# fail 0`, `# cancelled 0`, `# skipped 0`. `173 - 142 = 31` new tests, all green, consistent with the brief's "all prior 142 plus new taxonomy/ordering/refusal cases green."
- **`.venv/bin/pytest tests/python/ -q`**: `78 passed`, matching the expected no-regression baseline (no Python file touched by this diff, confirmed by `git status --short` above).

### A defect found by independent behavioural reproduction, not by reading alone

While tracing `selectPlanBFallback` (line 1473) against `PLAN.md`'s explicit rules, I found that it looks up a fallback venue directly from `venueById` — built from the **entire, unfiltered** `venues` array (line 1574) — and calls `evaluatePlanBFallback` on it with no re-application of **any** of the per-venue removal checks the main per-venue loop applies to a venue being considered as Plan A (`business_status`, `return_transport_status` STEP 0, `preference` validity). Read alone this looked like it might be intentional (the settled `evaluatePlanBFallback` primitive does its own hours/return resolution), so I wrote a scratch reproduction (`node`, importing `rankVenues` directly, fixtures built the same way `tests/js/ranking.test.js`'s `fullVenue`/`makeVenue` do) to check behaviourally rather than assert from reading:

- A fallback venue with `business_status: "CLOSED_PERMANENTLY"` is selected as Plan B with `backupStrength: "strong"` and `planB: {venueId: "closed-fb", overallTier: "robust", strength: "strong", ...}`. `PLAN.md` line 1602 states, in the taxonomy table itself, that a non-`OPERATIONAL` venue is "**never Plan A or Plan B**" — this is a direct, literal contradiction of an explicit written rule, not an inference.
- A fallback venue with `return_transport_status: {state: "invalid", reason: "malformed band"}` — the exact stamp `IMP-009`'s `validate_return_transport` bridge produces for a hand-maintained typo — is likewise selected as Plan B with `overallTier: "robust"`, `strength: "strong"`, using its own (flagged-broken) `return_transport` data to compute that tier. `PLAN.md`'s "STEP 0" section (`sed -n '995,1075p' PLAN.md`) states this check is "a precondition on the venue, not a branch inside a bound," evaluated "once before either bound," and that "absent counts as invalid... the data is unvalidated and must not be ranked" — worded as a property of the venue, not of "the venue when it is Plan A." The `rankVenues` per-venue loop enforces STEP 0 only for the venue being considered as a **primary** candidate (line 1603-1611); `selectPlanBFallback` never applies it to the fallback venue at all.
- The same reproduction with `return_transport_status` **absent entirely** (the "never validated, must fail closed" case `CLAUDE.md` calls "the one place the return design fails closed") also selects the venue as a `strong` Plan B.

Scratch script and full output are not part of the repository; the reproduction imported `rankVenues` from `web/ranking.js` directly with fixtures built identically to the ones already in `tests/js/ranking.test.js` (same `fullVenue`/`makeVenue` shape), so this is not an artifact of a malformed fixture on my part — it used the project's own existing fixture pattern.

**Practical consequence**: a venue that the tool has explicitly and separately decided is unsafe to visit or use for a return-transport calculation (closed for good, or carrying return data flagged broken or never checked) can still be shown to the user as "Plan B" with the most confident possible label (`strong`), for exactly the class of failure ("closes when open? gets you home when it says it will?") this whole feature exists to prevent. This is not one of the 14 taxonomy rows (the taxonomy is stated for the primary-candidate path only) so no taxonomy-row test would have caught it, and no test in the diff exercises a fallback venue with a non-`OPERATIONAL` `business_status` or a broken/absent `return_transport_status` — I found no such test anywhere in `tests/js/ranking.test.js`.

### A second, narrower ownership-order deviation, also confirmed behaviourally

`PLAN.md`'s "One entry point, pure, whole-dataset" section states the ownership order as: 3. travel-band parsing and per-venue arrivals, **then** 4. return-status STEP 0 removal. In the code (`web/ranking.js:1592-1617`), the access-entry existence/explicit-null checks run first (part of travel-band handling), but the **string-format parsing** of the band value (`normaliseTravelBand(accessEntry.band)`, line 1613) runs **after** the `return_transport_status` STEP 0 check (line 1603-1611) — the reverse of the stated order. Reproduced with a primary venue carrying both a malformed access band (`"not-a-band"`) and a broken `return_transport_status`: the venue is removed with `kind: "return_data_broken"`, never reaching the `access_band_invalid` branch. This does not change which group the venue lands in (both paths end in "unranked removal") — only which diagnostic `reason` string is surfaced when two defects coincide — so it is far lower-impact than the Plan B finding above, but it is a confirmed, literal deviation from an acceptance criterion ("ownership order exactly as `PLAN.md` states").

### Could not verify

- Whether the actual model/effort that produced this diff matches `HANDOFF.md`'s claimed `claude_sonnet`, effort high — not observable from this subagent, as with every prior gate (`Runtime model verification` constant, `WORKFLOW.md`).
- Whether `app.js` (not yet written, scope-excluded) will in fact render the two refusal messages with the exact wording `PLAN.md` quotes — `rankVenues` supplies the data (`noLowRiskOption` boolean, `noVerifiedReturn` formatted time or `null`) but not the literal sentence text, and the acceptance criteria's "both refusal messages present, worded distinctly" is only checkable at the data level from this assignment's scope.

### Not asked to check

- `app.js`/frontend, the HTML generator, the coarsening stage, refresh orchestration, `Makefile`, and live refresh — all explicit scope exclusions, confirmed not touched (`git diff --stat` above).
- Whether the six named settled primitives (`resolveOverallFeasibilityAtArrivals`, `resolveBackupStrength`, `evaluatePlanBFallback`, `resolveBusynessBand`, `resolveSeatConfidence`, `validateReturnTransport`) are themselves correct — confirmed unmodified by this diff, but their own correctness was gated in earlier assignments and was not re-examined here beyond confirming they are read-only imports in the new code.
- Prose quality, formatting, or style beyond what the acceptance criteria explicitly require.

### Status

`GATE_FAIL` — `git status`/`git diff` scope is clean and the six settled primitives are genuinely untouched; all 14 taxonomy rows have their own passing entry-point test; the 8-key ranking order and 7-key fallback order are implemented as specified (with one imprecise-but-inconsequential citation in the key-6-omission comment); `bicycle_with_you` is threaded correctly; `node --test` is 173/173 and `.venv/bin/pytest` is 78/78 with no regression. But `selectPlanBFallback` (`web/ranking.js:1473-1514`) fetches a fallback venue from the whole, unfiltered `venueById` map and evaluates it with no re-application of the `business_status` or `return_transport_status` STEP 0 checks that the same function applies to every candidate on the primary path (lines 1587-1590, 1603-1611). This was confirmed by direct behavioural reproduction, not inference: a `CLOSED_PERMANENTLY` venue and a venue whose `return_transport_status` is `invalid` (or entirely absent) can each be selected and surfaced as a `strong` Plan B. `PLAN.md` states explicitly, in the taxonomy table itself, that a non-`OPERATIONAL` venue is "never Plan A or Plan B" (line 1602), and separately (lines 1055-1062) that the STEP 0 return-validation precondition applies "on the venue, not a branch inside a bound" and that an absent or invalid stamp "must not be ranked" — worded as a venue-level guarantee, not one scoped to the primary-candidate path only. This is a genuine correctness defect against the acceptance criteria's ownership-order requirement (STEP 0 removal is supposed to run "before... evaluation," and Plan B evaluation is evaluation of a venue the same way Plan A's is) and against `PLAN.md`'s own explicit prose, is reproducible on demand, and is not covered by any test in the diff — the taxonomy tests all target the primary-candidate path, and none of the 31 new tests constructs a fallback venue with a broken `business_status` or `return_transport_status`. A second, narrower ownership-order deviation was also confirmed (return-status STEP 0 removal runs before, not after, travel-band **string** parsing completes for the primary venue), with lower practical impact (it changes only a diagnostic's `reason` string, never a venue's group placement) but is still a literal deviation from the stated order. Both findings are reported as facts for the primary and reviewer to act on; per the pre-gate protocol this record recommends nothing and advances no lifecycle state.

---

## Gate invocation 2 — 2026-09-03

- **Gate route**: `claude_sonnet`, effort high, fresh context (no memory of invocation 1's own run — read its record from disk)
- **Brief source**: this invocation's own assignment prompt, describing the primary's claimed fixes to both invocation-1 findings; verified against the actual code and tests rather than trusted from that description

### Checks run

- **`git status --short` / `git diff --stat`**: `M HANDOFF.md` (28 lines), `M reviews/LEDGER.md` (1 line), `M tests/js/ranking.test.js` (501 lines), `M web/ranking.js` (378 lines), plus untracked `reviews/IMP-010-gate.md` (this file, the assignment's own gate record). Identical path set to invocation 1, both diffs having grown (441→501, 368→378) with this round's fix and its three new tests; nothing outside the named scope touched.
- **`selectPlanBFallback` read in full, independently, at its current location (`web/ranking.js:1473-1524`)**: both STEP 0 gates are present exactly where invocation 1's finding said they were missing:
  - `if (fallbackVenue.business_status !== "OPERATIONAL") continue;` (line 1487)
  - `const fallbackReturnStatus = fallbackVenue.return_transport_status; if (!fallbackReturnStatus || fallbackReturnStatus.state !== "ok") continue;` (lines 1488-1489)
  - Confirmed the second gate reads `fallbackVenue.return_transport_status`, not `venue.return_transport_status` (the primary being ranked) — the two identifiers are visually similar but the code reads the right one.
  - Confirmed the loop is a `for (const link of links)` and both `continue` statements sit inside it, before `evaluatePlanBFallback` is called and before the `evaluated.push(...)` — a `continue` here skips only the current link's evaluation and push, then resumes the loop at the next link exactly as intended (no off-by-one, no early exit).
  - `evaluated.length === 0 → return null` (line 1521) still holds when every link is gated out, so a primary venue whose only fallback fails a gate correctly reports `planB: null` / `backupStrength: "none"`, not a stale or default "strong".
- **Per-venue loop in `rankVenues` re-read in full (`web/ranking.js:1592-1627`) for the ownership-order fix**: `normaliseTravelBand(accessEntry.band)` (line 1613, string-format parsing) now runs and can `continue` with `kind: "access_band_invalid"` (lines 1614-1617) *before* `venue.return_transport_status` is read at all (lines 1619-1627) — the reverse of invocation 1's finding, and now matching `PLAN.md`'s stated step 3-before-step-4 order.
- **Independent scratch reproduction**, written from scratch (not the primary's), importing `rankVenues` directly from `web/ranking.js` with a hand-built fixture matching `tests/js/ranking.test.js`'s `fullVenue`/`makeVenue` field shapes (in particular the `venue.hours = { current_hours_valid_from, current_hours_valid_through, current_hours_by_date, regular_hours }` nesting, read from the real `makeVenue` at `tests/js/ranking.test.js:62-76` before I built mine, since guessing the shape wrong would have produced false negatives). Ran four cases against a Monday-always-open, walk-accessible, dependable-baseline primary venue with one fallback link:
  - (a) fallback `business_status: "CLOSED_PERMANENTLY"` → `backupStrength: "none"`, `planB: null`.
  - (b) fallback `return_transport_status: {state: "invalid", reason: "typo"}` → `backupStrength: "none"`, `planB: null`.
  - (c) fallback `return_transport_status` deleted entirely → `backupStrength: "none"`, `planB: null`.
  - (d) control: a good fallback (`OPERATIONAL`, `return_transport_status: {state:"ok"}`) → `backupStrength: "strong"`, `planB: {venueId: "fb", overallTier: "robust", strength: "strong", ...}` — confirms the fix did not overcorrect into rejecting a valid fallback.
  All four outcomes match the fix's intent exactly; none of invocation 1's three reproduced defects survive.
- **Three new tests in `tests/js/ranking.test.js` read in full and checked for vacuity**:
  - `"fallback selection: a non-OPERATIONAL fallback venue is never selected as Plan B"` (line 2198) — constructs a real `CLOSED_PERMANENTLY` fallback, asserts `backupStrength === "none"` and `planB === null`. Genuine, not vacuous.
  - `"fallback selection: a fallback venue with a broken or absent return_transport_status is never selected as Plan B"` (line 2211) — constructs both sub-cases in one test body: `return_transport_status: {state: "invalid", ...}` and, separately, the field deleted entirely (`delete absentReturnFallback.return_transport_status`), each asserting `backupStrength === "none"` / `planB === null`. Both sub-cases are real, distinct constructions, not a single case dressed up as two.
  - `"taxonomy: a malformed access band is diagnosed even when return_transport_status is also broken, per the stated step order"` (line 1983) — builds a single venue carrying **both** defects simultaneously (`access.origin_a.walk.band: "not-a-band"` and `returnStatus: {state: "invalid", ...}`), asserts the removal's `kind` is `"access_band_invalid"`. Traced this against the current code: with both defects present, execution reaches `normaliseTravelBand` first (line 1613) and removes the venue there, so `return_transport_status` is never read for this venue in this test — genuinely exercises the corrected order and would fail (surface `"return_data_broken"` instead) against invocation 1's code.
  - None of the three is vacuous; each constructs the exact failure condition it claims to.
- **`node --test tests/js/*.test.js`**: `# tests 176`, `# pass 176`, `# fail 0`, `# cancelled 0`, `# skipped 0`. `176 - 173 = 3` new tests, matching the brief's expected count exactly; the other 173 remain green (no regression).
- **`.venv/bin/pytest tests/python/ -q`**: `78 passed`, matching the expected no-regression baseline (no Python file touched by this diff).
- **General sanity pass over `rankVenues`/`selectPlanBFallback`/`compareCandidates`/`compareFallbacks`** (not exhaustive, per the assignment): noticed that inside `selectPlanBFallback` itself, the three per-link checks run in the order `business_status` → `return_transport_status` → `normaliseTravelBand(link.travel_band)` — the reverse of the step-3-before-step-4 order just fixed in the main loop. However, unlike the main loop, `selectPlanBFallback` never records a diagnostic `reason`/`kind` for a gated-out link — a failing link is simply dropped from `evaluated` with no observable trace of *which* gate rejected it. Since invocation 1's step-order finding was explicitly scoped to "only affected which diagnostic reason string was reported," and no diagnostic string exists here at all, this order has no observable behavioural consequence in the current code and is not a defect in its own right — flagged only for completeness, not as a new finding.

### Could not verify

- Whether the actual model/effort that produced this diff matches `HANDOFF.md`'s claimed `claude_sonnet`, effort high — not observable from this subagent, as in invocation 1.
- Whether `app.js` (not yet written, scope-excluded) renders the taxonomy/refusal/Plan-B outcomes with the exact wording `PLAN.md` quotes — out of this assignment's scope, unchanged from invocation 1.

### Not asked to check

- Everything invocation 1 already passed and this brief did not ask to re-verify: the 14-row taxonomy, the 8-key ranking order, the 7-key fallback order (including its previously-noted imprecise-but-inconsequential key-6-omission comment citation), `bicycle_with_you` threading, the refusal-message field split, and area grouping. None of these paths were touched by this round's diff (confirmed by `git diff` scope above), so they were not re-read line-by-line this round.
- `app.js`/frontend, the HTML generator, the coarsening stage, refresh orchestration, `Makefile`, live refresh — explicit scope exclusions, confirmed not touched.
- Prose quality, formatting, or style beyond what the acceptance criteria explicitly require.

### Status

`GATE_PASS` — both defects from invocation 1 are fixed and independently re-confirmed. `selectPlanBFallback` (`web/ranking.js:1473-1524`) now applies both the `business_status !== "OPERATIONAL"` gate and the `return_transport_status` STEP 0 gate to each fallback venue before evaluating it, reading `fallbackVenue.return_transport_status` (not the primary's), with `continue` correctly scoped inside the `for` loop; my own from-scratch reproduction confirms a `CLOSED_PERMANENTLY` fallback, an `invalid` return-status fallback, and an absent-return-status fallback are all now excluded (`backupStrength: "none"`, `planB: null`), while a genuinely good fallback is still selected normally (`strong`) — the fix did not overcorrect. The main per-venue loop's ownership order is also corrected: `normaliseTravelBand`'s string-format parsing (`web/ranking.js:1613`) now runs and can remove a venue with `kind: "access_band_invalid"` before `return_transport_status` is read (line 1619), matching `PLAN.md`'s stated step-3-before-step-4 order; the new test constructing both defects simultaneously genuinely exercises this and would have failed against invocation 1's code. All three new tests in `tests/js/ranking.test.js` are non-vacuous, each constructing the exact failure condition it claims to test. `node --test tests/js/*.test.js` is 176/176 (173 prior + 3 new, no regression) and `.venv/bin/pytest tests/python/ -q` is 78/78 (no regression). A minor, purely cosmetic order note was found during the general sanity pass (`selectPlanBFallback`'s own three gates run business_status → return_transport_status → travel_band, the reverse of the just-fixed main-loop order) but it has no observable behavioural effect, since no diagnostic reason string is ever emitted for a gated-out fallback link — reported for completeness, not as a defect. Per the pre-gate protocol this record recommends nothing and advances no lifecycle state.

---
