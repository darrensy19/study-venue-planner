# Gate record: IMP-022

## Gate invocation 1 — 2026-09-06

- **Gate route**: `claude_sonnet`, effort high, fresh context (pre-gate subagent, no memory of the primary's own reasoning)
- **Brief source**: generated from `HANDOFF.md` (mechanical brief, `.cross-agent-workflow/gate_brief.py HANDOFF.md`, reproduced verbatim in the invocation prompt)
- **Baseline commit**: `cc93fa5` (current tip; the reviewed diff is uncommitted in the working tree)

### Checks run

- **Diff scope confirmed**: `git status --short` at baseline `cc93fa5` shows `web/ranking.js`, `build/refresh.py`, `tests/js/ranking.test.js`, `tests/js/app.test.js`, `tests/python/test_refresh.py` modified; `build/outbound_validator_bridge.py`, `build/validate_outbound_transport.mjs`, `tests/python/test_outbound_validator_bridge.py` untracked/new; plus `BACKLOG.md`, `HANDOFF.md`, `reviews/LEDGER.md` (bookkeeping, outside declared scope, not audited for prose accuracy) and `web/index.html` (generated artifact, checked for offline reproducibility below). An untracked `.claude/` directory is also present — outside the declared artifact under review, not inspected. This matches the brief's stated file list exactly.
- **Governing spec read in full**: `PLAN.md`'s "Getting there: outbound-mirror transport" (lines 1525-1775) — the exact pseudocode algorithm for `outbound_admissible`, the data contract, the ownership split (`validate_outbound_transport` / `resolve_outbound_service` / `outbound_admissible`), the pessimistic-lower-edge rule, and the two-label exclusion-reason mapping. `WORKFLOW.md`'s "The gate lives inside `draft`" and "Anti-laundering" sections read in full for the gate's own contract. `reviews/IMP-021-gate.md` read in full as a style/rigor template only (not reused for content).
- **Full diff read in full**: `web/ranking.js` (236 diff lines), `build/refresh.py` (56 diff lines), `tests/js/ranking.test.js` (452 diff lines), `tests/js/app.test.js` (15 diff lines), `tests/python/test_refresh.py` (47 diff lines), plus the three new files `build/outbound_validator_bridge.py` (120 lines), `build/validate_outbound_transport.mjs` (30 lines), `tests/python/test_outbound_validator_bridge.py` (146 lines) — every line, not a sample.
- **`SERVICE_*` rename verified complete**: `grep -rn "RETURN_CORE_FROM_MINUTES\|RETURN_CORE_UNTIL_MINUTES\|RETURN_SERVICE_DAY_START_MINUTES" web/ tests/ build/` returns nothing — the rename to `SERVICE_CORE_FROM_MINUTES`/`SERVICE_CORE_UNTIL_MINUTES`/`SERVICE_DAY_START_MINUTES` is clean with no stray reference to the old names anywhere, and `resolveReturnBound`/`serviceDateFromAbs`/`normaliseEdge` all read the renamed constants. `RETURN_TOLERANCE_MINUTES` and `RETURN_CYCLE_LATEST_MINUTES` are confirmed unrenamed, per the acceptance criteria's explicit exception.
- **`resolveOutboundService` mirrors `resolveReturnService` exactly**: read both functions side by side (`web/ranking.js:293-316` for the return side, `:1536-1560` for the outbound side). Identical structure — same holiday→by_weekday→default→MISSING precedence, same `Object.prototype.hasOwnProperty` guards, same "entry with no `last_departure_band` is missing, never malformed" rule, same `normaliseBand` call only on a present entry — differing only in reading `outbound_transport[origin][mode]` vs `return_transport[destination][mode]`, and both reading the shared `holiday_return_policy` (no `holiday_outbound_policy` exists anywhere in the diff or in `data/venues_meta.json`, confirmed by `grep -c "outbound_transport" data/venues_meta.json` returning `0` — the hand-curation is genuinely absent, matching the brief's "blocked, tracked separately" note).
- **`outboundAdmissible` matches PLAN.md's pseudocode precisely**: step 1 (schedule-free, `SCHEDULE_FREE_MODES.has(mode)`, no `access[origin]["cycle"]` re-check — correct, since the precondition is established by the caller, verified below) → step 2 (core span, inclusive both ends: `clock >= SERVICE_CORE_FROM_MINUTES && clock <= SERVICE_CORE_UNTIL_MINUTES`) → step 3 (pre-dawn, hard-filtered with no data read) → step 4 (`resolveOutboundService`, consulted only here). The pessimistic edge is the band's **lower** (`lo`) edge (`leaveAtAbs > absMinutes(serviceDate, resolved.lo)`), matching PLAN.md's stated "earlier edge is pessimistic for a last departure" rule exactly — read directly, not inferred from a comment.
- **Precondition traced at the call site**: read `rankVenues` (`web/ranking.js:1998-2127`) in full. The order is: preference validation → hours-source-failure diagnosis → `business_status` → `access[origin][resolvedMode]` hard filter (line 2060, hard-filters if the key is absent) → travel-band presence/validity (2064-2074) → **the new outbound-admissibility block (2076-2093)** → `return_transport_status` → feasibility. This confirms `access[origin][resolvedMode]` is already known present and non-null, and the travel band already validated, by the time `outboundAdmissible` runs — exactly the precondition its own docstring and PLAN.md state, and exactly why `SCHEDULE_FREE_MODES.has(mode)` alone is sufficient for the "cycle" case with no separate `access` re-check.
- **Ordering matches PLAN.md's "before any hours or return-leg evaluation"**: confirmed by the line-number trace above — the outbound check runs before `return_transport_status` (2095) and before `resolveOverallFeasibility` (2110).
- **`outbound_transport_status` never read by `outboundAdmissible`**: confirmed by reading the function body — it reads only `resolveOutboundService`'s return value, never `venue.outbound_transport_status` in any form. Independently confirmed by the "tripwire" test pattern in `tests/js/ranking.test.js` (a `venue.outbound_transport = tripwireOutboundTransport()` object whose `home`/`office` getters throw if ever read) passing for the schedule-free, core-span, and pre-dawn branches, and by direct tests asserting an `invalid` `outbound_transport_status` stamp does not gate a schedule-free or core-span candidate.
- **Two-label mapping verified**: `pre_dawn_gap`/`missing_data`/`after_last_departure` all produce `label: "outbound_gap"`; `invalid_metadata` alone produces `label: "outbound_data_error"` — read directly in `outboundAdmissible`'s four return statements, matching PLAN.md's stated mapping exactly.
- **`validateOutboundTransport` walks every reachable entry, per-origin/mode, never gating**: read the full function (`web/ranking.js:1660-1697`) — iterates every origin, every mode, `default` plus every `by_weekday` key, calling `normaliseBand` only on entries that actually have `last_departure_band` (mirrors `resolveOutboundService`'s own "missing, never malformed" rule for a bandless entry). Confirmed never referenced from `outboundAdmissible` or anywhere else in the ranking path (`grep -n "outbound_transport_status" web/ranking.js` shows it only written, in `rankVenues`... actually not even written there — it is written only in `build/refresh.py`, never read in `web/ranking.js` at all).
- **`build/refresh.py` step 6b**: reads `venues_meta_path` (not an in-memory object) via the new `build/outbound_validator_bridge.py`, mirroring step 6's `validate_return_transport` call exactly — same signature shape, same unconditional/mandatory placement, same "a broken bridge propagates uncaught" comment and behavior (no `try/except` wraps the call). Stamps `outbound_transport_status` onto every venue and reports it in the `report` dict and CLI diagnostics.
- **`build/outbound_validator_bridge.py` and `build/validate_outbound_transport.mjs`**: read both files in full and compared line-by-line against `build/return_validator_bridge.py` / `build/validate_return_transport.mjs`. Structurally identical patterns (subprocess invocation, `BridgeError` fail-closed on Node-missing/nonzero-exit/non-JSON/wrong-shape/missing-venue-id, "writes no file" contract) adapted only for the outbound shape (`{"rollup", "perOriginMode"}` instead of `{"state"}`), including a rollup/per-entry **consistency check** (`_is_valid_status` rejects a rollup that disagrees with whether any per-origin/mode entry is actually invalid) that the return-side bridge has no equivalent of, since the return side has no rollup concept.
- **`.venv/bin/pytest tests/python/ -q`**: `208 passed in 3.28s` — matches `HANDOFF.md`'s claimed 208/208 exactly.
- **`node --test tests/js/*.test.js`**: `# tests 284`, `# pass 284`, `# fail 0` — matches `HANDOFF.md`'s claimed 284/284 exactly.
- **`make generate` offline reproducibility**: ran twice with `http_proxy`/`https_proxy` pointed at `127.0.0.1:9` (unlistening, so any real network call fails loudly rather than silently succeeding). Both runs produced `web/index.html` with identical MD5 `c987a069a106b20246e77fa7765a87fd`; `diff` between the two fresh runs was empty, and `diff` between either fresh run and the pre-existing working-tree `web/index.html` (backed up first) was also empty — the checked-in file is exactly what `make generate` reproduces deterministically, offline, from the current `web/ranking.js`. `grep -c` confirms `outboundAdmissible`/`resolveOutboundService`/`validateOutboundTransport` are present (11 occurrences) inside the regenerated, inlined `<script type="module">` block.
- **`app.js` scope-exclusion claim verified literally true, not just documented**: `git diff cc93fa5 -- web/app.js` is 0 lines. Read `renderRemovalNotice` (`web/app.js:300-302`) — it reads only `removal.disambiguatedLabel` and `removal.reason`, with no branching on `removal.kind` at all, so the new `"outbound_gap"`/`"outbound_data_error"` `kind` values render through the pre-existing generic path with zero code change required. This is a real structural reason the exclusion "no UI/`app.js` change" claim holds, not merely a coincidence that no change was made.
- **`tests/js/app.test.js`'s one-line change is necessary and sufficient**: the only pre-existing `app.test.js` test using `mode: "transit"` at a leave time outside the core service span (`no_verified_return`, `leaveAtMinutes: 1350` = 22:30) needed a `venue.outbound_transport` record added, or the new hard filter would exclude it before it ever reached the return-leg logic the test targets. Confirmed `CONTROLS.mode` defaults to `"walk"` (schedule-free, unaffected) and no other `app.test.js` test uses `"transit"` — so no other pre-existing test could have silently broken from this new filter without a corresponding fixture fix, and none did (284/284 green).
- **Ledger and blocked-scope claims verified**: `reviews/LEDGER.md`'s tail carries the `IMP-022` allocation row. `data/venues_meta.json` contains zero occurrences of `"outbound_transport"` — the hand-curation genuinely has not happened, consistent with `HANDOFF.md`'s `Blocked` field and the brief's instruction not to treat its absence as a defect.

### Non-vacuity spot-checks (reverted, reran, restored — 6 performed, 3+ required)

Backed up `web/ranking.js`, `build/refresh.py`, and `build/outbound_validator_bridge.py` to scratch copies before any edit. Every restoration below was verified by `diff` against that backup (empty) and, cumulatively, by a final `node --test tests/js/*.test.js` (284/284) and `.venv/bin/pytest tests/python/ -q` (208 passed), plus a final `git status --short` matching the pre-check working tree exactly.

1. **Pre-dawn gap disabled** (commented out the `SERVICE_DAY_START_MINUTES <= clock < SERVICE_CORE_FROM_MINUTES` branch in `outboundAdmissible`, letting it fall through to consult `resolveOutboundService`): reran `node --test tests/js/ranking.test.js` — **1 failure**, `outboundAdmissible: the pre-dawn gap is excluded unconditionally, with zero outbound_transport reads` (262/263 passed). Restored; `diff` against backup empty.
2. **`outbound_transport_status` made to gate ranking** (added a check at the top of `outboundAdmissible` that excludes on `venue.outbound_transport_status?.[origin]?.[mode]?.state === "invalid"`, directly violating "never gates ranking, at any granularity"): reran — **2 failures**, both of the `outbound_transport_status is never read...` tests (schedule-free and core-span candidates) (261/263 passed). Restored; diff empty.
3. **Two-label distinction broken** (`invalid_metadata` changed to emit `label: "outbound_gap"` instead of `"outbound_data_error"`): reran — **2 failures**, the direct `outboundAdmissible` unit test and the `taxonomy: an outbound_data_error exclusion... is distinctly labelled` pipeline test (261/263 passed). Restored; diff empty.
4. **`build/refresh.py` step 6b disabled** (commented out the `validate_outbound_transport` call and the stamping loop): reran `.venv/bin/pytest tests/python/ -q` — **10 failures**, including the new `test_outbound_transport_status_stamped_and_diagnostics_only_never_blocking_generation` and `test_broken_outbound_bridge_stops_refresh_before_atomic_replace`, plus 8 pre-existing tests that read `report["outbound_transport_status"]` or the venue's stamped field and now hit a `KeyError` (198/208 passed). This confirms the stamping step is load-bearing for both the new tests and several pre-existing ones. Restored; diff empty; full suite back to 208/208.
5. **Python bridge's malformed-status detection disabled** (`_is_valid_status` in `build/outbound_validator_bridge.py` changed to `return True` unconditionally, before its real checks): reran `tests/python/test_outbound_validator_bridge.py` — **7 failures**, all 7 parametrized cases of `test_malformed_per_venue_status_raises_bridge_error` (9/16 passed), confirming this test genuinely exercises real malformed-shape rejection through the real bridge subprocess, not a mocked stand-in. Restored; diff empty.
6. **Pipeline-integration diagnostic text for two of four reasons deleted — this is the finding below.** In `rankVenues`'s outbound-exclusion block, removed the `pre_dawn_gap` and `after_last_departure` keys from the `reasonText` lookup object (leaving only `missing_data` and `invalid_metadata`), so a venue excluded for either of those two reasons is still correctly removed from ranking (the `kind`/kind-based tests still pass) but its `removed[].reason` field silently becomes `undefined`. Reran the full suite: **0 failures, 284/284 passed.** No test anywhere exercises `rankVenues` (the pipeline-integration level, as opposed to the standalone `outboundAdmissible` unit tests) with a `pre_dawn_gap` or `after_last_departure` exclusion and asserts on the resulting `removed[].reason` text. Restored; diff empty.

### Findings

1. **Vacuous coverage: `pre_dawn_gap` and `after_last_departure` exclusions are never verified to produce a visible diagnostic through the actual pipeline (`web/ranking.js:2084-2092`).** PLAN.md's "Pipeline integration and exclusion reasons" requires "every outbound exclusion appears in a visible removal notice" — for all four reasons, not two. `tests/js/ranking.test.js` has direct `outboundAdmissible` unit tests for all four reasons (lines ~2081, ~2093, ~2103, ~2133), and full-pipeline (`rankVenues`) "taxonomy" tests for `missing_data` (line 2342) and `invalid_metadata` (line 2354) that check `removed[].kind`. **No taxonomy test exists for `pre_dawn_gap` or `after_last_departure`, and no test of any kind — unit or pipeline — ever asserts on `removed[].reason`'s actual text for any of the four reasons.** Spot-check 6 above deleted the diagnostic-text mapping for exactly those two reasons and the full 284-test suite still passed. The `web/ranking.js` code itself is correct on direct reading (all four keys are present with distinct, correct text in the current diff), but a future regression — a typo in a map key, an accidental deletion, a refactor that drops a case — would ship a venue rendered as `"<venue name>: undefined"` in the removal notice (per `app.js`'s generic `renderRemovalNotice`, which interpolates `removal.reason` directly with no fallback) with the full test suite staying green. This is the same defect class `reviews/IMP-021-gate.md` found three instances of in the prior slice (an untested branch or field that passes silently when broken), reproduced here for the first two of the four outbound exclusion reasons.

None of this is a functional defect in the shipped code as it stands: reading `web/ranking.js:2084-2092` directly, all four `reasonText` map entries are present, non-empty, and textually distinct, and I have no evidence any of the four is actually broken. This is a coverage gap against an acceptance criterion this project's own testing culture (and this gate's explicit spot-check mandate) treats as required evidence, not merely nice-to-have — consistent with the standard `reviews/IMP-021-gate.md` applied to an equivalent gap in the prior slice, which returned `GATE_FAIL` for three coverage gaps with likewise-no-functional-defect-found code underneath them.

### Could not verify

None. Every item in the mechanical brief's "Required verification" was independently reproduced first-hand (`.venv/bin/pytest`, `node --test`, double offline `make generate` with a byte-identical three-way match), and every acceptance-criterion clause in PLAN.md's "Getting there: outbound-mirror transport" was traced to specific code and either confirmed correct by direct reading and cross-comparison with the mirrored return-leg functions, or spot-checked by breaking the mechanism and observing the expected test failure (or, for the finding above, the expected failure's absence).

### Not asked to check

- **`BACKLOG.md`, `HANDOFF.md`, `reviews/LEDGER.md`**: outside the declared "Artifact under review," per the brief's explicit instruction. Not audited for prose accuracy.
- **The untracked `.claude/` directory**: not part of the declared artifact under review; not inspected.
- **`data/venues_meta.json`'s `outbound_transport` hand-curation**: explicitly out of scope per the brief — blocked on the user, tracked separately in `HANDOFF.md`'s `Blocked` field. Its absence is not treated as a defect, per instruction.
- **The return-leg's own already-tested behavior** (`resolveReturnService`, `resolveReturnBound`, `validateReturnTransport`, etc.) — confirmed unchanged in logic (only the `RETURN_CORE_*`/`RETURN_SERVICE_DAY_START_MINUTES` constant renames touch this code, verified textually identical otherwise) and already gated under prior slices; re-verifying its internals is outside this assignment's scope, per the stated scope exclusion "no change to `return_transport`/return-leg logic."
- **UI/`app.js` progressive-disclosure and hierarchy work** — explicitly Slice 4, per the scope exclusions.
- **The Slice-3 shadow-trial declaration process** (`HANDOFF.md`'s "Shadow trial" field, `reviews/IMP-022.md`) — this is a reviewer-round artifact and process concern, outside the pre-gate's single permitted write path and outside what the gate brief asked verified.
- **Manual iPhone / Home Screen acceptance checklist** — out of scope for an automated gate per `CLAUDE.md`'s own testing section.

### Status

GATE_FAIL

## Gate invocation 2 — 2026-09-06

- **Gate route**: `claude_sonnet`, effort high, fresh context (pre-gate subagent, no memory of invocation 1's own reasoning process — only this file's written record)
- **Brief source**: generated from `HANDOFF.md` (mechanical brief, `.cross-agent-workflow/gate_brief.py HANDOFF.md`, reproduced verbatim in the invocation prompt) — identical in substance to invocation 1's brief, since the objective, acceptance criteria and required verification are unchanged; only `HANDOFF.md`'s `Gate result` and `Next action` fields differ.
- **Baseline commit**: `cc93fa5` (current tip; the reviewed diff, including invocation 1's finding and the primary's correction, is still uncommitted in the working tree — confirmed via `git rev-parse HEAD` and `git log -1`, matching the brief exactly)

### Checks run

- **Diff scope confirmed unchanged from invocation 1, plus no new files**: `git status --short` shows the identical file list invocation 1 recorded — `web/ranking.js`, `build/refresh.py`, `tests/js/ranking.test.js`, `tests/js/app.test.js`, `tests/python/test_refresh.py` modified; `build/outbound_validator_bridge.py`, `build/validate_outbound_transport.mjs`, `tests/python/test_outbound_validator_bridge.py` untracked; `BACKLOG.md`, `HANDOFF.md`, `reviews/LEDGER.md` bookkeeping (outside the declared artifact); `web/index.html` generated; plus the untracked `.claude/` directory (still outside scope, not inspected) and the now-present `reviews/IMP-022-gate.md` itself (this record). This confirms the correction was made entirely within the already-declared scope — no new file was added or removed to fix the finding.
- **`HANDOFF.md` read in full**: `Gate result` field accurately states invocation 1's outcome and the primary's claimed fix ("2 new taxonomy tests plus reason-text assertions added to the existing 2, RED-verified against a reproduction of the exact deleted mapping, then restored to GREEN (286/286)") without softening or reinterpreting the `GATE_FAIL` verbatim, matching `WORKFLOW.md`'s "primary copies that status verbatim" rule. `State` is still `draft`, correctly — the primary has not advanced the lifecycle itself.
- **Located the fix by its marker comment**: `grep -n "IMP-022-gate-F01" tests/js/ranking.test.js` finds one occurrence, a comment block at line 2370 immediately preceding the two new tests, explicitly naming the gap it closes and citing the exact spot-check that found it. Read in full alongside the two new tests and the two pre-existing taxonomy tests it references.
- **The 2 new taxonomy tests read in full** (`tests/js/ranking.test.js:2376-2403`): `"taxonomy: a pre_dawn_gap exclusion is an unranked removal with a visible diagnostic, reached through the actual pipeline"` (leaveAtMinutes 300/05:00, schedule-bound access only, no `outbound_transport`) and `"taxonomy: an after_last_departure exclusion is an unranked removal with a visible diagnostic, reached through the actual pipeline"` (a `20:00-20:05` band, leave at 22:00). Both call `rankVenues()` directly (not `outboundAdmissible()` standalone — this is the pipeline level invocation 1 found untested), both assert `removed.kind === "outbound_gap"`, and — the load-bearing addition — both assert `assert.match(removed.reason, /pre-dawn/)` and `assert.match(removed.reason, /after the last recorded departure/)` respectively against the actual `reason` string text, not merely its presence.
- **The 2 pre-existing taxonomy tests' diff confirmed to carry new reason-text assertions**: `"taxonomy: an outbound_gap exclusion (missing_data)..."` (line 2342) now includes `assert.match(removed.reason, /no departure recorded/)` (line 2352) and `"taxonomy: an outbound_data_error exclusion (invalid_metadata)..."` (line 2356) now includes `assert.match(removed.reason, /malformed/)` (line 2367) — read directly; invocation 1's record explicitly states these two tests previously checked only `removed[].kind`, so these two `assert.match` lines are the claimed addition. All four of `web/ranking.js`'s `reasonText` map entries (`web/ranking.js:2085-2090`) now have a corresponding pipeline-level regex assertion — read the map directly and cross-checked each of its four keys against the corresponding test file line: `no departure recorded` ↔ `missing_data`, `malformed` ↔ `invalid_metadata`, `pre-dawn` ↔ `pre_dawn_gap`, `after the last recorded departure` ↔ `after_last_departure`. No fifth, unmapped reason exists, and no reason is left without a regex assertion.
- **Baseline full-suite run, before any spot-check edit**: `.venv/bin/pytest tests/python/ -q` → `208 passed in 3.28s`. `node --test tests/js/*.test.js` → `# tests 286`, `# pass 286`, `# fail 0`. Both match `HANDOFF.md`'s claimed 208/208 and 286/286 exactly, and match invocation 1's own baseline count (which recorded 284/284 before the fix added 2 tests: 284 + 2 = 286, consistent).
- **Mandated RED-verification spot-check, performed independently** (backed up `web/ranking.js` to a scratch copy first): reproduced invocation 1's exact spot-check 6 — removed the `pre_dawn_gap` and `after_last_departure` keys from the `reasonText` object at `web/ranking.js:2085-2090`, leaving only `missing_data` and `invalid_metadata`. Reran `node --test tests/js/*.test.js`: **exactly 2 failures**, both newly added — `not ok 188 - taxonomy: a pre_dawn_gap exclusion is an unranked removal with a visible diagnostic, reached through the actual pipeline` and `not ok 189 - taxonomy: an after_last_departure exclusion is an unranked removal with a visible diagnostic, reached through the actual pipeline` (`# tests 286`, `# pass 284`, `# fail 2`). No other test failed — in particular, none of the pre-existing `outboundAdmissible` unit tests or the `missing_data`/`invalid_metadata` taxonomy tests were affected, confirming the two new tests are the ones — and the only ones — now catching this exact regression. This is the same deletion that left the full 284-test suite green under invocation 1's record; it now fails specifically and only where expected. Restored from the scratch backup; `diff` against the backup was empty; `git status --short` for `web/ranking.js` showed the identical single `M` line as before the edit; reran the full suite and confirmed `# pass 286`, `# fail 0`. **The primary's RED-verification claim is confirmed independently, not merely taken on trust.**
- **Non-vacuity spot-check A (own choosing): the Python bridge's rollup/per-entry consistency check, in the opposite direction from invocation 1's spot-check 5.** Invocation 1's spot-check 5 replaced `_is_valid_status` with `return True` unconditionally and observed all 7 parametrized `test_malformed_per_venue_status_raises_bridge_error` cases fail — but inspecting those 7 cases (`tests/python/test_outbound_validator_bridge.py:126-136`) shows only one exercises the rollup/per-entry consistency rule at all (`'{"rollup": "invalid", "perOriginMode": {}}'` — rollup says invalid with no invalid entries), and even that one is structurally malformed for an unrelated reason if perOriginMode also fails other checks. **No test anywhere constructs the opposite direction**: `rollup: "ok"` while a `perOriginMode` entry actually has `state: "invalid"`. Backed up `build/outbound_validator_bridge.py`, then narrowed `_is_valid_status`'s consistency line (`build/outbound_validator_bridge.py:71`) from `return rollup == ("invalid" if any_invalid else "ok")` to a version that only rejects `rollup: "invalid"` with no invalid entries, and unconditionally accepts `rollup: "ok"` regardless of `any_invalid`. Reran `.venv/bin/pytest tests/python/ -q`: **208 passed, 0 failed** — the full suite stays green with half the consistency check's logic inert. Restored from backup; `diff` empty; reran, confirmed `208 passed` again. **This is a genuine, independently-discovered coverage gap of the identical defect class invocation 1 found** (a mechanism exists in code, described in its own docstring as protecting against "corruption in the bridge itself," but only one of its two logical directions is ever exercised by a test) — see Findings below.
- **Non-vacuity spot-check B (own choosing): `make generate` offline reproducibility plus content verification of the new reason-text strings.** Backed up `web/index.html`, then ran `http_proxy=http://127.0.0.1:9 https_proxy=http://127.0.0.1:9 make generate` (unlistening proxy, so a real network call would fail loudly). Output MD5 `c987a069a106b20246e77fa7765a87fd` — byte-identical to the pre-existing working-tree `web/index.html` and to invocation 1's own recorded MD5 from its double-run reproducibility check, which is the expected result since the fix touched only `tests/js/ranking.test.js` (test-only, no production-code or generator-input change) and therefore cannot change the generated page. Confirmed all four new `reasonText` strings (`"no departure recorded for this mode from this origin"`, `"the recorded departure band is malformed"`, `"pre-dawn gap — no timetable is modelled"`, `"leave time is after the last recorded departure"`) are present in the regenerated `web/index.html`, and confirmed via a small Python parse check that the `<script type="module">` block and both `outboundAdmissible`/`validateOutboundTransport` function names are present and the file is otherwise structurally intact. No diff produced by this check (`make generate` overwrote `web/index.html` with byte-identical content, confirmed via `diff` against the pre-run backup — empty).
- **Full required verification reran one final time, after all spot-checks restored**: `.venv/bin/pytest tests/python/ -q` → `208 passed in 3.33s`. `node --test tests/js/*.test.js` → `# pass 286`, `# fail 0`. `git status --short` matches the exact file list recorded at the top of this invocation's Checks section, with no stray modifications from any spot-check.

### Findings

1. **Invocation 1's finding is genuinely resolved, not merely asserted resolved.** The primary's claimed fix — 2 new pipeline-level taxonomy tests for `pre_dawn_gap` and `after_last_departure`, plus reason-text regex assertions retrofitted onto the 2 pre-existing `missing_data`/`invalid_metadata` taxonomy tests — was independently verified by reproducing invocation 1's exact spot-check (deleting both reasonText map entries) and observing exactly 2 failures, both in the new/updated tests, with the rest of the suite (284 other tests) unaffected. All four of the four outbound exclusion reasons now have a pipeline-level (`rankVenues()`) test asserting on `removed[].reason`'s actual text, closing the gap invocation 1 identified. This finding is **resolved**.

2. **New finding, this invocation's own: the same defect class recurs in `build/outbound_validator_bridge.py`'s `_is_valid_status` (line 71), one file over in the same diff.** `_is_valid_status`'s own docstring states it exists to catch "a rollup that disagrees with its own per-entry data" — i.e. it is meant to fail closed if `validate_outbound_transport.mjs` (or a future edit to `validateOutboundTransport()`) ever emits `rollup: "ok"` while a `perOriginMode` entry is actually `state: "invalid"`, or vice versa. Of the 7 parametrized cases in `test_malformed_per_venue_status_raises_bridge_error` (`tests/python/test_outbound_validator_bridge.py:126-146`), only one touches this consistency rule at all, and only in the `rollup: "invalid"` direction. **No test constructs `rollup: "ok"` with an actually-invalid `perOriginMode` entry.** Spot-check A above confirmed this by disabling exactly that direction of the check — the full 208-test pytest suite still passed. As with invocation 1's finding, the code itself is correct on direct reading (the consistency check as written does reject both directions), and I have no evidence the bridge ever actually emits an inconsistent rollup today; this is a coverage gap against a fail-closed mechanism the code's own docstring treats as load-bearing, not a currently-live functional defect. A future edit to the Node-side rollup computation that silently reports `"ok"` while leaving a stale `state: "invalid"` entry underneath — the opposite-direction sibling of the one case the tests do cover — would ship with the full suite green. This finding is **new** as of invocation 2; it is not something invocation 1 raised or that the primary's correction touched, since the correction was scoped entirely to `tests/js/ranking.test.js`.

None of this is a functional defect in the shipped code as it stands, in either finding. Reading `build/outbound_validator_bridge.py:44-71` directly, the consistency check's logic is correct for both directions as written; only its test coverage is one-sided.

### Could not verify

None. Every item in the mechanical brief's "Required verification" was independently reproduced first-hand this invocation (`.venv/bin/pytest`, `node --test`, offline `make generate` with content verification), and both the primary's fix and this invocation's own spot-checks were verified by breaking the mechanism and observing the expected failure (or, for finding 2, the expected failure's absence), then restoring and re-confirming a clean, matching `git status`.

### Not asked to check

- **`BACKLOG.md`, `HANDOFF.md`, `reviews/LEDGER.md`**: outside the declared "Artifact under review," per the brief's explicit instruction. `HANDOFF.md` was read for its lifecycle/status fields per the pre-gate's own role, not audited for prose accuracy beyond that.
- **The untracked `.claude/` directory**: not part of the declared artifact under review; not inspected.
- **`data/venues_meta.json`'s `outbound_transport` hand-curation**: explicitly out of scope per the brief — blocked on the user, tracked separately in `HANDOFF.md`'s `Blocked` field. Its absence is not treated as a defect, per instruction.
- **The return-leg's own already-tested behavior**: unchanged since invocation 1 (no further edit touches it); not re-verified this invocation, consistent with the stated scope exclusion.
- **UI/`app.js` progressive-disclosure and hierarchy work**: explicitly Slice 4, per the scope exclusions.
- **The Slice-3 shadow-trial declaration process** (`HANDOFF.md`'s "Shadow trial" field, `reviews/IMP-022.md`): a reviewer-round artifact and process concern, outside the pre-gate's single permitted write path and outside what the gate brief asked verified.
- **Manual iPhone / Home Screen acceptance checklist**: out of scope for an automated gate per `CLAUDE.md`'s own testing section.
- **Every other area of the diff invocation 1 already traced line-by-line** (the `SERVICE_*` rename, `resolveOutboundService`'s mirroring of `resolveReturnService`, the two-label mapping, `outbound_transport_status` never gating, `build/refresh.py` step 6b, the `app.js` scope-exclusion claim, the ledger/blocked-scope claims): not re-derived from scratch this invocation, since none of it was touched by the correction and invocation 1's own record already traces it to specific code with direct citations. This invocation's own independent contribution is the RED-verification of the fix itself, plus 2 fresh non-vacuity spot-checks in areas invocation 1's own spot-check list did not cover (the bridge's consistency-check test coverage, and generated-page content verification).

### Status

GATE_FAIL
