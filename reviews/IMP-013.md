# Review record: IMP-013

## Assignment

- **Assignment ID**: `IMP-013`
- **Work type**: `implementation`
- **Primary route**: `claude_sonnet` — Sonnet, effort high
- **Reviewer route**: `codex_terra` — Terra, effort medium
- **Baseline commit**: `927bfd3`
- **Reviewed diff or commit**: uncommitted working-tree diff against `927bfd3`, including the declared modified and untracked artifacts
- **Scope**: Phase 1 step 7 fetch/refresh orchestration, its additive Places snapshot boundary, the `venues.json` wrapper-object correction, `holidays.json`, and the `Makefile` refresh target; excludes live API refresh, hand-curated return data, the outbound-mirror architecture, and verification of the four deferred movable holiday dates.
- **Finding disposition schema**: `factual-assessment/v1`

## Review round 1 — 2026-09-03

- **Lifecycle state reviewed**: `review_requested`
- **Reviewer route**: `codex_terra`
- **Runtime model verification**: per the standing constant in `WORKFLOW.md`; the runtime does not expose the selected model, so the user-selected Terra-medium route is relied on.
- **Reviewed artifact**: uncommitted working-tree diff against `927bfd3`, including `build/refresh.py`, `data/holidays.json`, `Makefile`, additive fetcher/generator changes, and their tests.

### Resolution of prior findings

None — first review round.

### Findings

No findings.

### Verification performed

- Read `HANDOFF.md` in full before inspecting `git status` and the full declared diff (including untracked artifacts), then read the load-bearing Phase 1 refresh contract in `PLAN.md`, reviewer/lifecycle rules in `WORKFLOW.md`, and the affected bridge/coarsening/fetcher/generator implementations.
- Independently traced the refresh order: coarsen precedes all fetches; source exceptions merge independently with last-known-good; the real bridge runs before `_write_venues_json_atomic`; a bridge exception propagates before replacement; per-venue `invalid` is stamped and continues to generation; generation runs only after replacement.
- Ran `.venv/bin/pytest tests/python/ -q` — 188 passed, 0 failed.
- Ran `node --test tests/js/*.test.js` — 184 passed, 0 failed.
- Ran `git diff --check 927bfd3` and `git diff --no-index --check /dev/null` for each untracked implementation/data artifact — no diagnostics.
- Ran an isolated copied-data dry-run with the real 28-venue registry, real coarsening/return-validator bridge/generation, and stubbed network fetchers — 28 venues written, every generated venue stamped, and `index.html` generated.

### Could not verify

- Live Google Places and SerpApi behavior with production credentials: step 8 is explicitly excluded and would spend API calls.
- The four deferred movable-date estimates in `data/holidays.json` against the official gazette: explicitly excluded from this assignment.
- Correctness of hand-curated `return_transport` and `holiday_return_policy` beyond the bridge completing against the current metadata: explicitly out of scope.

### User decisions required

None.

### Recommendation

`APPROVE`

---
