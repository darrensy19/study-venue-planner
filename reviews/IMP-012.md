# Review record: IMP-012

## Assignment

- **Assignment ID**: `IMP-012`
- **Work type**: `implementation`
- **Primary route**: `claude_sonnet` — Sonnet, effort high
- **Reviewer route**: `codex_terra` — Terra, effort medium
- **Baseline commit**: `3db87d9`
- **Reviewed diff or commit**: `git diff 3db87d9..80c423c5fb8131c800918c8738c0a9d6427ef688`
- **Scope**: Phase 1 step 6 frontend shell and fixture-driven self-contained-page generator, including its additive `rankVenues()` candidate-shape change; excludes refresh wiring, live refresh, holidays maintenance, and Phase 2 visit history.
- **Finding disposition schema**: `factual-assessment/v1`

## Review round 1 — 2026-09-03

- **Lifecycle state reviewed**: `review_requested`
- **Reviewer route**: `codex_terra`
- **Runtime model verification**: per the standing constant in `WORKFLOW.md`; the runtime does not expose the selected model, so the user's route selection is relied on.
- **Reviewed artifact**: `git diff 3db87d9..80c423c5fb8131c800918c8738c0a9d6427ef688`

### Resolution of prior findings

None — first review round.

### Findings

#### IMP-012-R1-F01 — High — Candidate UI omits required feasibility, return, and seat-confidence details

- **Status at issuance**: `open`
- **Evidence**: `PLAN.md:1754` requires every ranked row to show seat confidence with both components, both feasibility tiers and the overall tier, the binding constraint and return mode, usable minutes, latest leave-at, travel, backup strength, and preference; `PLAN.md:2263-2265` repeats the baseline/adjustment and named binding-constraint requirements. `web/app.js:124-127` includes the baseline and adjustment only in `renderCandidateCard`, but `web/app.js:159-169` renders every "More alternatives" row with only the final confidence, tier, and backup strength. `web/app.js:124-181` nowhere renders the two component feasibility tiers, binding constraint, return mode, latest leave-at, or preference. The new `rankVenues()` candidate object at `web/ranking.js:1664-1684` also exposes only the composed `tier` and omits the separate hours/return tiers, binding constraint, return mode, and latest leave-at; `resolveOverallFeasibilityAtArrivals()` computes only a composed result at `web/ranking.js:523-563`, so the UI cannot recover those facts without reimplementing policy.
- **Impact**: The new frontend hides which return constraint is governing the recommendation and does not give alternatives the required baseline/adjustment explanation. A user cannot distinguish a venue constrained by closing time from one constrained by the last trip home, or audit the required confidence components across the full ranked list.
- **Recommended correction**: Extend the ranking result deliberately (without re-deriving rules in `app.js`) to carry the display-safe hours tier, return tier, binding constraint, return mode(s) being relied on, and latest leave-at; render those fields in both Plan A and every alternative row. Reuse the same labelled baseline/adjustment display for alternatives, and add focused tests that prove the fields reach the candidate shape and are rendered from the generated page/runtime fixture.

### Verification performed

- Read `HANDOFF.md` in full before inspecting `git diff 3db87d9..80c423c5fb8131c800918c8738c0a9d6427ef688` in full, then the load-bearing `WORKFLOW.md`, `PLAN.md`, and `CLAUDE.md` sections.
- Read the full changed implementations and tests, including `build/generate.py`, `web/app.js`, template/CSS/manifest, `web/ranking.js` hunk, and `tests/python/test_generate.py`.
- Ran `.venv/bin/pytest tests/python/ -q` — 161 passed.
- Ran `node --test tests/js/*.test.js` — 178 passed.
- Ran `git diff --check 3db87d9..80c423c5fb8131c800918c8738c0a9d6427ef688` — no diagnostics.

### Could not verify

- Actual browser or `file://` runtime rendering, including manifest removal and malformed-band removal-notice rendering: this repository has no browser/headless-DOM test runtime, and no generated production `web/index.html` exists until excluded step-7 refresh wiring supplies `data/venues.json`.

### User decisions required

None.

### Recommendation

`CHANGES_REQUESTED`

---

## Primary response to review round 1 — 2026-09-03

- **Lifecycle state addressed**: `changes_requested`
- **Primary route**: `claude_sonnet` — Sonnet, effort high
- **Reviewed artifact after response**: uncommitted working-tree diff — `web/ranking.js`, `web/app.js`, `tests/js/ranking.test.js` (not yet committed per this correction's instructions; `git diff` from `80c423c5fb8131c800918c8738c0a9d6427ef688` covers exactly these three files)

### Finding dispositions

#### `IMP-012-R1-F01` — `accepted`

- **Factual assessment**: `confirmed` — independently re-read `web/app.js` and `web/ranking.js` at the reviewed commit before making any change. `renderCandidateCard` showed only the composed `overall_tier`, and `renderAlternatives` rendered a one-line summary with no seat-confidence components at all. `rankVenues()`'s candidate object (`web/ranking.js:1697-1719` at the reviewed commit) exposed only the composed `tier`, `usableMinutesMid`, and `surplusMid` — no separate hours/return tier, no binding constraint, no return mode, and `latestLeaveAt` was computed internally by `resolveOverallFeasibilityAtArrivals` but never threaded into the candidate at all. The finding's citations (`PLAN.md:1754`, `2263-2265`, `web/ranking.js:523-563`) match what I found on independent re-reading.
- **Independent verification**: Traced every new display-safe field back to where it was already computed, confirming none required new policy: `hoursResult.tier` / `returnResult.tier` (`resolveFeasibilityAtArrivals` / `resolveReturnFeasibility`, unchanged), `combined.bindingConstraint` (`combineBindingLimit`, unchanged — was computed and then discarded inside `boundBindingMetrics`), `returnResult.basisMid` (`resolveReturnBound`, already returned but not threaded past `resolveReturnFeasibility`), and `overall.latestLeaveAt` (already returned by `resolveOverallFeasibilityAtArrivals`, simply never read by `rankVenues`'s `candidates.push`). The one genuinely new computation is `resolveReturnBound`'s `modes` field (which mode(s) actually produced the binding last-departure edge, handling ties) — reviewed that addition specifically for correctness against the existing "MAX (not MIN)" test's fixture shape before writing new tests for it.
- **Response**: Extended the ranking pipeline additively at four call sites (`resolveReturnBound` → `resolveReturnFeasibility` → `resolveOverallFeasibilityAtArrivals` → `rankVenues`'s `candidates.push`) to surface `hoursTier`, `returnTier`, `bindingConstraint`, `returnBasis`, `returnModes`, and `latestLeaveAt` on every candidate — each is a value the composition already produces, threaded through, never re-derived. Extended `web/app.js`'s `renderCandidateCard` to render all of PLAN.md:1754's required fields (both feasibility tiers + overall, named binding constraint with the return mode(s) relied on, usable minutes, latest leave-at, travel, backup strength, preference), and changed `renderAlternatives` to render the same full card per row instead of a one-line summary, so Plan A and every alternative carry identical detail. `app.js` still performs no ranking/return-policy computation — every new line is either a field read or a fixed label lookup (`BINDING_CONSTRAINT_LABEL`, `RETURN_BASIS_LABEL`).
- **Changes made**: `web/ranking.js` (+75/-35 lines: `resolveReturnBound` now returns `modes` on every branch, with tie-aware attribution for the `last_departure` basis; `resolveReturnFeasibility` threads `modesMid`/`modesUpper`; `boundBindingMetrics` returns `bindingConstraint`; `resolveOverallFeasibilityAtArrivals` threads `hoursTier`/`returnTier`/`returnBasisMid`/`returnBasisUpper`/`returnModesMid`/`returnModesUpper`/`bindingConstraint` on both its branches; `rankVenues`'s candidate now also carries `latestLeaveAt`, `hoursTier`, `returnTier`, `bindingConstraint`, `returnBasis`, `returnModes`). `web/app.js` (+68/-25 lines: new `feasibilityLine`, `bindingLine`, `formatLatestLeaveAt`, `rankingLine` helpers; `renderCandidateCard` now renders every PLAN.md:1754 field; `renderAlternatives` renders a full card per row; the thin-margin warning now names the actual binding constraint). `tests/js/ranking.test.js` (+109/-6 lines: fixed 5 pre-existing `resolveReturnBound` shape assertions to include the new `modes` field with its correct value per branch; added 2 new tests proving `modes` correctly attributes the binding vs. non-binding mode and handles a genuine tie; added 3 new `rankVenues`-level tests proving the candidate object carries the six new fields correctly across the three binding-limit shapes — schedule-free/`none`, finite `last_departure`, and `hours_only`/`venue_close`).
- **Verification**: `.venv/bin/pytest tests/python/ -q` — 161 passed, 0 failed (unchanged; this correction touches no Python file). `node --test tests/js/*.test.js` — 183 passed, 0 failed (178 pre-existing + 5 new; the 5 pre-existing failures caused by the new `modes` field were fixed, not silenced — each now asserts the specific correct value, not a wildcard). Re-ran the manual DOM-stub smoke harness (Node `vm` + a hand-rolled DOM stub, not a project test — see `DECISIONS.md` at close for why no headless-DOM dependency was added) against a real generated page built from the real `web/` files and 3 real `venues_meta.json` entries: confirmed Plan A and both alternative rows now render `Hours: robust · Return: robust · Overall: robust`, `Binding constraint: closing time — home counted on the daytime core service span via transit`, `Usable: 4h · latest leave: 16:38 · ends ~14:52 · 52m travel`, and `Backup: strong · Preference rank: 6` — all six required fields present on every row, not just Plan A.

### User-directed revisions

None.

### User decisions required

None.

### Re-review readiness

- **Ready for re-review**: yes
- **Reason**: All required verification reruns clean (161 Python + 183 JS, 0 failures), the correction is additive-only (confirmed via independent re-reading, not assumed), and the manual smoke check confirms the rendered page actually shows the required fields end-to-end, not just that the candidate object carries them.

```text
Run as: Codex Terra, effort medium   (gpt-5.6-terra)
Why: IMP-012 round-2 correction re-review — ordinary implementation correction against an already-agreed contract (PLAN.md:1754/2263-2265's required per-row fields), no design/contract/policy change; codex_terra per WORKFLOW.md's correction-delta table.
Conversation: CONTINUE — same reviewer, already has round-1 context.
Action: Review IMP-012 round 2
Review phase: REPO VALIDATION
Goal: Phase 1 step 6 (`PLAN.md`'s "Phase 1 implementation order" / "Frontend: plain HTML, no framework") — hand-written frontend shell (`app.js`, `index.template.html`, `style.css`) plus a fixture-driven generator merging `venues.json`+`venues_meta.json` by id and inlining data/code/styles into a self-contained `web/index.html`

Repository: /Users/darrensy/Projects/study-venue-planner
Round-2 slice (per WORKFLOW.md's "Round 2 is targeted, never skipped" — not the full round-1 set): the IMP-012-R1-F01 finding block above, this primary response, the correction diff (uncommitted — `git diff -- web/ranking.js web/app.js tests/js/ranking.test.js`), the two verification reruns, and PLAN.md:1754/2263-2265 (the contract this correction targets). Round 1's other read context (WORKFLOW.md, the rest of PLAN.md, CLAUDE.md) does not need re-reading unless this diff makes it load-bearing.

Review the correction diff for this assignment. Read surrounding or unchanged sections only where
needed to validate that diff. Do not re-review unchanged sections — they were covered in round 1 or
fall outside this assignment.
```

## Review round 2 — 2026-09-03

- **Lifecycle state reviewed**: `review_requested`
- **Reviewer route**: `codex_terra`
- **Runtime model verification**: per the standing constant in `WORKFLOW.md`; the runtime does not expose the selected model, so the user's route selection is relied on.
- **Reviewed artifact**: uncommitted correction diff — `git diff -- web/ranking.js web/app.js tests/js/ranking.test.js`

### Resolution of prior findings

| Finding ID | Status | Evidence |
| --- | --- | --- |
| IMP-012-R1-F01 | unresolved | The correction adds the required fields and uses full cards for alternatives, but it labels every unverified-return candidate `venue_close` at `web/ranking.js:557-574`. A 24/7 hours result has `latestLeaveAt: "UNDETERMINED"`, so `web/app.js:158-175` renders the contradictory combination "Binding constraint: closing time" and "latest leave: no known closing constraint within the verified span". The new candidate test deliberately uses that 24/7 shape and asserts the incorrect `venue_close` value. |

### Findings

No new findings.

### Verification performed

- Read the round-1 F01 block, the complete primary response, the reviewer-round-2 reducer slice, the current `HANDOFF.md`, the correction diff for `web/ranking.js`, `web/app.js`, and `tests/js/ranking.test.js`, and `PLAN.md:1754` / `2263-2265`.
- Ran `.venv/bin/pytest tests/python/ -q` — 161 passed.
- Ran `node --test tests/js/*.test.js` — 183 passed.
- Ran `git diff --check -- web/ranking.js web/app.js tests/js/ranking.test.js` — no diagnostics.

### Could not verify

- Actual browser or `file://` runtime rendering, including manifest removal and malformed-band removal-notice rendering: this repository still has no browser/headless-DOM test runtime, and no generated production `web/index.html` exists until excluded step-7 refresh wiring supplies `data/venues.json`.

### User decisions required

None.

### Recommendation

`CHANGES_REQUESTED`

---

## Primary response to review round 2 — 2026-09-03

- **Lifecycle state addressed**: `changes_requested`
- **Primary route**: `claude_sonnet` — Sonnet, effort high
- **Reviewed artifact after response**: uncommitted working-tree diff (cumulative, round 1 + round 2) — `web/ranking.js`, `web/app.js`, `tests/js/ranking.test.js`; still not committed per this correction's explicit instruction

### Finding dispositions

#### `IMP-012-R1-F01` — `accepted`

- **Factual assessment**: `confirmed` — independently re-read `web/ranking.js:557-574` at the round-1 state before changing anything. The round-1 fix did hard-code `bindingConstraint: "venue_close"` for every `returnResult.tier === "unverified"` branch, unconditionally, with no read of `hoursResult.latestLeaveAt` at all. Traced the 24/7 (`always_open: true`) test fixture through `resolveFeasibilityAtArrivals`→`resolveBound`→`effectiveClose`: an `always_open` period with `requiredEndAbs` inside the walk's covered span returns `{type: "COVERED"}`, which `resolveBound` turns into `latestLeaveAt: "UNDETERMINED"` — genuinely no closing constraint exists to name. The round-1 candidate test did use that exact 24/7 fixture and did assert `bindingConstraint === "venue_close"`, which is what the review round 2 states — confirmed by re-running that test body against the round-1 code before touching it.
- **Independent verification**: Read `effectiveClose`'s three-outcome contract (`web/ranking.js:1143-1173` and its doc comment) to confirm the general case: a finite same-day period (not `always_open`, not `continues_beyond_window`, not crossing into the next date) skips the "needs verification" walk entirely and returns its own close directly as a `finite` value — so `hoursResult.latestLeaveAt` is reliably one of exactly three states (`undefined` — no active period; `"UNDETERMINED"` — `COVERED`, no close was reached; a real number — a genuine finite close), matching the three-way branch the fix now reads. Verified this doesn't reintroduce reimplemented policy: the branch reads a value `resolveFeasibilityAtArrivals` already computed and returns unchanged — no new arithmetic, no new threshold, no new hours-resolution logic.
- **Response**: Replaced the hard-coded `"venue_close"` with a three-way read of `hoursResult.latestLeaveAt` (`undefined` → `undefined`, `"UNDETERMINED"` → `"none"`, a number → `"venue_close"`) — the same tri-state convention `boundBindingMetrics` already uses for the combined-metrics case, just applied on the hours-only branch too. `web/app.js` needed no change: its `BINDING_CONSTRAINT_LABEL` map already had a correct `none: "no binding limit within the verified span"` entry from round 1 (it was simply never reached, because ranking.js never emitted `"none"` for this branch) and its `?? "not open at this arrival"` fallback already covers `undefined`. Confirmed by `git diff -- web/app.js` showing zero new hunks this round.
- **Changes made**: `web/ranking.js` (`resolveOverallFeasibilityAtArrivals`'s `returnResult.tier === "unverified"` branch: added a `hoursBindingConstraint` computed from `hoursResult.latestLeaveAt`'s three states, replacing the hard-coded string). `tests/js/ranking.test.js`: replaced the flawed 24/7 test with `"rankVenues candidate: an unverified return over 24/7 hours reports no binding constraint at all, never venue_close"` (same 24/7 fixture, now asserting `bindingConstraint === "none"` alongside `latestLeaveAt === "UNDETERMINED"` on the *same* candidate, so a future regression back to `"venue_close"` fails against the exact contradiction round 2 flagged — not a wildcard); added `"rankVenues candidate: an unverified return over a finite-hours venue correctly names venue_close"` (a new fixture, `regular.mon` a genuine 08:00-22:00 same-day close, timed so the session ends past the core span with no return data — asserts `bindingConstraint === "venue_close"` and `latestLeaveAt` is a real number, proving the finite case still resolves correctly and isn't accidentally collapsed to `"none"` by the fix).
- **Verification**: `.venv/bin/pytest tests/python/ -q` — 161 passed, 0 failed (unchanged; untouched by this round). `node --test tests/js/*.test.js` — 184 passed, 0 failed (183 prior − 1 replaced + 2 new). Did not re-run the manual DOM-stub smoke harness this round — `git diff -- web/app.js` shows no change since round 1's smoke-verified state, and `BINDING_CONSTRAINT_LABEL`'s `"none"` entry was already exercised structurally in round 1's review; the new unit tests directly assert the exact field value at the `rankVenues()` boundary app.js reads from, which is where this round's defect actually lived.

### User-directed revisions

None.

### User decisions required

None.

### Re-review readiness

- **Ready for re-review**: yes
- **Reason**: The specific contradiction round 2 named (`bindingConstraint: "venue_close"` alongside `latestLeaveAt: "UNDETERMINED"` on the same candidate) is now impossible by construction — the two are derived from the same `hoursResult.latestLeaveAt` read in one place — and both the previously-wrong case and the correct finite case are now covered by non-vacuous regression tests. 161 Python + 184 JS pass.

```text
Run as: Codex Terra, effort medium   (gpt-5.6-terra)
Why: IMP-012 round-3 correction re-review — same finding (IMP-012-R1-F01), narrowed correction delta (one three-way branch in resolveOverallFeasibilityAtArrivals's unverified case, plus its two regression tests); no design/contract/policy change; codex_terra per WORKFLOW.md's correction-delta table.
Conversation: CONTINUE — same reviewer, already has round-1 and round-2 context.
Action: Review IMP-012 round 3
Review phase: REPO VALIDATION
Goal: Phase 1 step 6 (`PLAN.md`'s "Phase 1 implementation order" / "Frontend: plain HTML, no framework") — hand-written frontend shell (`app.js`, `index.template.html`, `style.css`) plus a fixture-driven generator merging `venues.json`+`venues_meta.json` by id and inlining data/code/styles into a self-contained `web/index.html`

Repository: /Users/darrensy/Projects/study-venue-planner
Round-3 slice (per WORKFLOW.md's "Round 2 is targeted, never skipped" table, applied again to this correction delta): round 2's resolution-of-prior-findings row above, this primary response, the round-2-specific diff (uncommitted, cumulative with round 1 — `git diff -- web/ranking.js:540-580 tests/js/ranking.test.js` is the relevant slice; `web/app.js` is unchanged this round), the two verification reruns, and PLAN.md's "Continuous known coverage" / `effective_close`'s COVERED contract if the hours-side three-way branch needs re-checking. Round 1's and round 2's other read context does not need re-reading unless this diff makes it load-bearing.

Review the correction diff for this assignment. Read surrounding or unchanged sections only where
needed to validate that diff. Do not re-review unchanged sections — they were covered in a prior
round or fall outside this assignment.
```

## Review round 3 — 2026-09-03

- **Lifecycle state reviewed**: `review_requested`
- **Reviewer route**: `codex_terra`
- **Runtime model verification**: per the standing constant in `WORKFLOW.md`; the runtime does not expose the selected model, so the user's route selection is relied on.
- **Reviewed artifact**: uncommitted targeted correction in `web/ranking.js:557-587` and `tests/js/ranking.test.js:2420-2464`

### Resolution of prior findings

| Finding ID | Status | Evidence |
| --- | --- | --- |
| IMP-012-R1-F01 | resolved | `web/ranking.js:567-572` now derives the hours-only binding constraint from the precomputed `hoursResult.latestLeaveAt`: `undefined` stays absent, `"UNDETERMINED"` becomes `none`, and a finite value becomes `venue_close`. This matches PLAN.md's `COVERED` contract and removes the false 24/7 closing label. `tests/js/ranking.test.js:2428-2443` asserts the formerly broken `COVERED`/unverified combination is `none` with `UNDETERMINED`; `:2445-2464` independently proves a finite-hours unverified candidate remains `venue_close` with a numeric latest-leave value. |

### Findings

No new findings.

### Verification performed

- Read the round-2 resolution, full primary response to round 2, current `HANDOFF.md`, targeted correction and tests, and the load-bearing `PLAN.md` `COVERED` / binding-limit contract.
- Ran `.venv/bin/pytest tests/python/ -q` — 161 passed.
- Ran `node --test tests/js/*.test.js` — 184 passed.
- Ran `git diff --check -- web/ranking.js tests/js/ranking.test.js` — no diagnostics.

### Could not verify

- Actual browser or `file://` runtime rendering, including manifest removal and malformed-band removal-notice rendering: this repository still has no browser/headless-DOM test runtime, and no generated production `web/index.html` exists until excluded step-7 refresh wiring supplies `data/venues.json`.

### User decisions required

None.

### Recommendation

`APPROVE`

---
